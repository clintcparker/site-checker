/// <reference types="vite/client" />

// The standard Vite scaffold file, which this project had not needed until now.
// It is what types Vite's own import suffixes — specifically `?raw`, which
// `about.test.ts` uses to read the shipped `index.html` as a string so its
// fixture cannot drift from the markup that ships. `vite` is already a
// devDependency; nothing new is installed for this.
