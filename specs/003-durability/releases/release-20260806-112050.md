# Release Record — 003-durability

**Shipped**: 2026-08-06 11:20:50 · **Run**: `/speckit-ship-run`, unattended (no user present;
every confirmation auto-approved)

## Pull Request

- **PR**: [#3 — feat: durability & data integrity — sites.json saves are now atomic](https://github.com/clintcparker/site-checker/pull/3)
- **Branch**: `20260806-102818-durability-and-data` → `main`
- **Commit range**: `a1769a8..56c025d897ae303e9791dc5fdb129cde15b31a5d`
- **Head SHA**: `56c025d897ae303e9791dc5fdb129cde15b31a5d`
- **Commits**: 5 · **Files changed**: 28 (+2341 / −14)
- **Description**: [pr-description-20260806-112050.md](./pr-description-20260806-112050.md)

### Commits

| SHA | Subject |
|---|---|
| `00fd10c` | docs: before screenshots for 003-durability |
| `9205a5e` | docs: after screenshots for 003-durability |
| `6b4db7e` | feat: make sites.json saves atomic, lowercase URL schemes, guard duplicate ids |
| `d565431` | docs: spec-kit artifacts and changelog for 003-durability |
| `56c025d` | chore: companion bytecode regenerated by this run |

The two screenshot commits predate this ship run. `00fd10c` was authored on `main` in the
primary checkout and was still unpushed when this run began; it is an ancestor of the branch,
so the PR carries it.

## Readiness Check

```
Ship Readiness Check:
✅ Tasks:        30/30 complete (checklists/requirements.md 16/16)
➖ Review:       no reviews/ directory — /speckit-review was never run
➖ QA:           no qa/ directory — /speckit-qa was never run
✅ Working tree: clean after 3 commits made by this run
✅ Sync:         0 commits behind origin/main — no rebase needed
✅ Screenshots:  before + after, both views, both sizes, no capture failures

Overall: READY TO SHIP (with notes)
```

Notes carried into the PR rather than blocking:

1. No review or QA report exists. Neither command was run for this feature, so there is no
   verdict in either direction. The automated gates and the live-app confirmation stand in.
2. `tasks.md` declares the manual `pnpm tauri dev` pass undone. It was subsequently performed
   during the after-screenshots pass on this branch; the PR reconciles this rather than
   leaving the contradiction standing.

## Quality Gates

Run on the head of this branch during this ship run, in the worktree:

| Gate | Result |
|---|---|
| `cargo test` | 42 passed / 0 failed (baseline was 29) |
| `cargo clippy --all-targets -- -D warnings` | clean |
| `pnpm test` | 30 passed / 0 failed (unchanged — no frontend file touched) |
| `pnpm build` | succeeds |

## CI

**None configured.** No `.github/workflows/` directory exists in the repository, and
`gh run list --branch 20260806-102818-durability-and-data` returns no runs. Not a pending or
failed pipeline — there is no pipeline. Recorded in the PR description rather than waited on,
per the unattended run's instructions. The four gates above were run locally as the
substitute.

## Changelog

Already present on the branch before this run — written during implementation and committed
here in `d565431`. No new entry was prepended; doing so would have duplicated it.

Entry: **"Durability & data integrity — 2026-08-06"** at the top of `CHANGELOG.md`, with
`### Fixed` (atomic saves, duplicate-id guard), `### Changed` (lowercase scheme, symlink
replacement), and `### Added` (13 new Rust tests, 29 → 42).

## Artifacts

| Artifact | Status |
|---|---|
| `spec.md` | present — 3 stories, 11 FRs, 8 SCs |
| `plan.md` | present — constitution check all PASS, no violations |
| `research.md` | present — R1–R9 |
| `data-model.md` | present |
| `contracts/store-write-path.md`, `contracts/normalize-url.md` | present |
| `quickstart.md` | present |
| `checklists/requirements.md` | 16/16 complete |
| `tasks.md` | 30/30 complete |
| `reviews/` | **absent** — never run |
| `qa/` | **absent** — never run |
| `screenshots/` | before + after, 2 views × 2 sizes = 8 images, manifest with both passes' notes |

`docs/ROADMAP.md` section 1 was drained (SC-008) and two items were explicitly re-deferred
(research R5's symlink correction, R7's `add_site` refusal-vs-write-failure inconsistency).
`docs/` is gitignored in this repo, so that edit is local-only and does not appear in the PR
diff. Verified present on the branch during this run.

## Screenshots

Linked in the PR as `https://github.com/clintcparker/site-checker/blob/56c025d.../…?raw=true`
— the repository is private, so `raw.githubusercontent.com` would not render. Pinned to the
head SHA rather than the branch name so they keep resolving after the branch is deleted on
merge.

Verified two ways: `git ls-tree` against `origin/20260806-102818-durability-and-data`, and the
GitHub contents API at the head SHA, which returned all 8 blobs with matching sizes. The
rendered PR body keeps the URLs un-proxied, so they resolve with the viewer's own session.

## Follow-ups left for a human

1. **`56c025d` (`chore: companion bytecode…`) is droppable.** Five machine-specific `.pyc`
   files, committed only because this unattended run was told to leave no uncommitted change
   behind. Isolated to its own commit for exactly this reason. That `__pycache__` directory is
   worth gitignoring, which would also mean untracking the two `.pyc` files already tracked.
2. **The main checkout will refuse to pull after merge.** Untracked stale copies of
   `specs/003-durability/tasks.md` and `specs/003-durability/.spec-context.json` sit in
   `/Users/clint/src/clintcparker/site-checker`. The `tasks.md` there is a pre-plan draft that
   contradicts the shipped design in two places. Delete both before pulling. They were left in
   place rather than removed by this run because deleting a user's files unprompted is not
   this command's call.
3. **Safety copies of the live `sites.json`** remain at `/tmp/sites.json.safety` and
   `~/sites.json.003-durability.safety` (T004). Both verified byte-identical to the current
   live file during this run. Safe to remove.
4. **Living specs**: `living-specs.yml` registers capability `backend` matching
   `src-tauri/src/**` with nothing registered yet. Both changed files fall under it, so a
   `/speckit-companion-living-*` pass may be wanted. Out of scope here, as `plan.md` noted.

---

*Generated by `/speckit-ship-run`.*
