# QA Report: Retire the Node.js 20 Deprecation Warning in CI (re-run after fix)

**QA Mode**: CLI QA
**Date**: 2026-08-11 14:39 PDT
**Feature**: [tasks.md](../tasks.md) — no `spec.md` exists (feature bypassed `/speckit-specify` and `/speckit-plan`)
**Environment**: macOS 26.6 (arm64), Node v22.16.0, pnpm 10.30.3, rustc 1.94.1, actionlint (Homebrew), `gh` CLI authenticated — no application URL (this feature has no runtime surface)
**Verdict**: ⚠️ PARTIAL PASS

---

## QA Summary

This is the second and final QA run. The [first run](qa-20260811-143616.md) returned ❌ FAILURES FOUND on a single scenario — TC-014, the pinning-convention comment T018 added to `ci.yml`, which asserted a rule that two of its own six pins break. That comment was rewritten and this run re-verified every factual claim in it against primary sources. **TC-014 now passes**, and no other scenario regressed: the pins, the YAML, `actionlint`, and all four constitution gates are unchanged and green.

All fifteen locally-runnable scenarios now pass. Every one of the seven distinct actions across `ci.yml` and `release.yml` resolves to an existing tag whose `action.yml` declares `runs: using: node24`; every input the workflows pass is still accepted at those refs; the `upload-artifact@v6` → `download-artifact@v7` → `action-gh-release@v3` round-trip still deposits files where the `release` job's globs look for them; and zero `@v4` pins remain across 20 `uses:` lines.

**The verdict is PARTIAL PASS, not ALL PASSED, and the reason is verification depth rather than any known defect.** The acceptance criterion for all three user stories is the *absence of a deprecation annotation on a real GitHub Actions run*, and that has no local equivalent. Five scenarios remain blocked on GitHub-side events — a PR run (T007), a real tag push (T015), a merge to the default branch (T017), and a post-merge run (T020). Nothing on this branch has yet executed on a live runner. The `release.yml` path in particular is verified **by inspection only**: the pins are correct at their tags and the inputs all still exist, but the artifact round-trip has not been exercised end to end, and the first tag push after merge is its real test.

No scenario in this report was skipped for want of GUI automation. `specs/20260811-141602-ci-node24-action-bumps/screenshots/` contains `SKIPPED.md` rather than a `manifest.json` — the screenshots pass correctly established from `tasks.md` that this feature touches no path under `src/`, `index.html`, or `src-tauri/`, and never launched the app. TC-017 re-confirms that prediction against the actual diff: the app-surface diff is empty.

---

## Test Results

| ID | User Story | Scenario | Mode | Result | Evidence |
|----|-----------|----------|------|--------|----------|
| TC-001 | US1 | `ci.yml` parses as YAML and `actionlint` reports no diagnostics | CLI | ✅ | `yaml.safe_load` OK; `actionlint` exit 0 (re-run post-fix) |
| TC-002 | US1 | Every JS action pinned in `ci.yml` resolves to a real tag whose `runs.using` is `node24` | CLI | ✅ | [tc003-runs-using.txt](responses/tc003-runs-using.txt) |
| TC-003 | US1 | `setup-node@v6` still accepts `node-version` and `cache`; both preserved verbatim | CLI | ✅ | [tc006-inputs-preserved.txt](responses/tc006-inputs-preserved.txt); `ci.yml:94-97` |
| TC-004 | US1 | No `@v4` pin remains; `rust-cache@v2` and `rust-toolchain@stable` untouched | CLI | ✅ | [tc002-pin-inventory.txt](responses/tc002-pin-inventory.txt) — 20 `uses:` lines, zero `@v4` |
| TC-005 | US1 | A live `CI` run reports an empty annotation list on both `rust` and `frontend` | CLI | 🔵 | Blocked — T007; `ci.yml` triggers on `push: main` and `pull_request` only, so no run exists until a PR does |
| TC-006 | US2 | `release.yml` parses as YAML and `actionlint` reports no diagnostics | CLI | ✅ | `yaml.safe_load` OK; `actionlint` exit 0 |
| TC-007 | US2 | Every JS action pinned in `release.yml` resolves to a real tag whose `runs.using` is `node24` | CLI | ✅ | [tc003-runs-using.txt](responses/tc003-runs-using.txt) — all 6 distinct actions |
| TC-008 | US2 | `upload-artifact@v6`, `download-artifact@v7`, `action-gh-release@v3` still accept every input passed | CLI | ✅ | [tc006-inputs-preserved.txt](responses/tc006-inputs-preserved.txt) |
| TC-009 | US2 | The artifact round-trip still resolves the `release` job's `files:` globs | CLI | ✅ | `merge-multiple` still defaults `'false'` at v7 → per-artifact directories; `release.yml:246-247` globs match `artifacts/site-checker-<arch>/…` |
| TC-010 | US2 | `Swatinem/rust-cache@v2` left untouched and is already `node24` | CLI | ✅ | [tc003-runs-using.txt](responses/tc003-runs-using.txt); `release.yml:99,145` absent from the diff |
| TC-011 | US2 | A live `Release` run is annotation-free and both arch artifacts attach | CLI | 🔵 | Blocked — T015; requires a real tag push that publishes a release and writes to `clintcparker/homebrew-tap` |
| TC-012 | US3 | `.github/dependabot.yml` parses and every key is valid Dependabot v2 schema | CLI | ✅ | [tc010-dependabot-schema.txt](responses/tc010-dependabot-schema.txt) |
| TC-013 | US3 | Dependabot accepts the config with no parse error | CLI | 🔵 | Blocked — T017; Dependabot parses the file only once it lands on the default branch |
| TC-014 | Polish (T018) | The `ci.yml` pinning-convention comment accurately describes the pins in the same change | CLI | ✅ **fixed** | [rerun-tc014-comment-claims.txt](responses/rerun-tc014-comment-claims.txt) — all 6 claims re-verified |
| TC-015 | Polish (T019) | `docs/how-to/release.md` is tracked and its runner-floor claim is true | CLI | ✅ | `git ls-files` returns the path; ≥ 2.327.1 confirmed from `checkout@v5.0.0`, `setup-node@v5.0.0`, `upload-artifact@v6.0.0`, `download-artifact@v7.0.0` release notes |
| TC-016 | Cross-cutting | All four constitution quality gates green | CLI | ✅ | See Test Suite Results |
| TC-017 | Cross-cutting | No app surface touched — user-visible behavior unchanged | CLI | ✅ | `git diff -- src index.html src-tauri` empty |
| TC-018 | Cross-cutting | `verify-install-channels.yml` contains no actions and was not modified | CLI | ✅ | Zero `uses:` lines; absent from `git status` |
| TC-019 | Cross-cutting (T002) | The ≥ 2.327.1 runner floor is satisfied for every job | CLI | ✅ | `gh api …/actions/runners` → `total_count: 0`; all jobs on `macos-latest`/`ubuntu-latest` |
| TC-020 | Polish (T020) | The newest `CI` run on `main` post-merge has an empty annotation list | CLI | 🔵 | Blocked — post-merge by definition |

**Legend**: ✅ Pass | ❌ Fail | ⚠️ Partial | 🔵 Skipped

---

## Acceptance Criteria Coverage

`tasks.md` is the authority; its three user stories and their stated "Independent test" lines stand in for acceptance criteria.

| Criterion | Test ID(s) | Status | Notes |
|-----------|-----------|--------|-------|
| **US1** — `CI` produces zero Node 20 deprecation annotations on `rust` and `frontend`, both still `success` | TC-001–TC-005 | ⚠️ Met by inspection, unconfirmed live | Every pin in `ci.yml` verified `node24` at its tag. The annotation list itself cannot be read until a PR run exists (T007). |
| **US2** — `Release` emits no Node 20 annotations across all five jobs | TC-006–TC-011 | ⚠️ Met by inspection, unconfirmed live | All six actions `node24`; inputs and artifact round-trip verified at the target refs. Needs a real tag push (T015). |
| **US3** — a future deprecation surfaces as a dependency PR, not an unread annotation | TC-012, TC-013 | ⚠️ Met by inspection, unconfirmed live | Config is schema-valid and correctly scoped to `github-actions`. Dependabot only parses it after merge (T017). |
| **T018** — the pinning convention is recorded honestly alongside the existing CI comments | TC-014 | ✅ Met | Was ❌ in run 1; comment rewritten and every claim re-verified against `action.yml` and release notes. |
| **T019** — the runner floor is noted in a tracked doc | TC-015 | ✅ Met | File is tracked despite `docs/` being gitignored; the 2.327.1 figure is correct at every action that states one. |
| **T002** — no self-hosted runner defeats the runner floor | TC-019 | ✅ Met | |
| Constitution quality gates hold | TC-016 | ✅ Met | |
| No scope creep into app behavior | TC-017, TC-018 | ✅ Met | |

**Coverage**: 8/8 criteria exercised (100%); 8/8 met to the depth reachable locally; the 3 user-story criteria await live confirmation.

---

## Test Suite Results

| Test Suite | Total | Passed | Failed | Skipped | Coverage |
|-----------|-------|--------|--------|---------|----------|
| `cargo test --locked` (src-tauri) | 62 | 62 | 0 | 0 | not measured |
| `pnpm test` (vitest, 6 files) | 47 | 47 | 0 | 0 | not measured |
| `cargo clippy --all-targets -- -D warnings` | — | clean | 0 | — | — |
| `actionlint` (3 workflows) | — | exit 0 | 0 | — | — |
| YAML parse (4 config files) | 4 | 4 | 0 | 0 | — |

No pre-existing failures and no new ones, before or after the fix. Zero source files changed on this branch, so these gates confirm the change is inert with respect to application behavior — they do not and cannot confirm the deprecation warning is gone.

---

## Failure Details

No failures in this run.

### Resolved since run 1 — TC-014: `ci.yml` pinning-convention comment

**Run 1 status**: ❌ FAIL — the comment claimed every JS action "sits on the lowest major that runs on node24", which is false for `actions/setup-node` and `pnpm/action-setup` (both pinned v6; both already `node24` at v5).

**Fix applied**: `.github/workflows/ci.yml:10-27` — the comment now states the rule and then names the two deliberate exceptions with the reason each v6 is a no-op for this repo. No pin, input, or job was touched; the diff is comment-only, and `grep -n "uses:"` returns the same six lines for `ci.yml` as before.

**Re-verification** — every factual claim in the rewritten comment, checked against primary sources rather than against `tasks.md`:

| Claim in the comment | Verified how | Result |
|---|---|---|
| The four non-exception pins really are the lowest `node24` major | Read `action.yml` at the major *below* each pin | ✅ `checkout@v4`, `upload-artifact@v5`, `download-artifact@v6`, `action-gh-release@v2` — all `node20` |
| `setup-node@v5` and `pnpm/action-setup@v5` "are already node24" | Same method at v5 | ✅ Both `node24` — this is what run 1's failure turned on |
| "all three of its call sites pass `cache: pnpm`" | `grep` for `actions/setup-node@v6` and the input below it | ✅ 3 call sites (`ci.yml` ×1, `release.yml` ×2), 3 passing `cache: pnpm` |
| "v6 of setup-node narrows its automatic caching to npm only" | `setup-node` v6.0.0 release notes | ✅ Breaking change: "Limit automatic caching to npm" (PR #1374) |
| action-setup v6 "still self-updates to the `packageManager` version package.json pins" | `action-setup` v6.0.7 notes + `package.json:5` | ✅ "self-update bootstrap to packageManager-pinned version" (PR #256); `"packageManager": "pnpm@10.30.3"` |
| Runner floor "≥ 2.327.1" | `checkout@v5.0.0` release notes | ✅ States minimum compatible runner `v2.327.1` |

**Note**: the first rewrite of this comment said "both call sites" for `setup-node`; there are three. That was caught and corrected within this run before the scenario was marked passing.

---

## Environment Info

| Property | Value |
|----------|-------|
| Operating System | macOS 26.6 (Darwin 25.6.0, arm64) |
| Browser | n/a — no UI surface in this feature |
| Runtime | Node v22.16.0, pnpm 10.30.3, rustc 1.94.1 |
| Application URL | n/a — the app was never launched; nothing in this change reaches it |
| Test Runner | `cargo test`, `vitest`, `actionlint`, `gh` CLI, `python3 -c 'yaml.safe_load'` |

**On the absence of a screenshots manifest**: there is no `screenshots/manifest.json` for this feature — the screenshots pass wrote `SKIPPED.md` instead, having determined from `tasks.md` that no user-visible surface changes. So there is no drivability record to cite, and none was needed: no scenario here requires driving the app. The five 🔵 scenarios are blocked on GitHub-side events, not on automation capability.

---

## Metrics Summary

| Metric | Value |
|--------|-------|
| Total scenarios | 20 |
| ✅ Passed | 15 |
| ❌ Failed | 0 |
| ⚠️ Partial | 0 |
| 🔵 Skipped (all externally blocked) | 5 |
| Pass rate | 75% overall; 100% of locally-runnable scenarios (15/15) |
| Acceptance criteria coverage | 100% exercised (8/8) |
| Action tags resolved against GitHub | 13 (7 pinned + 6 one-major-below) |
| Constitution gates green | 4 / 4 |
| Scenarios fixed since run 1 | 1 (TC-014) |

---

## Carry into the PR

Not defects — facts a reader of the PR needs so "the pins merged" does not read as "the warning is confirmed gone".

- **T007, T015, T017, T020 are open** and cannot be closed from a local checkout. US1's success criterion is confirmed by the first `CI` run of the PR itself, not by this report.
- **`release.yml` is verified by inspection only.** The artifact round-trip through `upload-artifact@v6` → `download-artifact@v7` → `action-gh-release@v3` has not been exercised; the first tag push after merge is the real test.
- **R002 from the [staff review](../reviews/review-20260811-143026.md) is unaddressed by design.** `.github/dependabot.yml` has no `ignore:` entries, so its first weekly run will propose `download-artifact@v8`, `upload-artifact@v7`, and `checkout@v6` — the three majors `tasks.md` deliberately declined. That is a review finding about future behavior, not a failed acceptance criterion: T016's stated criterion (a `github-actions` entry at `/` on a weekly schedule) is met and TC-012 passes. Flagged here so the choice to ship without `ignore:` is a decision rather than an oversight.
- **`actionlint` is not a CI gate.** It was run by hand here and in the staff review. A malformed workflow edit is caught only by someone running it manually or by the run failing — most exposed on a branch that edits workflows exclusively.

---

*Generated by `/speckit.qa` — Systematic QA testing for spec-driven development.*
