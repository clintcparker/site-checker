# QA Report: Retire the Node.js 20 Deprecation Warning in CI (sixth run — independent re-execution; adds production evidence for the R001 fix)

**QA Mode**: CLI QA
**Date**: 2026-08-12 11:25 PDT
**Feature**: [tasks.md](../tasks.md) — no `spec.md` or `plan.md` exists (feature bypassed `/speckit-specify` and `/speckit-plan`)
**Branch**: `main` @ `59db8a8` + uncommitted working tree (6 modified files); `origin/main` is at `457f572`
**Environment**: macOS 26.6.1 (arm64, build 25G76), Node v22.16.0, pnpm 10.30.3, rustc 1.94.1, Ruby 3.4.2, actionlint 1.7.12 (Homebrew, go1.26.3), `gh` CLI authenticated as `clintcparker` — no application URL (this feature has no runtime surface)
**Verdict**: ⚠️ PARTIAL PASS

---

## QA Summary

This is an independent re-execution of the full plan at the same tree state as
[run 5](qa-20260812-112100.md), which landed at 11:22 — three minutes before this run started.
Nothing was carried forward: every `action.yml` was re-read live at its pinned ref, the Gem
requirement grammar was re-executed in Ruby, annotations were re-pulled from the GitHub API, and all
four quality gates were re-run. **Every result agrees with run 5.** Two scenarios add evidence run 5
did not have.

**The new evidence closes the open half of the R001 fix.** Run 5 verified that
`Gem::Requirement.new("8")` compiles to `= 8` and is satisfied by `8` and `8.0.0` but *not* by
`8.1.2`, and correctly noted that this makes the fix depend on an unobserved premise: that Dependabot
names a tag-pinned action's version as a bare major rather than a full semver. That premise is now
observed, from production. PR #28's own commit trailer reads
`dependency-name: actions/download-artifact` / `dependency-version: '8'`, and the same bare-integer
form for `checkout`, `setup-node`, and `upload-artifact` — all `'7'`. The corrected `versions: ["8"]`
entry is an exact match against the string Dependabot actually emitted for the very upgrade the
broken entry failed to decline. **The syntax fix is verified against the incident that exposed it**,
not merely against a Ruby REPL. What remains unobservable is only the end-to-end behavior — whether
Dependabot honors an ignore it has no occasion to apply — which is a coverage gap with no available
action, not an unverified claim.

**The second new scenario asks whether the PR #28 sequence can recur, and finds the answer is a
process gap rather than an automation one.** `allow_auto_merge` is `false` on the repository, `main`
carries no branch protection, no workflow auto-merges Dependabot PRs, and PR #28 carries
`autoMergeRequest: null` with zero reviews — it was merged by `clintcparker` three minutes after it
opened. So nothing in the configuration merged that PR; a person did, on a green check. Meanwhile
four next-majors are now reachable and undeclined (`checkout` v8, `setup-node` v8, `upload-artifact`
v8, `download-artifact` v9) under a group that still bundles `patterns: ['*']` into one PR. That is
the *correct* configuration for US3's stated goal — a future deprecation must surface as a PR — but
it means the same one-PR-four-majors shape will reappear, and the ignore block cannot stop it,
because ignore entries only decline named versions. This is recorded as a partial, not a failure:
the config does what US3 asked for.

**Everything run 5 established re-verifies.** All four YAML files parse; `actionlint` exits 0; 20
`uses:` lines across 8 distinct actions with zero `@v4`; all seven JS-action pins resolve to a real
tag whose `runs.using` is `node24`, each read live at the pinned ref (`checkout@v7`,
`setup-node@v7`, `action-setup@v6`, `upload-artifact@v7`, `download-artifact@v8`, `gh-release@v3`,
`rust-cache@v2`), and `dtolnay/rust-toolchain@stable` is `composite` with no JS runtime. Every input
the workflows pass is accepted at its ref. `digest-mismatch` exists at v8, is absent at v7, and its
own description reads "Options: ignore, info, warn, error. Default is error" — so `warn` is
documented, and it is what the `release` job passes. `merge-multiple` is `'false'` at both v7 and v8
and `skip-decompress` is `'false'` at v8, so the `release` job's
`artifacts/site-checker-<arch>/…` globs still resolve. `upload-artifact@v7`'s `archive` defaults to
`'true'`. All four constitution gates green: **62 Rust tests, 47 frontend tests, `cargo clippy
--all-targets -- -D warnings` clean, `actionlint` exit 0.**

**The CHANGELOG's arithmetic was re-derived from the diff rather than read.** PR #27's range
`b64c0e2..903d524` adds exactly **14** `uses:` lines across exactly **6** distinct actions
(`checkout` ×5, `setup-node` ×3, `action-setup` ×3, `upload-artifact` ×1, `download-artifact` ×1,
`gh-release` ×1), against a tree total of 20 lines and 8 actions. "Fourteen of the twenty `uses:`
lines, across six of the eight distinct actions" is exactly right, and T001's corrected count of 8
distinct actions matches `grep`.

**US1 is confirmed live, with the delta to the working tree stated precisely.** CI run
[31544339526](https://github.com/clintcparker/site-checker/actions/runs/31544339526) at
`457f572` — the tip of `origin/main` — concluded `success` on both `rust` and `frontend`, with both
`check-runs/*/annotations` returning length 0 and zero messages matching "Node.js 20 is deprecated".
The working tree is ahead of that run by one commit (`59db8a8`, docs only) plus six modified files.
The executable delta was measured, not assumed: **`ci.yml`'s diff against `origin/main` is
comment-only** (every changed non-`#` line is blank), and `release.yml`'s single executable change is
the added `digest-mismatch: warn`, which lives on the `release` job that `ci.yml` never invokes. So
the pin set CI validated is the pin set in the tree, and no uncommitted change can have disturbed the
US1 result.

**The verdict is PARTIAL PASS for three reasons, all narrow, none new, and none a code defect.**
T015 has still never run — the `upload → download → gh-release` round-trip has not been exercised at
any pin, and it now carries a `digest-mismatch: warn` placed by inspection. The corrected Dependabot
syntax cannot be observed working, because every entry is inert against the current pins (each names
a version the repo already runs, and Dependabot only proposes upgrades) — the file says so in the
file, which is the right handling. And one comparative clause in `release.yml`'s comment is
under-qualified against the repo's own docs; it is a rationale, not a behavior, and both remedies are
the maintainer's call.

**No scenario in this report was skipped for want of GUI automation, and no drivability claim is
made in either direction.** `specs/20260811-141602-ci-node24-action-bumps/screenshots/` contains
`SKIPPED.md` and **no `manifest.json`** — the before pass skipped rather than captured, so it never
wrote one. There is therefore no `capture_blocked` field to cite, **no drivability record, and no
record of any interaction having been performed** — not a click, not a keystroke, not a window
resize. `SKIPPED.md` states across all three of its passes that the app was never launched and
`sites.json` was never seeded, backed up, or restored. None of that is needed here: this feature
touches no path under `src/`, `index.html`, or `src-tauri/` (TC-018 re-confirms this against both the
working tree and the full merged range `b64c0e2..HEAD`), and no scenario in this plan requires
driving the app. The two 🔵 scenarios are each blocked on a named external event — a tag push and a
Dependabot cycle — not on automation capability.

---

## Test Results

| ID | User Story | Scenario | Mode | Result | Evidence |
|----|-----------|----------|------|--------|----------|
| TC-001 | US1 | `ci.yml` parses as YAML and `actionlint` reports no diagnostics | CLI | ✅ | [run6-tc001-static.txt](responses/run6-tc001-static.txt) — `yaml.safe_load` OK; `actionlint` exit 0 |
| TC-002 | US1 | Every JS action pinned in `ci.yml` resolves to a real tag whose `runs.using` is `node24` | CLI | ✅ | [run6-tc002-runs-using.txt](responses/run6-tc002-runs-using.txt) — `checkout@v7`, `setup-node@v7`, `action-setup@v6`, `rust-cache@v2` all `node24`; every tag resolves to a SHA |
| TC-003 | US1 | `setup-node@v7` still accepts `node-version` and `cache`; both preserved verbatim at all three call sites | CLI | ✅ | [run6-tc006-inputs.txt](responses/run6-tc006-inputs.txt) — both inputs present at v7; `ci.yml:115-116`, `release.yml:107-108,154-155` all pass `node-version: 22` / `cache: pnpm` |
| TC-004 | US1 | No `@v4` pin remains; `rust-cache@v2` and `rust-toolchain@stable` untouched | CLI | ✅ | [run6-tc001-static.txt](responses/run6-tc001-static.txt) — 20 `uses:` lines, 8 distinct actions, `@v4` count 0 |
| TC-005 | US1 | A live `CI` run reports an empty annotation list on both `rust` and `frontend` | CLI | ✅ | [run6-tc005-live-ci.txt](responses/run6-tc005-live-ci.txt) — run [31544339526](https://github.com/clintcparker/site-checker/actions/runs/31544339526) @ `457f572`: both `success`, `annotations=0`, `node20_warnings=0` |
| TC-006 | US2 | `release.yml` parses as YAML and `actionlint` reports no diagnostics | CLI | ✅ | [run6-tc001-static.txt](responses/run6-tc001-static.txt) |
| TC-007 | US2 | Every JS action pinned in `release.yml` resolves to a real tag whose `runs.using` is `node24` | CLI | ✅ | [run6-tc002-runs-using.txt](responses/run6-tc002-runs-using.txt) — all 6 distinct JS actions `node24`; `rust-toolchain@stable` is `composite` |
| TC-008 | US2 | Every `with:` key passed by any step is accepted at that step's pinned ref | CLI | ✅ | [run6-tc006-inputs.txt](responses/run6-tc006-inputs.txt) — 0 unaccepted inputs across `setup-node@v7`, `action-setup@v6`, `upload-artifact@v7`, `download-artifact@v8`, `gh-release@v3` |
| TC-009 | US2 | The artifact round-trip still resolves the `release` job's `files:` globs | CLI | ✅ | [run6-tc007-digest-mismatch.txt](responses/run6-tc007-digest-mismatch.txt) — `merge-multiple` `'false'` at both v7 and v8; `skip-decompress` `'false'` at v8; upload `name: site-checker-${{ matrix.arch }}` → both `release.yml:259-260` globs match |
| TC-010 | US2 | `Swatinem/rust-cache@v2` left untouched and is already `node24` | CLI | ✅ | [run6-tc002-runs-using.txt](responses/run6-tc002-runs-using.txt); absent from the working-tree diff |
| TC-011 | US2 | A live `Release` run is annotation-free and both arch artifacts attach | CLI | 🔵 | Blocked — T015; requires a real tag push that publishes a GitHub Release and writes to `clintcparker/homebrew-tap`. Never run at any pin |
| TC-012 | US3 | `.github/dependabot.yml` parses and every key is valid Dependabot v2 schema | CLI | ✅ | [run6-tc009-schema-complement.txt](responses/run6-tc009-schema-complement.txt) — `version: 2`, no unknown keys at top level, update level, or ignore level; **no `update-types`** anywhere (D1 stays fixed) |
| TC-013 | US3 | Complement: for every pin, the next major above it is either explicitly declined or reachable | CLI | ⚠️ | [run6-tc009-schema-complement.txt](responses/run6-tc009-schema-complement.txt) — reachability 7/7 (1 N/A, composite); all four declined majors now sit *at* their pin, i.e. inert. See "Failure Details" |
| TC-014 | US3 | Dependabot accepts the corrected config with no parse error and honors it | CLI | 🔵 | Blocked — T017; `origin/main` still holds `["8.x"]`, the fix is uncommitted, and once merged every entry is inert (TC-016), so acceptance cannot be observed in either direction |
| TC-015 | US3 (R001 fix) | The corrected `versions:` values parse as requirements that match the bare major Dependabot reports | CLI | ✅ **strengthened** | [run6-tc010-gem-grammar.txt](responses/run6-tc010-gem-grammar.txt) + [run6-tc011-dependabot-version-format.txt](responses/run6-tc011-dependabot-version-format.txt) — `"8"` → `= 8` matches `8`/`8.0`; `"8.x"` → `= 8.x` matches nothing; `Gem::Version.correct?("8.x")` → `true`. **PR #28's own trailer emits `dependency-version: '8'` and `'7'`** — bare integers, exactly what `= 8` matches |
| TC-016 | US3 (R004 finding) | The `ignore:` block's own inertness claim is true | CLI | ✅ | [run6-tc009-schema-complement.txt](responses/run6-tc009-schema-complement.txt) — every entry names a version at or below its pin; zero entries above any pin; `dependabot.yml:41` states this in the file |
| TC-017 | US3 | Can the PR #28 sequence recur? Is anything auto-merging Dependabot PRs? | CLI | ⚠️ **new** | [run6-tc013-recurrence-risk.txt](responses/run6-tc013-recurrence-risk.txt) — `allow_auto_merge=false`, `main` unprotected, no auto-merge workflow, PR #28 `autoMergeRequest: null`, 0 reviews, merged by `clintcparker` 3 min after opening. Four next-majors reachable under `patterns: ['*']`. See "Failure Details" |
| TC-018 | US2 (R002 fix) | `digest-mismatch: warn` is a documented v8 value and is set on the `release` job's step | CLI | ✅ | [run6-tc007-digest-mismatch.txt](responses/run6-tc007-digest-mismatch.txt) — input exists at v8, absent at v7, description reads "Options: ignore, info, warn, error. Default is error"; `release.yml:242-245` passes `warn` |
| TC-019 | Polish (T018) | The `ci.yml` and `release.yml` comment claims are accurate | CLI | ⚠️ | [run6-tc014-claims.txt](responses/run6-tc014-claims.txt) — every factual sub-claim verifies; one comparative clause is under-qualified (W003). See "Failure Details" |
| TC-020 | Polish (T019) | `docs/how-to/release.md` is tracked and its pin list matches `release.yml` exactly | CLI | ✅ | [run6-tc018-scope-docs.txt](responses/run6-tc018-scope-docs.txt) — `git ls-files --error-unmatch` → TRACKED despite `docs/` being gitignored; the six JS actions it names match `release.yml`'s six exactly |
| TC-021 | Cross-cutting (R003/R004 fix) | `CHANGELOG.md`'s counts are true, re-derived from the diff | CLI | ✅ | [run6-tc014-claims.txt](responses/run6-tc014-claims.txt) — PR #27's range adds exactly 14 `uses:` lines across exactly 6 distinct actions; tree holds 20 lines / 8 actions |
| TC-022 | Cross-cutting | All four constitution quality gates green | CLI | ✅ | [run6-tc017-gates.txt](responses/run6-tc017-gates.txt) — 62 Rust tests, 47 frontend tests, clippy `-D warnings` clean, actionlint exit 0 |
| TC-023 | Cross-cutting | No app surface touched — user-visible behavior unchanged | CLI | ✅ | [run6-tc018-scope-docs.txt](responses/run6-tc018-scope-docs.txt) — `git diff b64c0e2..HEAD -- src src-tauri index.html` empty; `git status --porcelain -- src src-tauri index.html` empty |
| TC-024 | Cross-cutting | `verify-install-channels.yml` contains no actions and was not modified | CLI | ✅ | [run6-tc018-scope-docs.txt](responses/run6-tc018-scope-docs.txt) — `uses:` count 0; absent from both the committed range and the working tree |
| TC-025 | Cross-cutting (T002) | The ≥ 2.327.1 runner floor is satisfied for every job | CLI | ✅ | [run6-tc018-scope-docs.txt](responses/run6-tc018-scope-docs.txt) — `actions/runners` → `total_count: 0`; every `runs-on:` is `ubuntu-latest` or `macos-latest` |
| TC-026 | Cross-cutting | The working tree's executable delta vs `origin/main` is bounded and cannot invalidate TC-005 | CLI | ✅ **new** | [run6-tc018-scope-docs.txt](responses/run6-tc018-scope-docs.txt) — `ci.yml` delta is comment-only; `release.yml`'s sole executable change is `+ digest-mismatch: warn` on the `release` job, which `CI` never invokes |

**Legend**: ✅ Pass | ❌ Fail | ⚠️ Partial | 🔵 Skipped

---

## Acceptance Criteria Coverage

`tasks.md` is the authority; its three user stories and their stated "Independent test" lines stand in for acceptance criteria.

| Criterion | Test ID(s) | Status | Notes |
|-----------|-----------|--------|-------|
| **US1** — `CI` produces zero Node 20 deprecation annotations on `rust` and `frontend`, both still `success` | TC-001–TC-005, TC-026 | ✅ **Met, verified live** | Run 31544339526 at `457f572` = `origin/main`: both jobs `success`, both annotation lists length 0. The working tree's `ci.yml` delta is comment-only (TC-026), so the validated pin set is the current one. |
| **US2** — `Release` emits no Node 20 annotations across all five jobs | TC-006–TC-011, TC-018 | ⚠️ Met by inspection, unconfirmed live | All six JS actions `node24` at their pinned refs; every input accepted; the artifact round-trip resolves. Needs a real tag push (T015), which has never happened at any pin. |
| **US3** — a future deprecation surfaces as a dependency PR, not an unread annotation | TC-012–TC-017 | ✅ **Met**; ignore block inert by design, acceptance unconfirmed | Every action has a reachable next major, no blanket `semver-major` ignore, no permanent silence. The `ignore:` syntax fix is now verified against PR #28's own version strings (TC-015). What is unobserved is only whether Dependabot honors an ignore it currently has no occasion to apply. |
| **T018** — the pinning convention is recorded honestly alongside the existing CI comments | TC-019 | ⚠️ Met with one under-qualified clause | Every factual sub-claim verifies against `action.yml`. W003 is a rationale, not a behavior. |
| **T019** — the runner floor is noted in a tracked doc | TC-020 | ✅ Met | Tracked despite `docs/` being gitignored; the six-action list matches `release.yml` exactly. |
| **T001 / T002** — pin inventory accurate; no self-hosted runner defeats the floor | TC-004, TC-021, TC-025 | ✅ Met | 8 distinct actions confirmed by `grep`; `total_count: 0` runners. |
| Constitution quality gates hold | TC-022 | ✅ Met | 62 Rust, 47 frontend, clippy clean, actionlint 0. |
| No scope creep into app behavior | TC-023, TC-024, TC-026 | ✅ Met | |

**Coverage**: 8/8 criteria exercised (100%). US1 met live; US3 met and its syntax fix now backed by production evidence; US2 met to the depth reachable without a tag push.

---

## Test Suite Results

| Test Suite | Total | Passed | Failed | Skipped | Coverage |
|-----------|-------|--------|--------|---------|----------|
| `cargo test` (`src-tauri`, lib) | 62 | 62 | 0 | 0 | not instrumented |
| `cargo test` (`src-tauri`, bin + doc-tests) | 0 | 0 | 0 | 0 | — |
| `vitest run` (frontend, 6 files) | 47 | 47 | 0 | 0 | not instrumented |
| `cargo clippy --all-targets -- -D warnings` | — | exit 0 | 0 | — | — |
| `actionlint` (4 workflow files) | — | exit 0 | 0 | — | — |

No pre-existing failures, and no new failures introduced by this change. The change set contains no
Rust or TypeScript, so the suites function here as a regression guard rather than as coverage of the
feature.

---

## Failure Details

No scenario failed. Three are partial and two are blocked; details for each partial follow.

### TC-013: Complement — declined majors versus current pins

**Status**: ⚠️ PARTIAL
**Steps to Reproduce**:
1. Parse `.github/dependabot.yml` and collect each `ignore` entry's `dependency-name` and `versions`.
2. Parse every `uses:` line in `.github/workflows/` and collect the pinned major per action.
3. For each pin, ask whether the next major above it is explicitly declined or reachable, and whether any declined version sits at or below the pin.

**Expected**: no entry declines a version at or below its own pin, and every action has a reachable next major.
**Actual**: the reachability half passes 7/7 (plus one N/A for the composite `dtolnay/rust-toolchain@stable`) — `checkout` v8, `setup-node` v8, `upload-artifact` v8, `download-artifact` v9, `action-setup` v7, `gh-release` v4, and `rust-cache` v3 are all reachable, and no blanket `update-types: ["version-update:semver-major"]` exists anywhere. The other half now reports four entries at their pin: `checkout` pinned v7 declines `["6","7"]`, `setup-node` pinned v7 declines `["7"]`, `upload-artifact` pinned v7 declines `["7"]`, `download-artifact` pinned v8 declines `["8"]`.
**Assessment**: **not a new defect, and not a regression** — this is precisely the inertness the file documents at `dependabot.yml:41` and that `tasks.md` and `CHANGELOG.md` record. PR #28 moved the pins *onto* the versions the file declines, and Dependabot only proposes upgrades, so an entry naming a version you already run has nothing to decline. The entries re-arm the instant a pin drops below one, which is the stated reason for keeping them. The assertion's polarity, not the config, is what changed between run 4 and now.
**Evidence**: [run6-tc009-schema-complement.txt](responses/run6-tc009-schema-complement.txt)
**Severity**: Informational

### TC-017: Recurrence risk of the PR #28 sequence

**Status**: ⚠️ PARTIAL
**Steps to Reproduce**:
1. `gh pr view 28 --json mergedBy,autoMergeRequest,reviews,createdAt,mergedAt`.
2. `gh api repos/clintcparker/site-checker --jq '.allow_auto_merge'` and `gh api .../branches/main/protection`.
3. Grep `.github/workflows/` for any job that merges Dependabot PRs.
4. Re-read the `groups` and `open-pull-requests-limit` settings.

**Expected**: after the fix, the failure mode that produced PR #28 — four majors crossing in one PR without evaluation — is either prevented or explicitly accepted.
**Actual**: no automation merged PR #28. `allow_auto_merge` is `false`, `main` returns `404 Branch not protected`, no workflow references Dependabot except two explanatory comments in `ci.yml`, and PR #28 carries `autoMergeRequest: null` with 0 reviews — merged by `clintcparker` at 22:54:10Z, three minutes after opening at 22:51:10Z. The configuration is unchanged in the relevant respect: the group still bundles `patterns: ['*']` with `open-pull-requests-limit: 5`, and four next-majors (`checkout` v8, `setup-node` v8, `upload-artifact` v8, `download-artifact` v9) are reachable and undeclined.
**Assessment**: **this is the configuration US3 asked for, and the gap it leaves is a human-review one.** US3's goal is that a future runtime deprecation surfaces as a PR rather than an unread annotation, and it will. An `ignore:` list cannot prevent a repeat of PR #28's shape, because it can only decline majors that already exist and have already been evaluated — the next one by definition has not. The only mechanisms that would force evaluation are outside this feature's scope: branch protection with a required review on `main`, or splitting the group so each action arrives as its own PR. Recorded so `/speckit.ship` can surface it, not fixed.
**Evidence**: [run6-tc013-recurrence-risk.txt](responses/run6-tc013-recurrence-risk.txt)
**Severity**: Low (process, not code; no defect in the change set)

### TC-019: Comment claim accuracy on the `release.yml` download step

**Status**: ⚠️ PARTIAL
**Steps to Reproduce**:
1. Read the comment block above `actions/download-artifact@v8` at `.github/workflows/release.yml:231-241`.
2. Check each factual sub-claim against `action.yml` at v7 and v8.
3. Check the comparative clause against `docs/how-to/release.md` § "Re-running a failed release".

**Expected**: every claim in the comment is true and consistent with the rest of the repo.
**Actual**: all factual sub-claims verify independently — v8 adds exactly two inputs (`digest-mismatch`, `skip-decompress`), removes none, `merge-multiple` is `'false'` at both v7 and v8, `skip-decompress` defaults `'false'`, `digest-mismatch` defaults `'error'` at v8 and does not exist at v7, and `warn` is one of the four documented options. The comparative clause is the exception: "failing mid-release leaves a tag pushed with no release object, which is worse than publishing and re-running." Two files away, `docs/how-to/release.md` documents that a re-run from the same tag *converges* — `action-gh-release` updates the existing release rather than failing, and `release.yml:247-248` carries a comment saying exactly that. So the `error` failure mode is recoverable by a path this repo already relies on, while the `warn` mode publishes an artifact that failed its digest check into a release that Homebrew installs from.
**Assessment**: left unfixed deliberately, concurring with [run 5](qa-20260812-112100.md) and with the [11:16 review](../reviews/review-20260812-111631.md), which calls this "not a live defect" and "the maintainer's call." The two remedies — soften the justification, or take `error` and rely on the documented re-run — are respectively a re-litigation of a rationale the sixth review recommended and a behavior change on an untested release path. Neither is QA's to take unilaterally, and taking the second while T015 has never run would change the release path's failure semantics with no way to observe the result.
**Evidence**: [run6-tc014-claims.txt](responses/run6-tc014-claims.txt)
**Severity**: Low (the trade-off is explicit in the comment and reversible by deleting one input)

---

## Environment Info

| Property | Value |
|----------|-------|
| Operating System | macOS 26.6.1 (arm64), build 25G76 |
| Browser | N/A — no browser QA; this feature has no UI surface |
| Runtime | Node v22.16.0, pnpm 10.30.3, rustc 1.94.1, Python 3 (PyYAML), Ruby 3.4.2 |
| Application URL | N/A — CI configuration only; the app was never launched |
| Test Runner | `cargo test`, `vitest run` (v4.1.10), `cargo clippy`, `actionlint` 1.7.12 |
| Repo state | `main` @ `59db8a8` + 6 modified files uncommitted; `origin/main` @ `457f572` |
| GitHub API | `gh` CLI authenticated as `clintcparker` |
| Screenshots | `screenshots/SKIPPED.md`; **no `manifest.json`** — no drivability record, no interaction record |

---

## Metrics Summary

| Metric | Value |
|--------|-------|
| Total scenarios | 26 |
| ✅ Passed | 21 |
| ❌ Failed | 0 |
| ⚠️ Partial | 3 |
| 🔵 Skipped | 2 |
| Pass rate | 80.8% passed outright; 92.3% passed or partial; **0% failed** |
| Acceptance criteria coverage | 8/8 (100%) |
| Test suite results | 109/109 tests passing (62 Rust + 47 frontend); clippy and actionlint clean |

---

## Verdict and what remains

⚠️ **PARTIAL PASS.** No scenario failed, so no fix-and-re-run was triggered; per this run's
instructions QA stops here and leaves the report for `/speckit.ship` to report against.

What an independent re-execution changed relative to [run 5](qa-20260812-112100.md): the R001 fix
moved from "verified in a Ruby REPL, premise unobserved" to **verified against the incident that
exposed it** — PR #28's own `updated-dependencies` trailer emits bare-integer versions (`'7'`,
`'8'`), which is exactly what the corrected `= 7` / `= 8` requirements match. Two new scenarios were
added (TC-017 recurrence risk, TC-026 executable-delta bounding). Everything else re-verified
identically from primary sources.

Three things remain open, in descending order of consequence:

1. **T015 — the release path has never run end to end, at any pin.** The
   `upload-artifact@v7 → download-artifact@v8 → action-gh-release@v3` round-trip is verified only by
   reading `action.yml` at each ref, and it now carries a `digest-mismatch: warn` placed by
   inspection. Closing this requires pushing a real tag, which publishes a GitHub Release and writes
   to `clintcparker/homebrew-tap` — a release decision, not a QA action.
2. **T017 — the corrected `ignore:` syntax cannot be observed working.** It is uncommitted, and even
   once merged every entry is inert against the current pins. It becomes observable only when a pin
   drops below an entry or a new major ships. The file states this about itself, which is the right
   handling of an unfalsifiable claim.
3. **W003 — one comparative clause in `release.yml`'s comment is under-qualified**, and TC-017's
   process gap (nothing forces evaluation of the next four-major group PR) sits alongside it. Both
   are maintainer calls that QA deliberately did not make.

---

*Generated by `/speckit.qa` — Systematic QA testing for spec-driven development.*
