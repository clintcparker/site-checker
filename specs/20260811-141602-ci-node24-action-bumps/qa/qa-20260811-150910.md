# QA Report: Retire the Node.js 20 Deprecation Warning in CI (fourth run)

**QA Mode**: CLI QA
**Date**: 2026-08-11 15:09 PDT
**Feature**: [tasks.md](../tasks.md) — no `spec.md` or `plan.md` exists (feature bypassed `/speckit-specify` and `/speckit-plan`)
**Branch**: `20260811-141602-ci-node24-action-bumps` @ `66ee67f`
**Environment**: macOS 26.6 (arm64), Node v22.16.0, pnpm 10.30.3, rustc 1.94.1, actionlint 1.7.12, `gh` CLI authenticated — no application URL (this feature has no runtime surface)
**Verdict**: ⚠️ PARTIAL PASS

---

## QA Summary

This is a fresh QA invocation at branch head `66ee67f`, run after the [fifth staff review](../reviews/review-20260811-150343.md) closed blocker D1. Every scenario was re-executed from primary sources; nothing was carried forward from [run 3](qa-20260811-145301.md).

**The material change in this run is TC-013.** The staff review's finding E2 observed that run 3's TC-013 asked only whether the `ignore:` block *covers* every declined major — an assertion that passes under both the broken and the fixed config, because it tests coverage and never tests the complement. TC-013 has been **replaced with that complement**: for each of the eight distinct pins, is the next major above it either explicitly named in `ignore:` (deliberately declined) or absent from it (reachable, so Dependabot will propose it)? That question has exactly one right answer, and it is the assertion that would have caught D1 on the first pass. It now passes 7/7 (plus one N/A for the composite `dtolnay/rust-toolchain@stable`): no blanket `update-types: ["version-update:semver-major"]` remains anywhere, every declined major sits strictly above its pin, and each of the eight actions has a named reachable next major — `checkout` v8, `download-artifact` v9, `setup-node` v8, `upload-artifact` v8, `action-setup` v7, `action-gh-release` v4, `rust-cache` v3. A future Node runtime deprecation has a route in for every action. **US3's `ignore:` block is now tested for behavior, not just schema.**

**US1 is confirmed live at the exact current head.** CI run [31540886309](https://github.com/clintcparker/site-checker/actions/runs/31540886309) ran at `66ee67fe590991d258c99aee08f84eb3de317628` — byte-identical to `HEAD` — and `check-runs/93942758143/annotations` and `check-runs/93942758200/annotations` both return `[]`. Both jobs concluded `success`. Zero annotations of any level, zero Node 20 deprecation messages. This is the sixth consecutive annotation-free run on this branch.

Eighteen of twenty-one scenarios pass, none fail. All seven JS-action pins resolve to a tag whose `action.yml` declares `runs: using: node24`, re-read live at each pinned ref rather than trusted from the audit table. Every input the workflows pass is still accepted at those refs. The artifact round-trip still resolves the `release` job's globs. 20 `uses:` lines carry zero `@v4`. All four constitution gates are green: 62 Rust tests, 47 frontend tests, clippy clean, `actionlint` exit 0.

**The verdict is PARTIAL PASS, and the reason is unchanged from run 3 and narrow.** Three scenarios remain blocked on GitHub-side events that have not happened: a real tag push (T015), the merge (T017), and the post-merge run (T020). `release.yml`'s `upload-artifact@v6` → `download-artifact@v7` → `action-gh-release@v3` round-trip has still never been exercised end to end. Per the staff review's E4, **T017 now carries weight it did not carry before**: the `versions: ["N.x"]` syntax is idiomatic and passes a full key-level schema check, but GitHub does not publish the `github-actions` ecosystem's accepted `versions` syntax, so its acceptance is asserted rather than observed until the file lands on the default branch.

**No scenario in this report was skipped for want of GUI automation.** `specs/20260811-141602-ci-node24-action-bumps/screenshots/` contains `SKIPPED.md`, not a `manifest.json` — so there is no `capture_blocked` field, no drivability record, and no record of any interaction having been performed. No drivability claim is made here in either direction. None is needed: this feature touches no path under `src/`, `index.html`, or `src-tauri/` (TC-018 re-confirms this against the actual diff), and no scenario in this plan requires driving the app. The three 🔵 scenarios are each blocked on a named external event, not on automation capability.

---

## Test Results

| ID | User Story | Scenario | Mode | Result | Evidence |
|----|-----------|----------|------|--------|----------|
| TC-001 | US1 | `ci.yml` parses as YAML and `actionlint` reports no diagnostics | CLI | ✅ | `yaml.safe_load` OK; `actionlint` exit 0 |
| TC-002 | US1 | Every JS action pinned in `ci.yml` resolves to a real tag whose `runs.using` is `node24` | CLI | ✅ | [tc002-runs-using-run4.txt](responses/tc002-runs-using-run4.txt) |
| TC-003 | US1 | `setup-node@v6` still accepts `node-version` and `cache`; both preserved verbatim | CLI | ✅ | [tc002-runs-using-run4.txt](responses/tc002-runs-using-run4.txt); `ci.yml:94-97` |
| TC-004 | US1 | No `@v4` pin remains; `rust-cache@v2` and `rust-toolchain@stable` untouched | CLI | ✅ | 20 `uses:` lines, zero `@v4`; `grep "@v4"` returns nothing |
| TC-005 | US1 | A live `CI` run reports an empty annotation list on both `rust` and `frontend` | CLI | ✅ | [tc005-live-annotations-run4.txt](responses/tc005-live-annotations-run4.txt) — run 31540886309 @ `66ee67f` (= HEAD): both jobs `success`, both `annotations=0`, `node20_warnings=0` |
| TC-006 | US2 | `release.yml` parses as YAML and `actionlint` reports no diagnostics | CLI | ✅ | `yaml.safe_load` OK; `actionlint` exit 0 |
| TC-007 | US2 | Every JS action pinned in `release.yml` resolves to a real tag whose `runs.using` is `node24` | CLI | ✅ | [tc002-runs-using-run4.txt](responses/tc002-runs-using-run4.txt) — all 6 distinct actions |
| TC-008 | US2 | `upload-artifact@v6`, `download-artifact@v7`, `action-gh-release@v3` still accept every input passed | CLI | ✅ | [tc002-runs-using-run4.txt](responses/tc002-runs-using-run4.txt) — `name`/`path`/`if-no-files-found`; `path`/`merge-multiple`; `generate_release_notes`/`fail_on_unmatched_files`/`files` |
| TC-009 | US2 | The artifact round-trip still resolves the `release` job's `files:` globs | CLI | ✅ | `merge-multiple` default `'false'` at v7 → per-artifact dirs; upload `name: site-checker-${{ matrix.arch }}` → `release.yml:246-247` globs match |
| TC-010 | US2 | `Swatinem/rust-cache@v2` left untouched and is already `node24` | CLI | ✅ | [tc002-runs-using-run4.txt](responses/tc002-runs-using-run4.txt); `release.yml:99,145` absent from the diff |
| TC-011 | US2 | A live `Release` run is annotation-free and both arch artifacts attach | CLI | 🔵 | Blocked — T015; requires a real tag push that publishes a release and writes to `clintcparker/homebrew-tap` |
| TC-012 | US3 | `.github/dependabot.yml` parses and every key is valid Dependabot v2 schema | CLI | ✅ | [tc012-dependabot-schema-run4.txt](responses/tc012-dependabot-schema-run4.txt) — no unknown keys; no `update-types` remains (D1 stays fixed) |
| TC-013 | US3 | **Complement form (E2): for every pin, the next major above it is either explicitly declined or reachable by Dependabot** | CLI | ✅ **rewritten scenario** | [tc013-complement.txt](responses/tc013-complement.txt) — 7 PASS, 0 FAIL, 1 N/A; no blanket `semver-major` ignore; nothing declined at or below a pin; no major above a pin left unaddressed |
| TC-014 | US3 | Dependabot accepts the config with no parse error | CLI | 🔵 | Blocked — T017; Dependabot parses the file only once it lands on the default branch. **Now load-bearing** (staff review E4) |
| TC-015 | Polish (T018) | The `ci.yml` pinning-convention comment accurately describes the pins in the same change | CLI | ✅ | [tc015-comment-claims-run4.txt](responses/tc015-comment-claims-run4.txt) — 4 claim groups re-verified against `action.yml` at each ref |
| TC-016 | Polish (T019) | `docs/how-to/release.md` is tracked and its runner-floor claim is true | CLI | ✅ | `git ls-files --error-unmatch` → TRACKED; `release.md:124-138` names all six pins and the ≥ 2.327.1 floor; list matches `release.yml` exactly |
| TC-017 | Cross-cutting | All four constitution quality gates green | CLI | ✅ | [tc017-gates-run4.txt](responses/tc017-gates-run4.txt) |
| TC-018 | Cross-cutting | No app surface touched — user-visible behavior unchanged | CLI | ✅ | `git diff --name-only origin/main...HEAD -- src index.html src-tauri` → empty |
| TC-019 | Cross-cutting | `verify-install-channels.yml` contains no actions and was not modified | CLI | ✅ | `grep -c "uses:"` → 0; absent from `git diff origin/main...HEAD` |
| TC-020 | Cross-cutting (T002) | The ≥ 2.327.1 runner floor is satisfied for every job | CLI | ✅ | `gh api …/actions/runners` → `total_count: 0`; all jobs on `macos-latest`/`ubuntu-latest` |
| TC-021 | Polish (T020) | The newest `CI` run on `main` post-merge has an empty annotation list | CLI | 🔵 | Blocked — post-merge by definition |

**Legend**: ✅ Pass | ❌ Fail | ⚠️ Partial | 🔵 Skipped

---

## Acceptance Criteria Coverage

`tasks.md` is the authority; its three user stories and their stated "Independent test" lines stand in for acceptance criteria.

| Criterion | Test ID(s) | Status | Notes |
|-----------|-----------|--------|-------|
| **US1** — `CI` produces zero Node 20 deprecation annotations on `rust` and `frontend`, both still `success` | TC-001–TC-005 | ✅ **Met, verified live at HEAD** | Run 31540886309 at `66ee67f`, the exact current head: both jobs `success`, both annotation lists empty. Sixth consecutive clean run. |
| **US2** — `Release` emits no Node 20 annotations across all five jobs | TC-006–TC-011 | ⚠️ Met by inspection, unconfirmed live | All six actions `node24`; inputs and artifact round-trip verified at the target refs. Needs a real tag push (T015). |
| **US3** — a future deprecation surfaces as a dependency PR, not an unread annotation | TC-012–TC-014 | ✅ **Met by behavior test**, acceptance unconfirmed live | TC-013's complement form confirms every one of the eight pins has a reachable next major. What remains unobserved is only whether Dependabot *accepts* the `versions:` syntax (T017), not whether the config expresses the right intent. |
| **T018** — the pinning convention is recorded honestly alongside the existing CI comments | TC-015 | ✅ Met | All four claim groups verified from `action.yml` at each ref, not carried forward from run 3. |
| **T019** — the runner floor is noted in a tracked doc | TC-016 | ✅ Met | File tracked despite `docs/` being gitignored; 2.327.1 confirmed from primary source. |
| **T002** — no self-hosted runner defeats the runner floor | TC-020 | ✅ Met | |
| Constitution quality gates hold | TC-017 | ✅ Met | |
| No scope creep into app behavior | TC-018, TC-019 | ✅ Met | |

**Coverage**: 8/8 criteria exercised (100%). US1 met live at HEAD; US3 upgraded from "met by inspection" to "met by behavior test"; US2 met to the depth reachable locally.

---

## Test Suite Results

| Test Suite | Total | Passed | Failed | Skipped | Coverage |
|-----------|-------|--------|--------|---------|----------|
| `cargo test --locked` (src-tauri) | 62 | 62 | 0 | 0 | not measured |
| `pnpm test` (vitest, 6 files) | 47 | 47 | 0 | 0 | not measured |
| `cargo clippy --all-targets --locked -- -D warnings` | — | clean (exit 0) | 0 | — | — |
| `actionlint` (3 workflows) | — | exit 0 | 0 | — | — |
| YAML parse (4 config files) | 4 | 4 | 0 | 0 | — |

No pre-existing failures and no new ones. Zero source files changed on this branch, so these gates confirm the change is inert with respect to application behavior — the deprecation warning's absence is established by TC-005, not by these.

---

## Failure Details

No failures in this run. Nothing regressed from run 3.

One scenario **changed shape rather than result**, and the distinction matters for anyone reading the two reports side by side. Run 3's TC-013 asserted *"all 4 deps are ignored at `semver-major`"* — a statement that is now false of the file, and whose form was, per the staff review's E2, the reason blocker D1 survived three reviews and three QA passes. It tested that the ignore list *covered* the declined majors and never tested the complement. Run 4's TC-013 tests the complement instead and passes on the merits. Run 3's TC-013 row should be read as superseded, not as a result that still stands.

---

## Environment Info

| Property | Value |
|----------|-------|
| Operating System | macOS 26.6 (Darwin 25.6.0, arm64) |
| Browser | n/a — no UI surface in this feature |
| Runtime | Node v22.16.0, pnpm 10.30.3, rustc 1.94.1 |
| Application URL | n/a — the app was never launched; nothing in this change reaches it |
| Test Runner | `cargo test`, `vitest`, `actionlint` 1.7.12, `gh` CLI, `python3 -c 'yaml.safe_load'` |
| Branch head under test | `66ee67f` (= `origin/20260811-141602-ci-node24-action-bumps`) |

**On the absence of a screenshots manifest**: there is no `screenshots/manifest.json` for this feature — the screenshots pass wrote `SKIPPED.md` instead, having determined from `tasks.md` that no user-visible surface changes, and having never launched the app. There is therefore no `capture_blocked` field, no drivability record, and **no record of any interaction having been performed** — not a click, not a keystroke, not a resize, not a capture. No drivability claim is made here in either direction, and none was needed: no scenario in this plan requires driving the app, and TC-018 confirms the app-surface diff is empty. The three 🔵 scenarios are blocked on GitHub-side events, not on automation capability.

---

## Metrics Summary

| Metric | Value |
|--------|-------|
| Total scenarios | 21 |
| ✅ Passed | 18 |
| ❌ Failed | 0 |
| ⚠️ Partial | 0 |
| 🔵 Skipped (all externally blocked) | 3 |
| Pass rate | 86% overall; 100% of locally-runnable scenarios (18/18) |
| Acceptance criteria coverage | 100% exercised (8/8) |
| Action refs resolved live against GitHub | 12 `action.yml` reads + 8 full tag listings |
| Pins passing the TC-013 complement test | 7 / 7 (+1 N/A: composite action) |
| Constitution gates green | 4 / 4 |
| Consecutive annotation-free `CI` runs on this branch | 6 |
| Scenarios changed vs. run 3 | 1 (TC-013 rewritten to its complement, per staff review E2) |

---

## Carry into the PR

Not defects — facts a reader of the PR needs.

- **US1 is confirmed live at the exact head `66ee67f`.** Run 31540886309, both jobs `success`, both annotation lists `[]`. T007 is closed and stays closed at the current tip.
- **US3's `ignore:` block is now behavior-tested, not just schema-tested.** TC-013's complement form confirms every one of the eight pins has a named reachable next major, so a future runtime deprecation has a route in for each. This closes the staff review's E2 and is the assertion that would have caught D1 on the first pass.
- **T015, T017, and T020 remain open** and cannot be closed from a local checkout. They need a tag push, the merge, and a post-merge run respectively.
- **`release.yml` is still verified by inspection only.** The `upload-artifact@v6` → `download-artifact@v7` → `action-gh-release@v3` round-trip has not been exercised; the first tag push after merge is its real test.
- **T017 is load-bearing** (staff review E4). The `versions: ["N.x"]` syntax is idiomatic and schema-valid, but GitHub does not publish the `github-actions` ecosystem's accepted `versions` syntax. Confirm on the Dependabot tab after merge; the fallback shape if rejected is `versions: [">= 8.0.0", "< 9.0.0"]`. The failure mode is loud rather than silent, and no workflow depends on it.
- **PR #27's body is stale** — staff review finding E1. It presents the `semver-major` ignore form as the accepted resolution of blocker B1, which is exactly the configuration D1 removed, and calls the 14:46 review the third and final round when there have since been two more. Also E3: it says `ci.yml` 3 pins / `release.yml` 11; the diff shows 4 / 10. Not a QA failure, but a reader of the PR is currently misinformed.
- **Run 3's TC-013 row is superseded**, not merely dated — its assertion is false of the current file. Cite this report's TC-013 instead.
- **`actionlint` is not a CI gate.** It was run by hand here and in all five staff reviews. A malformed workflow edit is caught only by someone running it manually or by the run failing — most exposed on a branch that edits workflows exclusively. Recorded in `docs/ROADMAP.md` (S2), which is gitignored and therefore invisible to PR reviewers (C4).

---

*Generated by `/speckit.qa` — Systematic QA testing for spec-driven development.*
