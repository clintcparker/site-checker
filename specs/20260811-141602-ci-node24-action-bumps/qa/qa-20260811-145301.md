# QA Report: Retire the Node.js 20 Deprecation Warning in CI (third run)

**QA Mode**: CLI QA
**Date**: 2026-08-11 14:53 PDT
**Feature**: [tasks.md](../tasks.md) — no `spec.md` exists (feature bypassed `/speckit-specify` and `/speckit-plan`)
**Environment**: macOS 26.6 (arm64), Node v22.16.0, pnpm 10.30.3, rustc 1.94.1, actionlint (Homebrew), `gh` CLI authenticated — no application URL (this feature has no runtime surface)
**Verdict**: ⚠️ PARTIAL PASS

---

## QA Summary

This is a fresh QA invocation against branch head `64c5bdb`, and two things changed materially since the [previous run](qa-20260811-143902.md): **US1's acceptance criterion is now confirmed on a live runner**, and `.github/dependabot.yml` gained the `ignore:` block that closed blocker B1 from the [second staff review](../reviews/review-20260811-144605.md). Both were re-verified here from primary sources rather than carried forward.

**The headline is TC-005.** It was 🔵 blocked in both prior runs; it now passes. CI run [31539663578](https://github.com/clintcparker/site-checker/actions/runs/31539663578) ran at `64c5bdb` — the *current* branch tip, not the older `bed16c1` the staff review cited — and I queried `check-runs/<job>/annotations` for both jobs directly: `rust` → `success`, 0 annotations; `frontend` → `success`, 0 annotations. Zero Node 20 deprecation messages on either. The run at `f9ea3e2` (31539620923) shows the same. The warning that started this feature is gone, observed rather than inferred.

Eighteen of twenty-one scenarios pass. Every one of the seven distinct actions resolves to a tag whose `action.yml` declares `runs: using: node24`; every input the workflows pass is still accepted at those refs; the artifact round-trip still deposits files where the `release` job's globs look; 20 `uses:` lines carry zero `@v4`; and all four constitution gates are green (62 Rust, 47 frontend, clippy clean, `actionlint` exit 0).

**The verdict is PARTIAL PASS, not ALL PASSED, and the reason is now narrower than in prior runs.** US1 is no longer subject to the "verified by inspection only" caveat — it was observed. But US2 and US3 still are: three scenarios remain blocked on GitHub-side events that have not happened — a real tag push (T015), the merge (T017), and the post-merge run (T020). `release.yml`'s artifact round-trip through `upload-artifact@v6` → `download-artifact@v7` → `action-gh-release@v3` has still never been exercised end to end.

**No scenario in this report was skipped for want of GUI automation.** `specs/20260811-141602-ci-node24-action-bumps/screenshots/` contains `SKIPPED.md`, not a `manifest.json` — so there is no drivability record to cite, and none is needed: this feature touches no path under `src/`, `index.html`, or `src-tauri/`, and no scenario here requires driving the app. TC-018 re-confirms that against the actual diff. The three 🔵 scenarios are each blocked on a named external event, not on automation capability.

---

## Test Results

| ID | User Story | Scenario | Mode | Result | Evidence |
|----|-----------|----------|------|--------|----------|
| TC-001 | US1 | `ci.yml` parses as YAML and `actionlint` reports no diagnostics | CLI | ✅ | `yaml.safe_load` OK; `actionlint` exit 0 |
| TC-002 | US1 | Every JS action pinned in `ci.yml` resolves to a real tag whose `runs.using` is `node24` | CLI | ✅ | [tc002-runs-using.txt](responses/tc002-runs-using.txt) |
| TC-003 | US1 | `setup-node@v6` still accepts `node-version` and `cache`; both preserved verbatim | CLI | ✅ | [tc008-inputs-preserved.txt](responses/tc008-inputs-preserved.txt); `ci.yml:94-97` |
| TC-004 | US1 | No `@v4` pin remains; `rust-cache@v2` and `rust-toolchain@stable` untouched | CLI | ✅ | 20 `uses:` lines, zero `@v4`; `grep "@v4"` returns nothing |
| TC-005 | US1 | **A live `CI` run reports an empty annotation list on both `rust` and `frontend`** | CLI | ✅ **newly passing** | [tc005-live-annotations.txt](responses/tc005-live-annotations.txt) — run 31539663578 @ `64c5bdb`: both jobs `success`, both `annotations=0`, `node20_warnings=0` |
| TC-006 | US2 | `release.yml` parses as YAML and `actionlint` reports no diagnostics | CLI | ✅ | `yaml.safe_load` OK; `actionlint` exit 0 |
| TC-007 | US2 | Every JS action pinned in `release.yml` resolves to a real tag whose `runs.using` is `node24` | CLI | ✅ | [tc002-runs-using.txt](responses/tc002-runs-using.txt) — all 6 distinct actions |
| TC-008 | US2 | `upload-artifact@v6`, `download-artifact@v7`, `action-gh-release@v3` still accept every input passed | CLI | ✅ | [tc008-inputs-preserved.txt](responses/tc008-inputs-preserved.txt) |
| TC-009 | US2 | The artifact round-trip still resolves the `release` job's `files:` globs | CLI | ✅ | `merge-multiple` default `'false'` at v7 → per-artifact dirs; upload `name: site-checker-<arch>` → `release.yml:246-247` globs match |
| TC-010 | US2 | `Swatinem/rust-cache@v2` left untouched and is already `node24` | CLI | ✅ | [tc002-runs-using.txt](responses/tc002-runs-using.txt); `release.yml:99,145` absent from the diff |
| TC-011 | US2 | A live `Release` run is annotation-free and both arch artifacts attach | CLI | 🔵 | Blocked — T015; requires a real tag push that publishes a release and writes to `clintcparker/homebrew-tap` |
| TC-012 | US3 | `.github/dependabot.yml` parses and every key is valid Dependabot v2 schema | CLI | ✅ | [tc012-dependabot-schema.txt](responses/tc012-dependabot-schema.txt) — no unknown keys |
| TC-013 | US3 | **The `ignore:` block actually covers every declined major** (B1 fix) | CLI | ✅ **new scenario** | [tc012-dependabot-schema.txt](responses/tc012-dependabot-schema.txt) — `download-artifact@v8`, `upload-artifact@v7`, `checkout@v6`/`@v7`, `setup-node@v7` all exist upstream and all 4 deps are ignored at `semver-major` |
| TC-014 | US3 | Dependabot accepts the config with no parse error | CLI | 🔵 | Blocked — T017; Dependabot parses the file only once it lands on the default branch |
| TC-015 | Polish (T018) | The `ci.yml` pinning-convention comment accurately describes the pins in the same change | CLI | ✅ | [tc015-comment-claims.txt](responses/tc015-comment-claims.txt) — all 3 claim groups re-verified against `action.yml` |
| TC-016 | Polish (T019) | `docs/how-to/release.md` is tracked and its runner-floor claim is true | CLI | ✅ | `git ls-files --error-unmatch` → TRACKED; `checkout@v5.0.0` notes state "Minimum Compatible Runner Version **v2.327.1**"; doc's pin list matches `release.yml` exactly |
| TC-017 | Cross-cutting | All four constitution quality gates green | CLI | ✅ | [tc017-gates.txt](responses/tc017-gates.txt) |
| TC-018 | Cross-cutting | No app surface touched — user-visible behavior unchanged | CLI | ✅ | `git diff --name-only main...HEAD -- src index.html src-tauri` → empty |
| TC-019 | Cross-cutting | `verify-install-channels.yml` contains no actions and was not modified | CLI | ✅ | `grep -c "uses:"` → 0; absent from `git diff main...HEAD` |
| TC-020 | Cross-cutting (T002) | The ≥ 2.327.1 runner floor is satisfied for every job | CLI | ✅ | `gh api …/actions/runners` → `total_count: 0`; all jobs on `macos-latest`/`ubuntu-latest` |
| TC-021 | Polish (T020) | The newest `CI` run on `main` post-merge has an empty annotation list | CLI | 🔵 | Blocked — post-merge by definition |

**Legend**: ✅ Pass | ❌ Fail | ⚠️ Partial | 🔵 Skipped

---

## Acceptance Criteria Coverage

`tasks.md` is the authority; its three user stories and their stated "Independent test" lines stand in for acceptance criteria.

| Criterion | Test ID(s) | Status | Notes |
|-----------|-----------|--------|-------|
| **US1** — `CI` produces zero Node 20 deprecation annotations on `rust` and `frontend`, both still `success` | TC-001–TC-005 | ✅ **Met, verified live** | Run 31539663578 at branch tip `64c5bdb`: both jobs `success`, both annotation lists empty. No longer inspection-only. |
| **US2** — `Release` emits no Node 20 annotations across all five jobs | TC-006–TC-011 | ⚠️ Met by inspection, unconfirmed live | All six actions `node24`; inputs and artifact round-trip verified at the target refs. Needs a real tag push (T015). |
| **US3** — a future deprecation surfaces as a dependency PR, not an unread annotation | TC-012–TC-014 | ⚠️ Met by inspection, unconfirmed live | Config is schema-valid, correctly scoped, and now declines exactly the majors `tasks.md` declined. Dependabot only parses it after merge (T017). |
| **T018** — the pinning convention is recorded honestly alongside the existing CI comments | TC-015 | ✅ Met | Every claim re-verified from `action.yml` at each ref, not carried forward from run 2. |
| **T019** — the runner floor is noted in a tracked doc | TC-016 | ✅ Met | File tracked despite `docs/` being gitignored; 2.327.1 confirmed from primary source. |
| **T002** — no self-hosted runner defeats the runner floor | TC-020 | ✅ Met | |
| Constitution quality gates hold | TC-017 | ✅ Met | |
| No scope creep into app behavior | TC-018, TC-019 | ✅ Met | |

**Coverage**: 8/8 criteria exercised (100%); US1 now met live, US2 and US3 met to the depth reachable locally.

---

## Test Suite Results

| Test Suite | Total | Passed | Failed | Skipped | Coverage |
|-----------|-------|--------|--------|---------|----------|
| `cargo test --locked` (src-tauri) | 62 | 62 | 0 | 0 | not measured |
| `pnpm test` (vitest, 6 files) | 47 | 47 | 0 | 0 | not measured |
| `cargo clippy --all-targets --locked -- -D warnings` | — | clean (exit 0) | 0 | — | — |
| `actionlint` (3 workflows) | — | exit 0 | 0 | — | — |
| YAML parse (4 config files) | 4 | 4 | 0 | 0 | — |

No pre-existing failures and no new ones. Zero source files changed on this branch, so these gates confirm the change is inert with respect to application behavior — the deprecation warning's absence is established by TC-005, not by these.

---

## Failure Details

No failures in this run. Nothing regressed from the previous run, and TC-014 (the `ci.yml` comment, which failed in run 1) remains correct under independent re-verification as TC-015 here.

---

## Environment Info

| Property | Value |
|----------|-------|
| Operating System | macOS 26.6 (Darwin 25.6.0, arm64) |
| Browser | n/a — no UI surface in this feature |
| Runtime | Node v22.16.0, pnpm 10.30.3, rustc 1.94.1 |
| Application URL | n/a — the app was never launched; nothing in this change reaches it |
| Test Runner | `cargo test`, `vitest`, `actionlint`, `gh` CLI, `python3 -c 'yaml.safe_load'` |

**On the absence of a screenshots manifest**: there is no `screenshots/manifest.json` for this feature — the screenshots pass wrote `SKIPPED.md` instead, having determined from `tasks.md` that no user-visible surface changes, and having never launched the app. So there is no `capture_blocked` field and no record of any interaction being performed; no drivability claim is made here in either direction. None was needed: no scenario in this plan requires driving the app, and TC-018 confirms the app-surface diff is empty. The three 🔵 scenarios are blocked on GitHub-side events, not on automation capability.

---

## Metrics Summary

| Metric | Value |
|--------|-------|
| Total scenarios | 21 |
| ✅ Passed | 18 |
| ❌ Failed | 0 |
| ⚠️ Partial | 0 |
| 🔵 Skipped (all externally blocked) | 3 |
| Pass rate | 86% overall; 100% of locally-runnable scenarios (18/18) |
| Acceptance criteria coverage | 100% exercised (8/8) |
| Action tags resolved against GitHub | 20 (7 pinned + 6 one-major-below + 7 declined-major existence checks) |
| Constitution gates green | 4 / 4 |
| Scenarios newly passing vs. run 2 | 2 (TC-005 unblocked by the live run; TC-013 added for the B1 fix) |

---

## Carry into the PR

Not defects — facts a reader of the PR needs.

- **US1 is confirmed live and T007 is closed.** Run 31539663578 at `64c5bdb` is the freshest evidence and supersedes the review's citation of `bed16c1`. `tasks.md` has T007 checked off as of `64c5bdb`.
- **T015, T017, and T020 remain open** and cannot be closed from a local checkout. They need a tag push, the merge, and a post-merge run respectively.
- **`release.yml` is still verified by inspection only.** The `upload-artifact@v6` → `download-artifact@v7` → `action-gh-release@v3` round-trip has not been exercised; the first tag push after merge is its real test.
- **PR #27's body is stale** — finding C1 of the [second staff review](../reviews/review-20260811-144605.md). It lists blockers B1 and B2 as open; both are closed, and its artifact links are pinned to `bed16c1`, now three commits behind. Not a QA failure, but a reader of the PR is currently misinformed.
- **`actionlint` is not a CI gate.** It was run by hand here and in both staff reviews. A malformed workflow edit is caught only by someone running it manually or by the run failing — most exposed on a branch that edits workflows exclusively. Recorded in `docs/ROADMAP.md` (S2), which is gitignored and therefore invisible to PR reviewers (C4).

---

*Generated by `/speckit.qa` — Systematic QA testing for spec-driven development.*
