# Release Record — Retire the Node.js 20 Deprecation Warning in CI

**Ship run**: 2026-08-11 14:58 PDT (unattended)
**Feature dir**: `specs/20260811-141602-ci-node24-action-bumps/`
**Branch**: `20260811-141602-ci-node24-action-bumps` → `main`
**Remote**: `origin` (`git@github.com:clintcparker/site-checker.git`, **PUBLIC**)
**PR**: [#27](https://github.com/clintcparker/site-checker/pull/27) — **updated, not created**; the
PR already existed from the 14:44 ship pass and is still OPEN, so this run edited its body rather
than opening a second one.
**Commit range**: `b64c0e2..66ee67f` — 16 commits
**Pushed head**: `66ee67fe590991d258c99aee08f84eb3de317628`
**PR description**: [pr-description-20260811-145801.md](pr-description-20260811-145801.md)
**Supersedes**: [release-20260811-144414.md](release-20260811-144414.md)

---

## Why this pass ran at all

The 14:44 ship pass opened PR #27 and a later commit synced its body to the third staff review.
Since then two things landed that the PR did not yet reflect:

1. **QA run 3** (`qa/qa-20260811-145301.md`) — uncommitted at the start of this run. It moves the
   QA record from 15/20 to **18/21** and, more importantly, records TC-005 (the live annotation
   check) as **newly passing** at branch tip `64c5bdb`.
2. **`4cb572f`** — the `after`-mode screenshots verification, committed locally but never pushed.

Both are now committed, pushed, and cited in the PR body.

## Readiness check

```
Ship Readiness Check (unattended — every confirmation taken as YES):
⚡️ Tasks:        17/20 complete — T015, T017, T020 externally blocked
⚡️ Review:       APPROVED WITH CONDITIONS (15:03, fifth round, 0 blockers)
                 — but rounds 4 and 5 both landed MID-RUN; see below
⚡️ QA:           PARTIAL PASS (run 3, 18/21; 3 blocked, 0 failed)
✅ Working tree: clean after committing the QA report (49e6b11) and the fourth review (08ee1e5)
✅ Base ref:     0 commits behind origin/main — no rebase or merge performed
✅ CI:           run 31540635351 at 4adc898 — both jobs success, 0 annotations

Overall: SHIPPED (with conditions)
```

**The review verdict changed twice while this ship run was executing.** The pre-flight read
`review-20260811-144605.md` (⚠️ APPROVED WITH CONDITIONS, 0 blockers) as the standing verdict and
composed a PR body on it. Then, in order:

1. Right after the first push, `review-20260811-145843.md` appeared untracked in the tree — a fourth
   round returning **❌ CHANGES REQUIRED** with blocker **D1**. Per this run's instructions the ship
   did not stop: the report was committed (`08ee1e5`) so PR reviewers could see it, and the body was
   rewritten to lead with the blocker.
2. Between two of this run's own pushes, the concurrent review process committed **`4adc898`** —
   the D1 remediation, plus fixes for its D2 and D4 against `CHANGELOG.md`. This run pushed it.
3. During the final body edit, `review-20260811-150343.md` appeared and was committed by that same
   process (`66ee67f`) — a fifth round, **⚠️ APPROVED WITH CONDITIONS, 0 blockers**, verifying D1
   closed. The body was recomposed once more against it, which is also what closes that report's
   own finding E1.

The body published to #27 reflects state (3). This is finding **C2/D6/E5** playing out in real time
and is recorded as such in the PR.

`check-prerequisites.sh --json --require-tasks --include-tasks` was **not** usable: it hard-fails on
the missing `plan.md` (this feature bypassed `/speckit-specify` and `/speckit-plan`). The documented
path `.specify/extensions/ship/scripts/bash/check-prerequisites.sh` also does not exist — the real
script lives at `.specify/scripts/bash/`. The pre-flight was therefore run by hand: `FEATURE_DIR`
resolved from the branch name, available docs enumerated with `ls -R`.

## Actions taken

| Step | Outcome |
|---|---|
| Pre-flight commit | `49e6b11` — committed the untracked QA run-3 report. Working tree clean afterward |
| Branch sync | **None needed.** `git rev-list --count HEAD..origin/main` → `0`. No rebase, no merge, no conflict |
| Push | Four fast-forwards: `9eb0cc6..49e6b11` (QA report), `..e5258d8` (release artifacts), `..08ee1e5` (fourth review), `..ba91e63` (re-archive, carrying the concurrent `4adc898` D1 fix along with it). `66ee67f` — the fifth review — was pushed by the concurrent process. **No force-push at any point** |
| Changelog | **No new entry.** `CHANGELOG.md`'s "CI and release actions move to the Node 24 runtime — 2026-08-11" entry (written in `bed16c1`) already covers this feature end to end; prepending a second would have duplicated it. It *was* corrected in `4adc898` for findings D2 and D4 — by the concurrent remediation, not by this ship pass |
| CI | Not blocked on. Nine runs completed on this branch, all `success` with zero annotations on both jobs. Run 31540635351 at `4adc898` — the head carrying the D1 fix — is the one the PR cites. Run 31540886309 for the final head `66ee67f` was still `in_progress` at the end of this run; `git diff --name-only 4adc898..66ee67f -- .github/workflows/` is empty, so the workflow files are byte-identical to the green run's |
| Screenshots | `screenshots/SKIPPED.md` present, no images → PR carries the single line "No UI changes — screenshots skipped." |
| PR | `gh pr edit 27 --body-file …` five times, as the record moved underneath: at compose, after CI completed, after the fourth review landed, after the D1 fix landed, and after the fifth review landed |

## Worktree isolation

**Failed — the branch is in the primary checkout.** `git worktree list` shows
`/Users/clint/src/clintcparker/site-checker` itself holding
`20260811-141602-ci-node24-action-bumps`; no separate worktree exists for it. Speckit only creates
worktrees at `/speckit-specify` time and this was a fix-up run, so none was ever created and none
can be retroactively. The agent session for this ship run also executed from that primary checkout
(`session=primary`), not via `SPECIFY_INIT_DIR`. Stated near the top of the PR body.

Nothing was moved out from under the user: the primary checkout was already on this branch before
the run started, so no recovery step was needed and no working directory changed underneath anyone.

## Base ref and gitignored inputs (Outline step 4)

- **Base ref**: `origin/main` is `b64c0e2` and is an ancestor of `HEAD`. Local `main` carries one
  commit `origin/main` does not — `bd7a559` (`docs: screenshots skipped — CI-only feature`) — but
  that commit is contained in this branch, so it ships here rather than being missing from the PR.
  A reviewer's local `main` may be one commit ahead of the remote; nothing in this branch depends
  on a commit the reviewer cannot see.
- **Gitignored feature inputs**: `docs/ROADMAP.md` is excluded by `.gitignore:28` (`docs/*`, with
  `how-to/` re-included). It is the constitution's designated home for deferred findings and is the
  artifact that closes blocker **B2** — so that gate's evidence is invisible to every reviewer of
  this PR. Called out in the PR body's preamble and as finding **C4**. `docs/how-to/release.md`, the
  one tracked path under `docs/`, is in the diff.

## Incomplete tasks (carried into the PR)

| Task | Blocked on |
|---|---|
| T015 | A real prerelease tag push — publishes a release and writes to `clintcparker/homebrew-tap` |
| T017 | The merge — Dependabot parses `.github/dependabot.yml` only on the default branch. **Now load-bearing** (E4): it is the first observation of whether the `versions: ["8.x"]` form the D1 fix introduced is accepted for the `github-actions` ecosystem |
| T020 | The post-merge `CI` run on `main` |

T007 was closed in `64c5bdb` during the previous ship pass, on live-runner evidence.

## Surviving findings (carried into the PR under "Known issues")

**D1 — raised at 14:58, fixed in `4adc898`, verified closed at 15:03.** The `ignore:` block declined
`checkout`, `setup-node`, `upload-artifact`, and `download-artifact` at
`update-types: ["version-update:semver-major"]`. All four are referenced by bare major tags, so a
major bump is the only version update Dependabot could ever propose for them — the decline was total,
not narrow, and US3 was functionally unmet for 11 of the 14 bumped `uses:` lines. The fix names each
declined major by version (`8.x`, `7.x`, `6.x`/`7.x`, `7.x`), leaving the next major live. **Not this
ship run's work** — the concurrent review process applied it; this run pushed it and recomposed the
PR body around it.

**D2 and D4 — also fixed in `4adc898`**: `CHANGELOG.md`'s cargo/npm rationale now matches
`dependabot.yml`'s (those lockfile flags do not catch upstream drift), and the "not against a live
runner" caveat is narrowed to `release.yml`.

**D3/E1 and D5/E3 were raised against the PR body and were fixed by this run**: links re-pinned from
`f5032b6` — where `review-20260811-144605.md` does not exist, so the "current verdict" link 404'd —
to `66ee67f`, with every path verified present there via `git cat-file -e` before publishing; the B1
row rewritten to state the `versions:` form rather than the broad form D1 rejected; all five review
rounds listed; and the bump split corrected from 3/11 to 4/10.

Still open from the 15:03 report: **E2** (QA's TC-013 is stale and tests coverage rather than its
complement — which is why D1 survived three reviews), **E4** (T017 is now load-bearing: the
`versions:` syntax for `github-actions` is asserted, not observed, until the file is on the default
branch; failure would be loud, on the Dependabot tab), **E5** (the concurrency pattern, recorded
again because it produced a false statement in an intermediate revision of this body — CI run
31540270044's head was restamped from `49e6b11` to `08ee1e5` and corrected).

Open from earlier rounds, deliberately: **C2/D6** (concurrent unattended speckit runs sharing one
checkout), **S2** (`actionlint` is not a CI gate), **W2/C5** (prose naming exact pins can drift
against weekly Dependabot bumps — and after the D1 fix this applies to all six actions again, not
the two the 14:46 review narrowed it to), **R003** (`pnpm/action-setup` has a successor Dependabot
will never surface), **C4** (B2's evidence lives in a gitignored file).

Closed: R001, B1, B2, W1, S1, S3/R004, R005, C1, C3, D1, D2, D3, D4, D5, E1, E3.

**C2's recommended mitigation was applied here, and it was not enough.** `reviews/` and `qa/` were
re-read at the start of this pass rather than at pipeline start — that is what surfaced QA run 3 —
and re-read again before each of the five body publications. The record still moved three more times
mid-run. The mitigation catches artifacts that exist when you look; it cannot catch ones written
afterward. Serializing the steps, or giving each its own worktree, is the actual fix.

## Artifact links (pinned to `66ee67f`)

- [tasks.md](https://github.com/clintcparker/site-checker/blob/66ee67fe590991d258c99aee08f84eb3de317628/specs/20260811-141602-ci-node24-action-bumps/tasks.md)
- [review 15:03 — **current verdict, ⚠️ APPROVED WITH CONDITIONS, 0 blockers**](https://github.com/clintcparker/site-checker/blob/66ee67fe590991d258c99aee08f84eb3de317628/specs/20260811-141602-ci-node24-action-bumps/reviews/review-20260811-150343.md)
- [review 14:58 — superseded (raised D1)](https://github.com/clintcparker/site-checker/blob/66ee67fe590991d258c99aee08f84eb3de317628/specs/20260811-141602-ci-node24-action-bumps/reviews/review-20260811-145843.md)
- [review 14:46 — superseded](https://github.com/clintcparker/site-checker/blob/66ee67fe590991d258c99aee08f84eb3de317628/specs/20260811-141602-ci-node24-action-bumps/reviews/review-20260811-144605.md)
- [QA run 3 — current](https://github.com/clintcparker/site-checker/blob/66ee67fe590991d258c99aee08f84eb3de317628/specs/20260811-141602-ci-node24-action-bumps/qa/qa-20260811-145301.md)
- [screenshots/SKIPPED.md](https://github.com/clintcparker/site-checker/blob/66ee67fe590991d258c99aee08f84eb3de317628/specs/20260811-141602-ci-node24-action-bumps/screenshots/SKIPPED.md)

Links pin the SHA, not the branch name, which dies when the branch is deleted after merge.
