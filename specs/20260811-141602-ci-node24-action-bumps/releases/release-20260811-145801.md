# Release Record — Retire the Node.js 20 Deprecation Warning in CI

**Ship run**: 2026-08-11 14:58 PDT (unattended)
**Feature dir**: `specs/20260811-141602-ci-node24-action-bumps/`
**Branch**: `20260811-141602-ci-node24-action-bumps` → `main`
**Remote**: `origin` (`git@github.com:clintcparker/site-checker.git`, **PUBLIC**)
**PR**: [#27](https://github.com/clintcparker/site-checker/pull/27) — **updated, not created**; the
PR already existed from the 14:44 ship pass and is still OPEN, so this run edited its body rather
than opening a second one.
**Commit range**: `b64c0e2..08ee1e5` — 13 commits
**Pushed head**: `08ee1e5d7371ee320a3eea7995314e425110f9ef`
**PR description**: [pr-description-20260811-145801.md](pr-description-20260811-145801.md)
**Supersedes**: [release-20260811-144414.md](release-20260811-144414.md)

---

## Why this pass ran at all

The 14:44 ship pass opened PR #27 and a later commit synced its body to the third staff review.
Since then two things landed that the PR did not yet reflect:

1. **QA run 3** (`qa/qa-20260811-145301.md`) — uncommitted at the start of this run. It moves the
   QA record from 15/20 to **18/21** and, more importantly, records TC-005 (the live annotation
   check) as **newly passing** at branch tip `64c5bdb`.
2. **`4cb572f`** — the `after`-mode screenshots verification, committed locally but never pushed.

Both are now committed, pushed, and cited in the PR body.

## Readiness check

```
Ship Readiness Check (unattended — every confirmation taken as YES):
⚡️ Tasks:        17/20 complete — T015, T017, T020 externally blocked
❎ Review:       CHANGES REQUIRED (14:58, fourth round, 1 blocker: D1) — landed MID-RUN
⚡️ QA:           PARTIAL PASS (run 3, 18/21; 3 blocked, 0 failed)
✅ Working tree: clean after committing the QA report (49e6b11) and the review (08ee1e5)
✅ Base ref:     0 commits behind origin/main — no rebase or merge performed
✅ CI:           run 31540270044 at 49e6b11 — both jobs success, 0 annotations

Overall: SHIPPED (with an open blocker, reported not resolved)
```

**The review verdict changed while this ship run was executing.** The pre-flight read
`review-20260811-144605.md` (⚠️ APPROVED WITH CONDITIONS, 0 blockers) as the standing verdict and
composed a PR body on it. Immediately after the first push, `review-20260811-145843.md` appeared
untracked in the working tree — a fourth round returning **❌ CHANGES REQUIRED**. Per this run's
instructions the ship did not stop: the report was committed so PR reviewers can see it, and the PR
body was rewritten to lead with the blocker.

`check-prerequisites.sh --json --require-tasks --include-tasks` was **not** usable: it hard-fails on
the missing `plan.md` (this feature bypassed `/speckit-specify` and `/speckit-plan`). The documented
path `.specify/extensions/ship/scripts/bash/check-prerequisites.sh` also does not exist — the real
script lives at `.specify/scripts/bash/`. The pre-flight was therefore run by hand: `FEATURE_DIR`
resolved from the branch name, available docs enumerated with `ls -R`.

## Actions taken

| Step | Outcome |
|---|---|
| Pre-flight commit | `49e6b11` — committed the untracked QA run-3 report. Working tree clean afterward |
| Branch sync | **None needed.** `git rev-list --count HEAD..origin/main` → `0`. No rebase, no merge, no conflict |
| Push | Three fast-forwards: `9eb0cc6..49e6b11` (QA report), `..e5258d8` (release artifacts), `..08ee1e5` (fourth review). **No force-push at any point** |
| Changelog | **Left unchanged.** `CHANGELOG.md`'s "CI and release actions move to the Node 24 runtime — 2026-08-11" entry (written in `bed16c1`) already covers this feature end to end; prepending a second entry would have duplicated it. Note D2 and D4 are open *against* that entry |
| CI | Not blocked on. Run 31540270044 for `49e6b11` was `in_progress` when the body was first written and completed `success` with zero annotations moments later. Runs 31540527436 (`e5258d8`) and 31540566511 (`08ee1e5`) were still in progress at the end of this run; `git diff --name-only 49e6b11..08ee1e5 -- .github/` is empty, so the workflow files are byte-identical to the green run's |
| Screenshots | `screenshots/SKIPPED.md` present, no images → PR carries the single line "No UI changes — screenshots skipped." |
| PR | `gh pr edit 27 --body-file …` three times: at compose, after CI completed, and again after the fourth review landed |

## Worktree isolation

**Failed — the branch is in the primary checkout.** `git worktree list` shows
`/Users/clint/src/clintcparker/site-checker` itself holding
`20260811-141602-ci-node24-action-bumps`; no separate worktree exists for it. Speckit only creates
worktrees at `/speckit-specify` time and this was a fix-up run, so none was ever created and none
can be retroactively. The agent session for this ship run also executed from that primary checkout
(`session=primary`), not via `SPECIFY_INIT_DIR`. Stated near the top of the PR body.

Nothing was moved out from under the user: the primary checkout was already on this branch before
the run started, so no recovery step was needed and no working directory changed underneath anyone.

## Base ref and gitignored inputs (Outline step 4)

- **Base ref**: `origin/main` is `b64c0e2` and is an ancestor of `HEAD`. Local `main` carries one
  commit `origin/main` does not — `bd7a559` (`docs: screenshots skipped — CI-only feature`) — but
  that commit is contained in this branch, so it ships here rather than being missing from the PR.
  A reviewer's local `main` may be one commit ahead of the remote; nothing in this branch depends
  on a commit the reviewer cannot see.
- **Gitignored feature inputs**: `docs/ROADMAP.md` is excluded by `.gitignore:28` (`docs/*`, with
  `how-to/` re-included). It is the constitution's designated home for deferred findings and is the
  artifact that closes blocker **B2** — so that gate's evidence is invisible to every reviewer of
  this PR. Called out in the PR body's preamble and as finding **C4**. `docs/how-to/release.md`, the
  one tracked path under `docs/`, is in the diff.

## Incomplete tasks (carried into the PR)

| Task | Blocked on |
|---|---|
| T015 | A real prerelease tag push — publishes a release and writes to `clintcparker/homebrew-tap` |
| T017 | The merge — Dependabot parses `.github/dependabot.yml` only on the default branch |
| T020 | The post-merge `CI` run on `main` |

T007 was closed in `64c5bdb` during the previous ship pass, on live-runner evidence.

## Surviving findings (carried into the PR under "Known issues")

**🔴 D1 — open blocker, not fixed by this run.** `.github/dependabot.yml`'s `ignore:` block declines
`checkout`, `setup-node`, `upload-artifact`, and `download-artifact` at
`update-types: ["version-update:semver-major"]`. All four are referenced by bare major tags, so a
major bump is the only version update Dependabot could ever propose for them — the decline is total,
not narrow, and US3 is functionally unmet for 11 of the 14 bumped `uses:` lines. The recommended fix
is `versions:` naming only the evaluated majors (`8.x`, `7.x`, `6.x`/`7.x`, `7.x`). This run's single
remediation pass was already spent, and the review process that raised D1 was still executing and
states it will apply the fix and emit a fifth report. **The PR body leads with a do-not-merge banner
pointing at it.**

Also open from 14:58: **D2** (`CHANGELOG.md` repeats the false cargo/npm rationale that W1 removed
from `dependabot.yml`), **D4** (changelog's "not against a live runner" caveat now false for
`ci.yml`), **D6** (the concurrency pattern is still live).

**D3 and D5 were raised against the PR body and were fixed by this run**: links re-pinned from
`f5032b6` (where `review-20260811-144605.md` does not exist — the link 404'd) to `08ee1e5`, and the
bump split corrected from 3/11 to 4/10. Every pinned path was verified present at `08ee1e5` with
`git cat-file -e` before the body was published.

Open from earlier rounds, deliberately: **C2** (concurrent unattended speckit runs sharing one checkout), **S2**
(`actionlint` is not a CI gate), **W2/C5** (prose naming exact pins can drift against weekly
Dependabot bumps, now narrowed to `pnpm/action-setup` and `softprops/action-gh-release`), **R003**
(`pnpm/action-setup` has a successor Dependabot will never surface), **C4** (B2's evidence lives in
a gitignored file).

Closed: R001, B1, B2, W1, S1, S3/R004, R005, C1, C3.

**C2's recommended mitigation was applied here**: `reviews/` and `qa/` were re-read at the start of
this pass, immediately before composing the body, rather than at pipeline start. That is what
surfaced QA run 3.

## Artifact links (pinned to `08ee1e5`)

- [tasks.md](https://github.com/clintcparker/site-checker/blob/08ee1e5d7371ee320a3eea7995314e425110f9ef/specs/20260811-141602-ci-node24-action-bumps/tasks.md)
- [review 14:58 — **current verdict, ❌ CHANGES REQUIRED**](https://github.com/clintcparker/site-checker/blob/08ee1e5d7371ee320a3eea7995314e425110f9ef/specs/20260811-141602-ci-node24-action-bumps/reviews/review-20260811-145843.md)
- [review 14:46 — superseded](https://github.com/clintcparker/site-checker/blob/08ee1e5d7371ee320a3eea7995314e425110f9ef/specs/20260811-141602-ci-node24-action-bumps/reviews/review-20260811-144605.md)
- [QA run 3 — current](https://github.com/clintcparker/site-checker/blob/08ee1e5d7371ee320a3eea7995314e425110f9ef/specs/20260811-141602-ci-node24-action-bumps/qa/qa-20260811-145301.md)
- [screenshots/SKIPPED.md](https://github.com/clintcparker/site-checker/blob/08ee1e5d7371ee320a3eea7995314e425110f9ef/specs/20260811-141602-ci-node24-action-bumps/screenshots/SKIPPED.md)

Links pin the SHA, not the branch name, which dies when the branch is deleted after merge.
