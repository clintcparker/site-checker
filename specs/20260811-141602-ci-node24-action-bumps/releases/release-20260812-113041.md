# Release Record — 2026-08-12 11:30 PDT

**Feature**: Retire the Node.js 20 Deprecation Warning in CI → follow-up fix, "Dependabot ignore syntax"
**Feature dir**: `specs/20260811-141602-ci-node24-action-bumps/`
**Branch**: `20260812-113500-dependabot-ignore-syntax-fix` → `main`
**Mode**: unattended; every confirmation treated as YES
**Pull request**: [#29](https://github.com/clintcparker/site-checker/pull/29) — **updated, not created**
(an earlier ship run at 11:27 opened it at `c6f4396`)

---

## What this run did

| Step | Outcome |
|---|---|
| Prerequisites script | ❌ `.specify/extensions/ship/scripts/bash/check-prerequisites.sh` does not exist; the real script is `.specify/scripts/bash/check-prerequisites.sh`, and it **hard-fails** on this feature because there is no `plan.md`. Readiness was assessed by reading the artifacts directly. |
| `before_ship` hooks | None registered in `.specify/extensions.yml`. |
| Working tree | One untracked file (`qa/qa-20260812-112530.md`) → committed as `5f74647`. |
| Branch sync | **No rebase needed.** `origin/main...HEAD` = 0 behind, 6 ahead. |
| Push | `00a4953..5f74647`, then `d8af34d` (see concurrency note). Never force-pushed. |
| Changelog | **Already present** — `CHANGELOG.md`'s 2026-08-12 entry landed in `c41b34a` on this branch. No second entry prepended. |
| CI | Not blocked on. ✅ success at `c6f4396`; two docs-only runs in flight at `00a4953` and `5f74647`. |
| Screenshots | `screenshots/SKIPPED.md`, no images → single line in the PR body. |
| PR | `gh pr edit 29` with the regenerated description. |

## Readiness summary

```
Ship Readiness Check:
⚡️ Tasks:    19/20 complete  (T015 open — blocked on a real tag push)
⚡️ Review:   APPROVED WITH CONDITIONS  (C1, C2; 0 blockers)
⚡️ QA:       PARTIAL PASS ×2 independent runs  (21 pass, 3 partial, 2 blocked, 0 fail)
✅ Tree:     clean after committing the run-6 QA report

Overall: READY TO SHIP (with notes) — proceeded per unattended instructions
```

## Worktree isolation

`worktree_isolation=failed`, `session=primary`. The run executed in the primary checkout
`/Users/clint/src/clintcparker/site-checker`, not an isolated worktree — the feature's original
worktree was consumed when PR #27 merged, and this follow-up branch was cut at `main`'s tip by an
earlier ship step. The user's working directory remains switched onto
`20260812-113500-dependabot-ignore-syntax-fix`; local `main` was left at `1e3bc88` and never reset.

Gitignored feature inputs carried in the PR only by force-add: `specs/*/screenshots/SKIPPED.md` and
`specs/*/releases/*`. `docs/` is gitignored except `docs/how-to/`, which is why
`docs/how-to/release.md` is visible.

## Concurrency note

**A second ship session was running against this same checkout during this run.** At 11:31:06 it
committed `d8af34d` ("archive the ship record and PR description for PR #29"), force-adding its own
`releases/*-20260812-112700.md` pair, and pushed it. That commit did not touch this run's files. If
PR #29's body does not match `pr-description-20260812-113041.md`, the other session overwrote it
after this record was written.

## Commit range

`457f572..5f74647` (`origin/main`..head at PR-update time), 6 commits:

```
5f74647 docs(qa): add the sixth QA run — production evidence for the R001 ignore-syntax fix
00a4953 docs: after screenshots skipped — ci-node24-action-bumps has no UI surface
c6f4396 docs(specs): close T017 and T020, supersede the out-of-scope bullets, add review and QA records
c41b34a docs: record the PR #28 pin crossing and the inert-ignore defect
f8eb78d fix(ci): make the Dependabot ignore block functional; pin v8 digest-mismatch back to warn
1e3bc88 docs: after screenshots skipped — ci-node24-action-bumps has no UI surface (re-verified)
59db8a8 docs: screenshots skipped — ci-node24-action-bumps has no UI surface (before, post-merge)
```

13 files changed, 1723 insertions(+), 39 deletions(-). Executable surface: `.github/dependabot.yml`,
`.github/workflows/ci.yml`, `.github/workflows/release.yml`. Everything else is docs and spec artifacts.

## Surviving findings carried into the PR

1. **C1 / W003** — `digest-mismatch: warn` is a maintainer decision; the justifying comment's
   comparative clause contradicts `docs/how-to/release.md`. 🟡 open.
2. **C2 / TC-014** — the `ignore:` fix cannot be observed working; every entry is inert against the
   current pins. 🟡 open, no action available.
3. **TC-017** — nothing forces evaluation of the next four-major group PR (`allow_auto_merge=false`,
   `main` unprotected, `patterns: ['*']`); PR #28 was merged by a human with zero reviews. 🟡 process.
4. **T015** — the release path has never run end to end at any pin. Open task, blocked on a tag push.
5. **TC-013** — inverted assertion, same fact as C2. 🟢 low.
6. **S005** — the "every entry is INERT" banner has a shelf life. 🟢 suggestion, not done.

## Links

- [PR #29](https://github.com/clintcparker/site-checker/pull/29)
- [PR description for this run](pr-description-20260812-113041.md)
- [tasks.md](../tasks.md)
- [Final review — 8th](../reviews/review-20260812-111856.md) — ⚠️ APPROVED WITH CONDITIONS
- [QA run 6](../qa/qa-20260812-112530.md) — ⚠️ PARTIAL PASS
- [QA run 5](../qa/qa-20260812-112100.md) — ⚠️ PARTIAL PASS
- [Screenshot skip record](../screenshots/SKIPPED.md)
- No `spec.md`, no `plan.md` — this feature bypassed `/speckit-specify` and `/speckit-plan`.

---
*Generated by `/speckit.ship`.*
