# Release Record — Retire the Node.js 20 Deprecation Warning in CI

**Shipped**: 2026-08-11 14:44 PDT (unattended run)
**PR**: [#27 — ci: retire the Node 20 deprecation warning — bump all Actions pins to node24 majors](https://github.com/clintcparker/site-checker/pull/27)
**Branch**: `20260811-141602-ci-node24-action-bumps` → `main`
**Repository**: `clintcparker/site-checker` (PUBLIC)
**Commit range**: `b64c0e2..f5032b6` (6 commits ahead of `origin/main` at ship time)
**Head SHA**: `f5032b6ac4cc192965b24011c3cc7e474e791e75` (artifact links in the PR body pin this SHA)
**PR description**: [pr-description-20260811-144414.md](pr-description-20260811-144414.md)

---

## Readiness check results

| Gate | Result |
|---|---|
| Tasks | ⚠️ 17/20 complete — **T007 closed during this ship** (CI run 31539366957: both jobs `success`, zero annotations); T015, T017, T020 remain externally blocked |
| Review | ❌ CHANGES REQUIRED (14:40 re-review) — **both blockers remediated on-branch afterward** (B1 four-action `ignore:` block, B2 roadmap entries + R005 fix), plus W1, S1, S3. Open: S2, R003, W2, all recorded in `docs/ROADMAP.md`. No third review confirms this, so the standing verdict is still ❌ |
| QA | ⚠️ PARTIAL PASS — 15 pass, 0 fail, 5 blocked on GitHub-side events |
| Working tree | ✅ Clean after auto-commit (unattended run committed rather than prompted) |
| Branch sync | ✅ 0 commits behind `origin/main`; no rebase needed |
| CI | ✅ Run [31539366957](https://github.com/clintcparker/site-checker/actions/runs/31539366957) — `rust` and `frontend` both `success`, **0 annotations each**. The Node 20 warning is gone from `CI` |
| Screenshots | Skipped — `SKIPPED.md`, no UI surface |

**Overall**: SHIPPED WITH OPEN FINDINGS. The unattended-run instruction was to proceed past
non-passing verdicts and carry every surviving finding into the PR body, which was done.

## Prerequisite-script note

`.specify/extensions/ship/scripts/bash/check-prerequisites.sh` does not exist; the real script is at
`.specify/scripts/bash/check-prerequisites.sh`, and it hard-fails on this feature because there is no
`plan.md` (the feature bypassed `/speckit-specify` and `/speckit-plan`). `FEATURE_DIR` was resolved
by hand and the pre-flight run manually.

## Worktree isolation

**Not isolated.** The branch is checked out in the primary checkout at
`/Users/clint/src/clintcparker/site-checker`; `git worktree list` shows no worktree for it. This was
a fix-up run, and speckit creates worktrees only at `/speckit-specify` time. Nothing was moved out
from under the user, but the isolation the workflow normally provides was not in effect. This is
stated at the top of the PR body.

## Gitignored feature inputs

- `docs/ROADMAP.md` — the constitution's designated home for deferred findings, and the file blocker
  B2 turns on. Gitignored (`docs/` is ignored except `how-to/`), so it is absent from the PR and no
  reviewer can verify B2's grep result.
- `docs/how-to/release.md` — the one tracked path under `docs/`; T019's edit does reach the PR.
- `specs/*/screenshots/` is gitignored; `SKIPPED.md` was force-added by the screenshots pass and is
  present at the pushed head.

## Base ref

`origin/main` was 0 commits ahead of this branch — no rebase performed. Local `main` carried one
commit `origin/main` did not (`bd7a559`, a spec-artifact commit), but it is contained in this branch,
so it ships here rather than being missing from it.

## Commits

```
f5032b6 docs: correct stale ci.yml line numbers in tasks.md
bed16c1 docs: changelog entry for the node24 action bumps
b92a6bb docs: record tasks, staff review, and QA for the node24 action bumps
accd2ee ci: bump GitHub Actions to node24 majors and add Dependabot
1d4ef38 docs: after screenshots skipped — prediction verified, no UI surface
bd7a559 docs: screenshots skipped — CI-only feature, no UI surface
```

11 files changed, +1216 / −17.

**A remediation pass ran concurrently with this ship.** The expanded `.github/dependabot.yml` — the
four-action `ignore:` block, the `groups:` entry, the explicit `open-pull-requests-limit`, and the
rewritten cargo/npm comment — was written between this run reading the file and staging it, so it
landed inside `accd2ee` rather than as its own commit. `f5032b6` (R005) landed after the branch was
first pushed. The PR description was rewritten twice to match what is actually at the head.

## Changelog entry

Prepended to `CHANGELOG.md` as **"CI and release actions move to the Node 24 runtime — 2026-08-11"**,
in the file's existing narrative style. Covers the six action bumps, the two deliberate
one-major-above pins (`setup-node@v6`, `pnpm/action-setup@v6`), why `download-artifact` stops at v7
(v8's `digest-mismatch: error`), the new `dependabot.yml`, and the ≥ 2.327.1 runner floor. Closes by
stating that everything is verified by inspection, not against a live runner.

## Artifact links

- [tasks.md](../tasks.md) — sole design artifact (no `spec.md`, no `plan.md`)
- [review-20260811-144021.md](../reviews/review-20260811-144021.md) — ❌ CHANGES REQUIRED (latest)
- [review-20260811-143026.md](../reviews/review-20260811-143026.md) — ⚠️ APPROVED WITH CONDITIONS
- [qa-20260811-143902.md](../qa/qa-20260811-143902.md) — ⚠️ PARTIAL PASS (run 2)
- [qa-20260811-143616.md](../qa/qa-20260811-143616.md) — ❌ FAILURES FOUND (run 1, TC-014)
- [screenshots/SKIPPED.md](../screenshots/SKIPPED.md)

## Follow-up owed

B1, B2, W1, S1, S3, and R005 are all closed on-branch. What remains:

1. **A third review** to move the recorded verdict off ❌ CHANGES REQUIRED. The blockers are fixed;
   nothing has re-reviewed them.
2. **T015** — push a throwaway prerelease tag and exercise the `upload-artifact@v6` →
   `download-artifact@v7` → `action-gh-release@v3` round-trip end to end. `release.yml` is still
   verified by inspection only, and this is its real test.
3. **T017 / T020** — post-merge confirmations (Dependabot parses the config; `CI` on `main`).
4. **S2** — add an `actionlint` step to CI. Deferred to `docs/ROADMAP.md`; this feature edited
   workflows exclusively and had no automated check for them.
5. **W2 / R003** — de-enumerate the version lists in `ci.yml` and `docs/how-to/release.md`, and plan
   the `pnpm/action-setup` → `pnpm/setup` migration. Both in `docs/ROADMAP.md`.
