> **This branch was built in the primary checkout, not an isolated worktree.**
> `20260811-141602-ci-node24-action-bumps` is checked out at
> `/Users/clint/src/clintcparker/site-checker` and `git worktree list` shows no worktree for it —
> this was a fix-up run, and speckit only creates worktrees at `/speckit-specify` time. Nothing was
> moved out from under anyone, but the isolation the workflow normally provides was not in effect.

> **This feature bypassed `/speckit-specify` and `/speckit-plan`.** There is no `spec.md` and no
> `plan.md`; [`tasks.md`](https://github.com/clintcparker/site-checker/blob/f5032b6ac4cc192965b24011c3cc7e474e791e75/specs/20260811-141602-ci-node24-action-bumps/tasks.md)
> is the sole design artifact and its audit table stands in for both. `check-prerequisites.sh
> --require-tasks` hard-fails on the missing plan, so the ship pre-flight was run by hand.

> **Two artifacts this work was judged against are not in this PR, because `docs/` is gitignored
> except `how-to/`.** `docs/ROADMAP.md` — the constitution's designated home for deferred findings,
> and the file blocker **B2** below turns on — exists only in the author's checkout. Reviewers cannot
> see it. `docs/how-to/release.md` is the one tracked path under `docs/` and is included here.

## Summary

Every JS action pinned in `ci.yml` and `release.yml` sat on a major whose `runs.using` is `node20`,
so both workflows emitted a Node 20 deprecation annotation on every otherwise-green run — the
failure mode where nothing breaks and nobody looks. This bumps all 14 `uses:` lines across six
distinct actions to majors that run on `node24`, adds `.github/dependabot.yml` so the next runtime
deprecation arrives as a reviewable PR instead of an annotation, and records the resulting Actions
runner floor of ≥ 2.327.1 in `docs/how-to/release.md`.

Nothing user-visible. `git diff -- src index.html src-tauri` is empty.

| Action | Before | After | Why not higher |
|---|---|---|---|
| `actions/checkout` | `v4` | `v5` | v6/v7 change credential-persistence defaults; v5 is the first `node24` major |
| `actions/setup-node` | `v4` | `v6` | v5 is already `node24`; v6 narrows auto-caching to npm only, and all three call sites pass `cache: pnpm` explicitly, so the narrowing is never consulted. v7 declined |
| `pnpm/action-setup` | `v4` | `v6` | v5 is already `node24`; v6 adds pnpm 11 and still self-updates to the `packageManager` version `package.json` pins. No v7 exists |
| `actions/upload-artifact` | `v4` | `v6` | v5 is still `node20`; v7 switches to direct upload |
| `actions/download-artifact` | `v4` | `v7` | v6 is still `node20`. **v8 defaults `digest-mismatch: error`** — accepting it turns a logged warning into a failed release run — and v8 also skips decompression, which would break the `release` job's globs |
| `softprops/action-gh-release` | `v2` | `v3` | No v4 exists. v3 still defaults `overwrite_files: true`, preserving the "re-run converges on one release object" property the file's comment promises |

`Swatinem/rust-cache@v2` is already `node24` and `dtolnay/rust-toolchain@stable` is a composite
action; both were deliberately left untouched. `verify-install-channels.yml` has zero `uses:` lines
and is not in this diff.

## Implementation

- **`ci.yml`** — 3 pins bumped, plus a comment block stating the pinning rule and naming the two
  deliberate exceptions to it.
- **`release.yml`** — 11 pins bumped across the `test`, `build`, `homebrew`, and `release` jobs.
  All inputs preserved verbatim: `node-version: 22` / `cache: pnpm`, `if-no-files-found: error`,
  `path: artifacts`, and the `generate_release_notes` / `fail_on_unmatched_files` / `files:` trio.
- **`.github/dependabot.yml`** (new) — `github-actions` at `/`, weekly, `commit-message.prefix: ci`,
  grouped into one PR with an explicit `open-pull-requests-limit: 5`, and an `ignore:` block over the
  four actions whose next major this feature evaluated and declined. Cargo and npm deliberately
  excluded, for the reason the file states.
- **`docs/how-to/release.md`** — records the ≥ 2.327.1 runner floor so a future self-hosted runner
  is not introduced unknowingly. The repo has no self-hosted runners today (`actions/runners` →
  `total_count: 0`) and every job runs on `macos-latest`/`ubuntu-latest`.
- **`CHANGELOG.md`** — narrative entry matching the file's existing style.

## Testing

[QA report (run 2)](https://github.com/clintcparker/site-checker/blob/f5032b6ac4cc192965b24011c3cc7e474e791e75/specs/20260811-141602-ci-node24-action-bumps/qa/qa-20260811-143902.md)
— **⚠️ PARTIAL PASS**. 20 scenarios: 15 pass, 0 fail, 5 blocked on GitHub-side events.
[Run 1](https://github.com/clintcparker/site-checker/blob/f5032b6ac4cc192965b24011c3cc7e474e791e75/specs/20260811-141602-ci-node24-action-bumps/qa/qa-20260811-143616.md)
failed TC-014 (the `ci.yml` comment asserted a rule two of its own pins break); the comment was
rewritten and the re-run passes.

| Suite | Result |
|---|---|
| `cargo test --locked` | 62 passed, 0 failed |
| `pnpm test` (vitest, 6 files) | 47 passed, 0 failed |
| `cargo clippy --all-targets -- -D warnings` | clean |
| `actionlint` (3 workflows) | exit 0 — **run by hand; not a CI gate** (see S2) |
| YAML parse (4 config files) | 4/4 |

**The verdict is PARTIAL PASS because of verification depth, not a known defect.** The acceptance
criterion for all three user stories is the *absence* of a deprecation annotation on a real Actions
run, and that has no local equivalent. **Nothing had been observed on a live runner when QA ran** — that has since changed for `ci.yml`
only; see CI status below.
Every claim in it — including every claim in both reviews — is verified by inspection: `action.yml`
read at 14 tags (7 targets plus every rejected major), plus release notes for each major crossed.

`release.yml` in particular is verified **by inspection only**. The artifact round-trip through
`upload-artifact@v6` → `download-artifact@v7` → `action-gh-release@v3` has not been exercised end to
end. The first tag push after this merges is its real test.

## CI status

✅ **Green, and — the part that actually matters — annotation-free.**

No CI run existed for this branch before this PR: `ci.yml` triggers only on `push: main` and
`pull_request`, so the first run is the one this PR created.

| Run | Job | Conclusion | Annotations |
|---|---|---|---|
| [31539366957](https://github.com/clintcparker/site-checker/actions/runs/31539366957) | `rust` | `success` | **0** |
| [31539366957](https://github.com/clintcparker/site-checker/actions/runs/31539366957) | `frontend` | `success` | **0** |

Read via `gh api repos/clintcparker/site-checker/check-runs/<job_id>/annotations`. The empty list is
the evidence, not the green check — a Node 20 deprecation annotation appears on a run that *passes*,
which is the whole reason this warning went unnoticed for as long as it did.

**This closes T007 and confirms US1 on a live runner rather than by inspection.** US2 and US3 remain
unconfirmed live; see the task table below.

## Screenshots

No UI changes — screenshots skipped.

## Known issues

Three review rounds, in order: **⚠️ APPROVED WITH CONDITIONS** (14:30) → **❌ CHANGES REQUIRED**
(14:40, two blockers) → **⚠️ APPROVED WITH CONDITIONS** (14:46, **0 blockers**). Every blocker and
warning raised in the first two rounds is closed. The standing verdict is the third one.

[14:46 review — current](https://github.com/clintcparker/site-checker/blob/f5032b6ac4cc192965b24011c3cc7e474e791e75/specs/20260811-141602-ci-node24-action-bumps/reviews/review-20260811-144605.md)
· [14:40](https://github.com/clintcparker/site-checker/blob/f5032b6ac4cc192965b24011c3cc7e474e791e75/specs/20260811-141602-ci-node24-action-bumps/reviews/review-20260811-144021.md)
· [14:30](https://github.com/clintcparker/site-checker/blob/f5032b6ac4cc192965b24011c3cc7e474e791e75/specs/20260811-141602-ci-node24-action-bumps/reviews/review-20260811-143026.md)

### Closed

| ID | Sev | Finding | How it was closed |
|---|---|---|---|
| R001 | 🟡 | `ci.yml` claimed "lowest major that runs on node24" — false for two of its six pins | Rule restated with both exceptions named; QA TC-014 re-verified all six claims |
| B1 | 🔴 | `dependabot.yml` had no `ignore:`, so its first weekly run would re-propose the exact majors this feature declined — including `download-artifact@v8`, whose `digest-mismatch: error` default turns a logged warning into a **failed release run** | `ignore:` block over four actions (`checkout`, `setup-node`, `upload-artifact`, `download-artifact`) at `update-types: ["version-update:semver-major"]` — the major *line*, not a version, so a later v8/v9 does not reopen a settled question. Each carries a one-line reason |
| B2 | 🔴 | Three Minor findings neither fixed nor recorded | R003, S2, W2, and the security-update consequence recorded in `docs/ROADMAP.md`; R004 fixed; R005 fixed in `f5032b6` |
| W1 | 🟡 | The cargo/npm exclusion comment defended itself with a mechanism that does something else — `--locked`/`--frozen-lockfile` catch a lockfile disagreeing with its own manifest, not upstream drift | Comment rewritten to the real reason, and it now names the unstated consequence: **no security-update PRs for the Rust or JS trees** |
| S1 | 🟢 | Ragged mid-sentence wrap left by the R001 fix | Re-flowed |
| S3 / R004 | 🟢 | Eight actions ungrouped against the invisible default `open-pull-requests-limit: 5` | `groups:` entry plus an explicit limit, so the number is a choice |
| R005 | 🟢 | Stale `ci.yml` line numbers in `tasks.md` | Both old and current locations given (47/90/92/94) |
| C1 | 🟡 | This PR body originally reported B1 and B2 as open blockers, because it was composed from the 14:40 review while the fixes were still landing | Rewritten — this section |
| C3 | 🟢 | T007 satisfied but still unchecked in `tasks.md` | Checked off citing run 31539366957 |

### Open — carried deliberately

| ID | Sev | Finding |
|---|---|---|
| **C2** | 🟡 | **Three unattended speckit runs shared this working tree concurrently** — `/speckit-implement`, `/speckit-staff-review-run`, and this `/speckit-ship-run`, plus a QA pass. `ci.yml` was rewritten at 14:38 and again at ~14:42; `.github/dependabot.yml` was expanded between this ship run reading it and staging it, so the `ignore:` block landed inside `accd2ee` rather than as its own commit. Nothing was corrupted — the tree is clean and every gate is green — but review #1 was rendered against a moving target and the ship step first froze a verdict its own pipeline was still producing. Compounds the no-worktree-isolation note at the top |
| **S2** | 🟢 | **Nothing in CI runs `actionlint`.** Clean here only because it was run by hand, four times across two reviews and two QA passes. This branch edits workflows exclusively — precisely where the gap is most exposed. Pre-existing; deferred to `docs/ROADMAP.md` |
| **W2 / C5** | 🟢 | `docs/how-to/release.md` enumerates six exact pins and `ci.yml`'s comment names `setup-node` and `pnpm/action-setup` as "on v6", in the same change that configures weekly bumps. Four of six are now ignored at the major line, so the drift risk is **narrowed to `pnpm/action-setup` and `softprops/action-gh-release`** — neither has a major above its current pin today, but the first that does makes both prose blocks false. Nothing checks prose against pins |
| **R003** | 🟢 | `pnpm/action-setup`'s own README now points users at a successor, `pnpm/setup`. The v6 line is maintained and correctly node24, so nothing is wrong today — but Dependabot tracks versions, not successors, and will never surface it |
| **C4** | 🟢 | B2 was closed by recording findings in `docs/ROADMAP.md` — which `.gitignore` excludes. The Quality Gate is satisfied in letter while the evidence is **invisible to every reviewer of this PR**. Tracked as #21; noted because this is the first case where the gate itself depends on the invisible file |

## Incomplete tasks — 17/20 (85%)

T007 closed during this ship — see CI status above. The remaining three are open because none can be
closed from a local checkout or from a PR run, not because they were skipped. **For `release.yml`,
"the pins merged" must still not be read as "the warning is confirmed gone."**

| Task | What it needs | Status |
|---|---|---|
| **T015** | Verify the release path end to end via a throwaway prerelease tag: zero Node 20 annotations, both arch artifacts attach, `homebrew`'s `gh release download` still resolves the asset names | Blocked on a real tag push. Should stay visibly open |
| **T017** | Confirm Dependabot accepted `dependabot.yml` with no parse error | Blocked — Dependabot only parses the file once it lands on the default branch |
| **T020** | Re-read annotations on the newest `CI` run on `main` post-merge and confirm an empty list | Blocked by definition |

## Note on the base ref and the pushed head

`origin/main` is unchanged relative to this branch (0 commits behind), so no rebase was needed. Local
`main` carries one commit `origin/main` does not — `bd7a559` — but it is contained in this branch, so
it lands here rather than being missing from it.

All artifact links above pin `f5032b6`, the pushed head, rather than the branch name — the branch
dies on merge and the links would 404 with it.

## Checklist

- [x] Changelog updated
- [x] Documentation updated (`docs/how-to/release.md`, `ci.yml` comment block)
- [x] QA testing run — ⚠️ PARTIAL PASS, 15/15 locally-runnable scenarios pass
- [ ] All tasks completed — **17/20**; T007 closed during ship, T015/T017/T020 externally blocked
- [x] Code review passed — **⚠️ APPROVED WITH CONDITIONS** (14:46, third round, **0 blockers**). The
      conditions are that T015 stay visibly open and that `release.yml` be read as
      inspection-verified only. See Known issues
- [x] CI pipeline green — run 31539366957, both jobs `success` with **zero annotations**

---
*Generated by `/speckit.ship` from spec-driven development artifacts.*
