# Release Record — Retire the Node.js 20 Deprecation Warning in CI

**Ship run**: 2026-08-11 15:15 PDT (unattended)
**Feature**: `20260811-141602-ci-node24-action-bumps`
**Target**: `main` · **Remote**: `origin` (`git@github.com:clintcparker/site-checker.git`, **PUBLIC**)
**PR**: [#27](https://github.com/clintcparker/site-checker/pull/27) — **updated, not created** (an open PR already existed for this branch)
**Head shipped**: `067516a015794981f197046a2079e587eb09b8fe`
**Commit range**: `b64c0e2..067516a` (20 commits, 20 files changed, 5 outside `specs/`)

---

## Readiness check

```
Ship Readiness Check:
⚡️ Tasks:        17/20 complete — T015, T017, T020 open, all externally blocked
⚡️ Review:       APPROVED WITH CONDITIONS (15:03, fifth round, 0 blockers)
⚡️ QA:           PARTIAL PASS (18/21; 0 fail; 3 externally blocked)
✅ Working tree: clean after committing one untracked QA report
✅ Sync:         0 behind origin/main, 19 ahead — no rebase required
✅ CI:           run 31541576664 at the shipped head — success, 0 annotations

Overall: SHIPPED (with notes) — unattended run, all confirmations auto-answered yes
```

## What this run did

1. **Committed one untracked artifact** before the pre-flight check, per the unattended instruction: `067516a` *"docs: fourth QA run for the node24 action bumps"* — the QA run 4 report was sitting untracked.
2. **No rebase performed.** `git rev-list --count HEAD..origin/main` → `0`. The branch was already current with the base ref; the preferred rebase was unnecessary rather than skipped.
3. **Pushed** `418a9e9..067516a` to `origin`. No force-push at any point.
4. **Did not prepend a changelog entry.** `CHANGELOG.md` already carries this feature's entry (added in `bed16c1`, corrected in `4adc898` to match the shipped `dependabot.yml` and to narrow the "not verified on a live runner" caveat to `release.yml`). Prepending would have duplicated it. The existing entry was re-read against the current `dependabot.yml` and is accurate at the shipped head.
5. **Recomposed PR #27's body** against `067516a` and pushed it with `gh pr edit`. This closes staff review finding **E1** and corrects **E3**.
6. **Screenshots**: `screenshots/SKIPPED.md` present, no images — PR body carries the single line "No UI changes — screenshots skipped." `SKIPPED.md` was verified present in the pushed head (`git cat-file -e origin/<branch>:…`) before linking.

## Provenance and caveats carried into the PR

| Field | Value | Consequence |
|---|---|---|
| `worktree_isolation` | **failed** | Ran in the primary checkout `/Users/clint/src/clintcparker/site-checker`, not an isolated worktree. Worktrees are only created at `/speckit-specify`; later passes on this feature were resumed runs. Stated near the top of the PR body. |
| `session` | primary | Same cause as above. |
| Base ref vs. local | `origin/main` is **1 commit behind local `main`** (`bd7a559`, unpushed) | That commit **is** contained in this branch, so nothing is missing from the diff — but a reviewer at `origin/main` sees `bd7a559` as part of the PR rather than as pre-existing history. Merge-base used: `b64c0e2`. Stated in the PR body. |
| Gitignored inputs | `docs/` (except `how-to/`) and `specs/*/screenshots/` | `docs/ROADMAP.md` — where all five reviews' Minor findings live, including "`actionlint` is not a CI gate" — is unreadable from the PR. `SKIPPED.md` had to be force-added. Stated in the PR body. |

## Surviving findings reported in the PR

**0 blockers.** Full text under the PR's "Known issues" heading.

| ID | Sev | Source | Status at ship |
|---|---|---|---|
| E1 | 🟡 Warning | Review 15:03 | **Addressed by this run** — the body described the pre-`4adc898` `semver-major` ignore form as B1's accepted resolution and called the 14:46 review final. Body recomposed, all five rounds listed, links re-pinned. |
| E2 | 🟢 | Review 15:03 | **Addressed** by QA run 4's complement-form TC-013 (7/7). |
| E3 | 🟢 | Review 15:03 | **Corrected** — 4 / 10, not 3 / 11. Verified from the diff. |
| E4 | 🟢 | Review 15:03 | **Open** — T017 is load-bearing; `versions:` syntax asserted, not observed. Fallback shape recorded. |
| E5 | 🟢 | Review 15:03 | **Open and recurring** — concurrent QA/review/ship share one checkout. This run is another instance. |
| — | ⚠️ | QA 15:09 | 3 scenarios blocked on GitHub-side events (TC-011/T015, TC-014/T017, TC-021/T020). |

## Incomplete tasks

- **T015** [US2] — end-to-end release verification via a throwaway prerelease tag. Blocked on a release decision.
- **T017** [US3] — confirm Dependabot parsed the config with no error. Blocked on merge; **load-bearing** per E4.
- **T020** — re-read annotations on the first post-merge `CI` run on `main`. Blocked on merge.

## Verification at the shipped head

| Claim | How | Result |
|---|---|---|
| CI green at `067516a` | `gh api .../runs/31541576664/jobs` | ✅ `rust` success, `frontend` success |
| Zero annotations at `067516a` | `check-runs/93944913125/annotations`, `check-runs/93944913043/annotations` | ✅ `0` and `0` — seventh consecutive clean run |
| Branch pushed | `git ls-remote origin <branch>` | ✅ `067516a` |
| PR at the shipped head | `gh pr view 27 --json headRefOid` | ✅ `067516a`, OPEN |
| Repo visibility | `gh repo view --json visibility` | ✅ PUBLIC (screenshots moot — none exist) |
| Pin split (E3) | `git diff origin/main...HEAD -- <file> \| grep -c "^+.*uses:"` | ✅ `ci.yml` 4, `release.yml` 10 |
| No app surface touched | `git diff --name-only origin/main...HEAD -- src index.html src-tauri` | ✅ empty |

## Artifacts

- [tasks.md](../tasks.md) — the spec of record (no `spec.md`/`plan.md`; `check-prerequisites.sh --require-tasks` hard-fails on this feature)
- [Staff review 15:03](../reviews/review-20260811-150343.md) — ⚠️ APPROVED WITH CONDITIONS, current verdict
- [QA run 4, 15:09](../qa/qa-20260811-150910.md) — ⚠️ PARTIAL PASS, current verdict
- [PR description as shipped](pr-description-20260811-151517.md)
- [screenshots/SKIPPED.md](../screenshots/SKIPPED.md)

---

*Generated by `/speckit.ship`.*
