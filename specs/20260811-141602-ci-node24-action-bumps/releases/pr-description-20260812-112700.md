> ## ⚠️ This run had no worktree isolation
>
> `worktree_isolation=failed`, `session=primary`. This round executed in the **primary checkout**, not
> an isolated worktree. The feature's original worktree was consumed when PR #27 merged; this follow-up
> work was done directly on `main` in `/Users/clint/src/clintcparker/site-checker`, and the ship step cut
> the head branch `20260812-113500-dependabot-ignore-syntax-fix` at `main`'s tip to have something to
> open a PR from. **The user's working directory was switched onto that branch** — local `main` was left
> untouched at `1e3bc88` and was never reset.
>
> Two consequences a reviewer should know about:
>
> - **Local `main` was 2 commits ahead of `origin/main`** (`59db8a8`, `1e3bc88` — screenshot-skip
>   records). Both are carried in this PR, so someone reading `origin/main` is seeing them for the first
>   time here.
> - **Concurrent speckit runs share this checkout and committed into the branch while the PR was being
>   opened.** A sixth QA run (`qa-20260812-112530.md`) and a further screenshot re-verification landed
>   after PR creation and are included below. The branch was pushed again; nothing was force-pushed.

## Summary

`.github/dependabot.yml` shipped an `ignore:` block in which **every entry was a silent no-op**, and
Dependabot proved it in production: it opened
[PR #28](https://github.com/clintcparker/site-checker/pull/28) **64 seconds** after the file landed on
`main`, proposing all four majors the file declined, and that PR merged on a green check three minutes
later. This PR fixes the syntax, evaluates after the fact the four majors that were crossed, pins back
the one that carried a real behavior change, and corrects every artifact in the repo that was left
describing pins `main` no longer carries.

Nothing user-visible. No file under `src/`, `src-tauri/`, or `index.html` is touched.

## The defect

The `github-actions` ecosystem parses `versions:` as a **Gem requirement**. RubyGems accepts `8.x` as a
legal *version* — it permits alphabetic segments, as in `1.0.beta` — so `versions: ["8.x"]` compiled to
the equality `= 8.x`, a match against a version that will never exist. It raised no error; it simply
matched nothing. The grammar was reproduced in Ruby rather than assumed:

| Input | Compiles to | Matches |
|---|---|---|
| `"8.x"` | `= 8.x` | nothing (`Gem::Version.correct?("8.x")` → `true`, which is *why* it was silent) |
| `"8"` | `= 8` | `8`, `8.0.0` — the bare major tag Dependabot reports |
| `"7.*"` | — | raises `Gem::Requirement::BadRequirementError` (would have been loud) |

All four entries are now bare integers. `>= 8` also parses but declines every future major forever —
the permanent-silence failure that blocker D1 was raised against two reviews ago, and the reason the
narrow per-version form exists.

**The fix is verified against the incident that exposed it, not just against a REPL.** QA run 6 pulled
PR #28's own `updated-dependencies` trailer: it emits `dependency-version: '8'` for `download-artifact`
and `'7'` for `checkout`, `setup-node`, and `upload-artifact` — bare integers, exactly what the
corrected `= 7` / `= 8` requirements match. That was the one unobserved premise run 5 had to assume.

## Implementation

There is **no `spec.md` and no `plan.md`** for this feature — it bypassed `/speckit-specify` and
`/speckit-plan`, and [`tasks.md`](https://github.com/clintcparker/site-checker/blob/d8af34da8c6f35661454835e3ecdc0c1e89ee5d9/specs/20260811-141602-ci-node24-action-bumps/tasks.md)
is the authority.

| File | Change |
|---|---|
| `.github/dependabot.yml` | Four `ignore:` entries → bare integers. Comment block documents the grammar trap, states plainly that every entry is **inert today**, and gives the two reasons they are kept. |
| `.github/workflows/release.yml` | `download-artifact@v8` gains `digest-mismatch: warn`, with the reasoning inline. |
| `.github/workflows/ci.yml` | Comment block gains the per-major evaluation of the four crossed pins that should have preceded PR #28's merge. |
| `docs/how-to/release.md` | Pin list corrected to match `release.yml`; new section on the pin that was not the one evaluated. |
| `CHANGELOG.md` | 2026-08-12 entry added; the 2026-08-11 entry marked **Superseded in part**. |
| `specs/…/tasks.md` | T017 and T020 closed, T001's action count corrected 7 → 8, "Deliberately Out of Scope" given a dated banner. |

**PR #28 is deliberately not reverted.** A human merged it on a green check and the pin set is that
human's call. All four crossed majors were read at their `action.yml` and release notes; exactly one
had an effect:

- `download-artifact@v8` — **real**. Promotes a digest mismatch from a logged warning to a run failure.
  Pinned back to `warn`. Its decompression change is narrower than originally feared: it skips unzipping
  only for content whose `Content-Type` is not a zip, and `skip-decompress` defaults `false`, so the
  `release` job's `artifacts/site-checker-<arch>/…` globs still resolve.
- `upload-artifact@v7` — inert. Direct upload is opt-in; `archive` defaults `true`.
- `checkout@v7` — inert. The fork-PR block fires only on `pull_request_target`/`workflow_run`, neither
  of which any workflow here uses, and the `homebrew` job pushes with `TAP_PUSH_TOKEN` rather than a
  persisted checkout credential.
- `setup-node@v7` — inert. Inherits v6's npm-only caching narrowing, which all three call sites bypass
  by passing `cache: pnpm` explicitly.

## Testing

Two QA runs at this tree state, three minutes apart, executed independently. **They agree on every
result.**

- [QA run 5](https://github.com/clintcparker/site-checker/blob/d8af34da8c6f35661454835e3ecdc0c1e89ee5d9/specs/20260811-141602-ci-node24-action-bumps/qa/qa-20260812-112100.md)
  — ⚠️ **PARTIAL PASS**, 26 scenarios: 22 pass, 2 partial, 2 blocked, **0 fail**.
- [QA run 6](https://github.com/clintcparker/site-checker/blob/d8af34da8c6f35661454835e3ecdc0c1e89ee5d9/specs/20260811-141602-ci-node24-action-bumps/qa/qa-20260812-112530.md)
  — ⚠️ **PARTIAL PASS**, 26 scenarios: 22 pass, 3 partial, 2 blocked, **0 fail**. Full independent
  re-execution, nothing carried forward. Adds the PR #28 trailer evidence above, plus TC-026 bounding
  the working tree's executable delta, plus one new partial (TC-017, below).

| Suite | Result |
|---|---|
| `cargo test` (src-tauri) | 62/62 |
| `vitest run` (frontend, 6 files) | 47/47 |
| `cargo clippy --all-targets -- -D warnings` | clean |
| `actionlint` (3 workflows) | exit 0 |

All four constitution gates green in both runs. The Rust and frontend suites are unaffected by action
pins and serve as gates rather than as coverage of this change — correctly so, since no source file
appears in the diff.

**US1 is verified live and the diff cannot have disturbed it.** Run 6 measured the executable delta
rather than assuming it: `ci.yml`'s diff against `origin/main` is **comment-only** — every changed
non-`#` line is blank — and `release.yml`'s sole executable change is the added `digest-mismatch: warn`,
which lives on the `release` job that `CI` never invokes. So the pin set CI validated at `457f572`
(`rust` and `frontend` both `success`, both annotation lists length 0, zero "Node.js 20 is deprecated"
messages) is the pin set in this branch.

## Review

Eight reviews across this feature; the last four are in this PR. Final:
[review-20260812-111856.md](https://github.com/clintcparker/site-checker/blob/d8af34da8c6f35661454835e3ecdc0c1e89ee5d9/specs/20260811-141602-ci-node24-action-bumps/reviews/review-20260812-111856.md)
— ⚠️ **APPROVED WITH CONDITIONS**. Four blockers were raised across this round (R001–R004); **all four
are fixed and zero remain.**

## Known issues

Both the review and both QA reports carry non-passing verdicts. Nothing below blocks the merge, and none
of it is resolvable from a local checkout.

- **C1 / W003 — `digest-mismatch: warn` is a maintainer decision (🟡 open).** v8 defaulted this to
  `error` deliberately ("to be secure by default"). Pinning back to `warn` re-opens that integrity
  check. The justifying comment at `release.yml:238–240` claims failing mid-release "leaves a tag
  pushed with no release object, which is worse than publishing and re-running" — but
  `docs/how-to/release.md` § "Re-running a failed release" documents two files away that a re-run from
  the same tag *converges*, and that `action-gh-release` updates an existing release in place. So
  `error`'s failure mode is recoverable by a path this repo already relies on, while `warn`'s failure
  mode publishes an artifact that failed its digest check to a Homebrew-installed release. **If you keep
  `warn`, drop that clause. If you don't, delete the input.** Left unresolved deliberately — neither
  review nor QA would make this call unilaterally.
- **C2 / TC-014 — US3's fix is correct but cannot be observed working end to end (🟡 open, no action
  available).** Every `ignore:` entry is inert today, and not because of a syntax problem: PR #28 moved
  the tree onto `checkout@v7`, `setup-node@v7`, `upload-artifact@v7`, and `download-artifact@v8`, which
  are the newest majors each has published. The entries name versions the repo already runs, and
  Dependabot only proposes upgrades, so there is nothing left to decline. The first `checkout@v8`, or
  the first pin dropped below an entry, is the real test. **Do not read this block as live protection
  for the current pins.** The file says exactly this, in the file.
- **T015 — the release path has still never run end to end.** This is the one incomplete task, of 20
  total (19 done):
  > `[ ] T015 [US2]` Verify the release path end to end by pushing a throwaway prerelease tag, then
  > confirm zero Node 20 annotations, that both arch artifacts attach to the release, and that the
  > `homebrew` job's `gh release download` still resolves the asset names.

  The `upload-artifact@v7 → download-artifact@v8 → action-gh-release@v3` round-trip has been exercised
  at no pin, and it now carries a `digest-mismatch: warn` placed by inspection rather than by a run. It
  requires a real tag push that publishes a GitHub Release and writes to `clintcparker/homebrew-tap`, so
  it stays a release decision rather than a QA step. **This feature's riskiest deliverable is the one
  with no local verification surface at all.**
- **TC-017 — the PR #28 sequence can recur, and it is a process gap rather than an automation one
  (⚠️ new in run 6).** Nothing in the configuration merged PR #28: `allow_auto_merge` is `false`, `main`
  carries **no branch protection**, no workflow auto-merges Dependabot PRs, and PR #28 shows
  `autoMergeRequest: null` with **zero reviews** — a person merged it three minutes after it opened.
  Meanwhile four next-majors are now reachable and undeclined (`checkout` v8, `setup-node` v8,
  `upload-artifact` v8, `download-artifact` v9) under a group that still bundles `patterns: ['*']` into
  one PR. That is the *correct* configuration for US3's stated goal — a deprecation must surface as a
  PR — but the same one-PR-four-majors shape will reappear, and the ignore block cannot stop it, because
  ignore entries only decline versions they name.
- **TC-013 — inverted assertion, not a regression (🟢 low).** Run 4 asserted "every declined major sits
  strictly above its pin"; all four now sit at or below it. Same fact as C2. The property US3 actually
  depends on still passes 7/7: no blanket `update-types: ["version-update:semver-major"]` remains
  anywhere, and every action has a named reachable next major, so **no permanent silence exists**.
- **S005 — the "every entry is INERT" banner has a shelf life (🟢 suggestion, not done).** It becomes
  false the moment any of these actions ships a new major, at which point the entry above it goes live
  and the banner misleads in the opposite direction from the defect it documents. It is dated, which is
  most of the mitigation.

Also worth stating plainly, from the review: **there is no local check for any of this.** `actionlint`
does not read `dependabot.yml` and no schema catches a semantically empty ignore. The only signal is on
Dependabot's side, and it is unambiguous — a PR proposing a version that appears in an `ignore` list
means the ignore is broken.

## Screenshots

No UI changes — screenshots skipped.

## CI

`ci.yml` triggers on `push: [main]` and `pull_request`, so the branch push alone started nothing and no
run existed when this PR was created — the first `CI` run started with the PR itself. The ship step did
not block on it, but by the time the branch settled four runs had completed and **all four are green**:

| Run | Commit | Result |
|---|---|---|
| [31627878936](https://github.com/clintcparker/site-checker/actions/runs/31627878936) | `c6f4396` | ✅ success |
| [31627943150](https://github.com/clintcparker/site-checker/actions/runs/31627943150) | `00a4953` | ✅ success |
| [31627992028](https://github.com/clintcparker/site-checker/actions/runs/31627992028) | `5f74647` | ✅ success |
| [31628048776](https://github.com/clintcparker/site-checker/actions/runs/31628048776) | `d8af34d` | ✅ success |

A fifth run was queued for the branch tip at the time of writing. **Check the Checks tab for the
authoritative current result rather than this table.**

The most recent `CI` run on `main`
([31544339526](https://github.com/clintcparker/site-checker/actions/runs/31544339526) @ `457f572`)
concluded **success** on both `rust` and `frontend` with empty annotation lists, and this branch's
executable delta from that run is one `with:` input on a job `CI` never invokes.

## Checklist

- [x] All tasks completed — **19/20**; T015 open and blocked on a real tag push (see Known issues)
- [x] Code review passed — ⚠️ approved with conditions C1, C2; 0 blockers
- [x] QA testing passed — ⚠️ partial pass ×2 (independent runs, agreeing); 0 failures
- [x] CI pipeline green — 4 consecutive runs green on this branch; a 5th was queued at the tip
- [x] Changelog updated
- [x] Documentation updated — `docs/how-to/release.md`

## Artifacts

Links pinned to `d8af34d`. `specs/*/releases/`, `specs/*/qa/responses/`, and `specs/*/screenshots/` are
all gitignored in this repo — the text records below were force-added so they reach the PR, but the QA
`responses/` evidence files cited inside the reports are **local-only and not in this branch.**

- [tasks.md](https://github.com/clintcparker/site-checker/blob/d8af34da8c6f35661454835e3ecdc0c1e89ee5d9/specs/20260811-141602-ci-node24-action-bumps/tasks.md)
- [Final review — 8th](https://github.com/clintcparker/site-checker/blob/d8af34da8c6f35661454835e3ecdc0c1e89ee5d9/specs/20260811-141602-ci-node24-action-bumps/reviews/review-20260812-111856.md)
- [Sixth review — R001–R003](https://github.com/clintcparker/site-checker/blob/d8af34da8c6f35661454835e3ecdc0c1e89ee5d9/specs/20260811-141602-ci-node24-action-bumps/reviews/review-20260812-110430.md)
- [QA run 5](https://github.com/clintcparker/site-checker/blob/d8af34da8c6f35661454835e3ecdc0c1e89ee5d9/specs/20260811-141602-ci-node24-action-bumps/qa/qa-20260812-112100.md)
- [QA run 6](https://github.com/clintcparker/site-checker/blob/d8af34da8c6f35661454835e3ecdc0c1e89ee5d9/specs/20260811-141602-ci-node24-action-bumps/qa/qa-20260812-112530.md)
- [Screenshot skip record](https://github.com/clintcparker/site-checker/blob/d8af34da8c6f35661454835e3ecdc0c1e89ee5d9/specs/20260811-141602-ci-node24-action-bumps/screenshots/SKIPPED.md)
- [Ship record](https://github.com/clintcparker/site-checker/blob/d8af34da8c6f35661454835e3ecdc0c1e89ee5d9/specs/20260811-141602-ci-node24-action-bumps/releases/release-20260812-112700.md)
- No `spec.md`, no `plan.md` — this feature bypassed `/speckit-specify` and `/speckit-plan`.

---
*Generated by `/speckit.ship` from spec-driven development artifacts.*
</content>
