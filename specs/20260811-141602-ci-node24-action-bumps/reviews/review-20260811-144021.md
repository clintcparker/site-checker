# Code Review Report: Retire the Node.js 20 Deprecation Warning in CI

**Reviewer**: AI Agent (Staff Engineer Perspective)
**Date**: 2026-08-11 14:40 PDT
**Feature**: [tasks.md](../tasks.md) — no `spec.md` or `plan.md` exists (see Provenance below)
**Branch**: `20260811-141602-ci-node24-action-bumps`
**Prior review**: [review-20260811-143026.md](review-20260811-143026.md) (⚠️ APPROVED WITH CONDITIONS, 0 blockers)
**Verdict**: ❌ **CHANGES REQUIRED**

---

## Executive Summary

The pins themselves are correct and I re-derived that from primary sources rather than inheriting
it — I read `runs.using` out of `action.yml` at **fourteen** tags, including every *rejected* major,
not just the chosen ones. Every target resolves to `node24`; every rejected lower major really is
still `node20`; every input the workflows pass still exists at the target ref. All 20 `uses:` lines
are accounted for, no `@v4` remains, `Swatinem/rust-cache@v2` and `dtolnay/rust-toolchain@stable`
are correctly untouched, `verify-install-channels.yml` genuinely has zero `uses:` lines, all four
YAML files parse, `actionlint` exits 0, and the four constitution gates are green and unmoved
(62 Rust / 47 frontend / clippy clean). **T012's open question resolves clean**: I confirmed from
v7's own `action.yml` that `merge-multiple` still defaults `'false'` — "extracted into individual
named directories within the specified path" — so the `release` job's
`artifacts/site-checker-<arch>/…` globs still resolve with no change.

**The blocker is not in the pins. It is that this branch is walking into `/speckit-ship` with an
Important finding from its own prior review still open.** The constitution's Quality Gates are
explicit: *"No Critical or Important findings open from review; Minor findings either fixed or
recorded in the roadmap rather than silently dropped."* Since the 14:32 review, `ci.yml` was
amended at 14:38 and R001 is genuinely fixed — the "lowest major" claim now carries its two
exceptions by name, which I verified is the honest statement (`setup-node@v5` and
`pnpm/action-setup@v5` are both already `node24`, so the v6 pins really are one major above the
minimum). But `.github/dependabot.yml` has not been touched since 14:22, before the review, and
R002 stands exactly as written (**B1**). R003–R005 were likewise neither fixed nor recorded in
`docs/ROADMAP.md`, which I grepped — no mention of dependabot, `download-artifact`, `pnpm/setup`,
or node24 anywhere in it (**B2**).

R002's substance survives independent checking and is worse than the prior review scoped it. I
confirmed `download-artifact@v8`'s `digest-mismatch` input defaults to **`error`** — "which will
fail the action" in its own description — so that is a real release-path hard failure, not a
style preference. And the prior review's ignore list of three is **incomplete**: it named
`checkout@v6`, but `checkout@v7` exists, so Dependabot proposes v7; `setup-node@v7` and
`upload-artifact@v7` also exist and `setup-node@v7` is explicitly in tasks.md's "Deliberately Out
of Scope". The correct list is **four** actions, ignored at the major level, not three at a version.

Two further findings are mine and were not raised before. `dependabot.yml`'s comment defends
excluding cargo and npm with a mechanism that does not do what it claims — `--locked` and
`--frozen-lockfile` catch a lockfile that disagrees with its manifest, not a dependency that has
fallen behind upstream (**W1**). And US2 and US3 quietly work against each other: T019 wrote a
hardcoded six-pin version list into `docs/how-to/release.md` and T018 named two specific v6 pins in
`ci.yml`'s comment, in the same change that configures Dependabot to bump those exact pins weekly —
both prose blocks go false on the first merged bump and nothing checks them (**W2**).

Four of twenty tasks (T007, T015, T017, T020) remain open. That is correct and well-documented —
none can be closed from a local checkout — but it means **nothing on this branch has been observed
on a live runner**. Every claim here, including mine, is verified by inspection.

---

## Review Findings

| ID | Severity | File | Line(s) | Category | Finding | Recommendation |
|----|----------|------|---------|----------|---------|----------------|
| B1 | 🔴 Blocker | `.github/dependabot.yml` | 14–20 | Constitution / Spec Compliance | **Constitution Quality Gates violation**: *"No Critical or Important findings open from review."* R002 of [review-20260811-143026.md](review-20260811-143026.md) — an Important (🟡) finding — is open and un-actioned; the file's mtime (14:22) predates that review (14:32), while `ci.yml` was amended at 14:38 to fix R001. Substance re-verified and it is worse than scoped: the config has no `ignore:`, so the first weekly run after merge proposes exactly the majors `tasks.md` §"Deliberately Out of Scope" evaluated and declined, and re-proposes them every week. I read `actions/download-artifact@v8`'s `action.yml` directly — `digest-mismatch` defaults to `'error'`, described as "will fail the action" — so accepting that PR turns a logged warning into a failed release run. The prior review's list of three is also incomplete: `checkout@v7`, `setup-node@v7`, and `upload-artifact@v7` all exist (SHAs verified), so Dependabot proposes v7 for checkout — not the v6 that was named — and `setup-node@v7` is declined by `tasks.md` but was omitted from the list entirely. | Add an `ignore:` block covering **four** actions — `actions/checkout`, `actions/setup-node`, `actions/upload-artifact`, `actions/download-artifact` — each with `update-types: ["version-update:semver-major"]` so it tracks the major line rather than a version that will move, and each with a one-line reason. Re-evaluating later then means deleting an entry deliberately, not re-litigating a PR. |
| B2 | 🔴 Blocker | `docs/ROADMAP.md` | — | Constitution | **Constitution Quality Gates violation**: *"Minor findings either fixed or recorded in the roadmap rather than silently dropped."* R003 (`pnpm/action-setup` has a named successor, `pnpm/setup`), R004 (ungrouped weekly updates against the invisible default `open-pull-requests-limit: 5`), and R005 (stale `ci.yml` line numbers in `tasks.md`) are all 🟢 Minor findings from the prior review. None is fixed and none is in `docs/ROADMAP.md` — I grepped it for `dependabot`, `download-artifact`, `action-setup`, `pnpm/setup`, and `node24` and got zero hits. Shipping now drops three findings silently, which is the specific outcome the gate names. | Record R003 and R004 in `docs/ROADMAP.md` (the constitution's own designated home for deferred work, untracked and local by design), and fix R005 in place since it is a one-line factual correction. R004 is cheapest fixed rather than deferred — it is the same file as B1. |
| W1 | 🟡 Warning | `.github/dependabot.yml` | 10–13 | Documentation Accuracy | The comment justifies excluding cargo and npm: *"Those already fail loudly when they drift — `cargo test --locked` and `pnpm install --frozen-lockfile` in ci.yml both hard-error on a lockfile that no longer matches."* The mechanism does not do this. Both flags detect a lockfile that disagrees with **its own manifest** — a lockfile the committed `Cargo.toml`/`package.json` no longer matches. Neither reports that an upstream dependency has published a newer version, and neither surfaces a security advisory. So the stated reason for the exclusion describes a guard against a different failure entirely, and the actual consequence — no dependency or security-update PRs for the Rust and JS trees — is left unstated. Same class as R001, which was fixed: a comment asserting something false. The constitution requires "the code comments record why the tricky parts are the way they are — keep both honest." | Restate as what is actually true: cargo/npm are excluded because this is a personal single-user app where dependency churn is not worth the PR volume, and `--locked`/`--frozen-lockfile` ensure the *pinned* set stays reproducible. If security updates are wanted without the churn, that is a `security-updates-only` decision worth naming rather than leaving implied. |
| W2 | 🟡 Warning | `docs/how-to/release.md`, `.github/workflows/ci.yml` | 122–128, 11–16 | Documentation Accuracy / Architecture | US2 and US3 pull against each other and the change ships both. T019 wrote a **hardcoded list of six exact pins** into `docs/how-to/release.md` (`checkout@v5`, `setup-node@v6`, `pnpm/action-setup@v6`, `upload-artifact@v6`, `download-artifact@v7`, `action-gh-release@v3`), and T018's `ci.yml` comment names `setup-node` and `pnpm/action-setup` as being "on v6" specifically. In the same change, `dependabot.yml` is configured to propose bumps to those exact actions weekly. The first merged Dependabot PR makes both prose blocks false, and nothing — not CI, not `actionlint` — checks prose against pins. Concrete failure: six months out, `release.yml` says `checkout@v6` while `docs/how-to/release.md` still says v5, and a maintainer reasoning about the runner floor trusts the doc. | Replace the enumerated list in `docs/how-to/release.md` with the invariant it is trying to state — "every JS action in `release.yml` is pinned to a node24 major, which implies runner ≥ 2.327.1" — and point at the file for the actual versions. Same for `ci.yml`: state the rule and the *reason* for the two exceptions without pinning the sentence to "v6". A doc that names no version cannot go stale. |
| S1 | 🟢 Suggestion | `.github/workflows/ci.yml` | 14–16 | Maintainability | The R001 fix at 14:38 left a ragged wrap mid-sentence — `# consulted; pnpm's` is a short orphan line followed by `# v6 only adds pnpm 11 support…`. Purely cosmetic, but it is visible evidence of a hand-patched comment in a block whose whole purpose is to read as considered. | Re-flow the paragraph to the file's existing ~76-column comment width. |
| S2 | 🟢 Suggestion | `.github/workflows/` (all) | — | Test Quality | Nothing in CI runs `actionlint`. It is clean here only because it was run by hand during review — twice now, across two reviews. This feature edits workflows exclusively, which is precisely the case where the gap is most exposed: a malformed edit is caught by a maintainer remembering, or by the run failing. Pre-existing and out of scope, recorded so it is a known gap rather than an assumed cover. | Add an `actionlint` step to the `frontend` job, or a small `workflows` job gated on `paths: .github/**`. Cheap, and it would have been the automated check this entire feature lacked. |
| S3 | 🟢 Suggestion | `.github/dependabot.yml` | 14–20 | Efficiency | Carried from prior R004 and confirmed: eight distinct actions, ungrouped, weekly, against Dependabot's **default** `open-pull-requests-limit: 5`. A week where several move opens five PRs and then silently stops — the cap is invisible, which is the same "a signal nobody sees" failure mode this file exists to fix. | Fold into the B1 edit: add a `groups:` entry collapsing `github-actions` into one PR, or set `open-pull-requests-limit` explicitly so the number is a choice. |

**Categories**: Constitution, Spec Compliance, Documentation Accuracy, Architecture, Maintainability, Efficiency, Test Quality

---

## What was independently verified during this review

Re-derived from the live repositories at each named tag. Not inherited from the prior review — the
point of re-checking is that a verification table is exactly the artifact that gets copied forward.

| Claim | Verified how | Result |
|---|---|---|
| Every target tag exists | `gh api repos/<a>/git/ref/tags/<v>` | ✅ All resolve to a commit SHA |
| Every target is `runs.using: node24` | Read `action.yml` at each target ref | ✅ `checkout@v5`, `setup-node@v6`, `pnpm/action-setup@v6`, `upload-artifact@v6`, `download-artifact@v7`, `action-gh-release@v3`, `rust-cache@v2` — all `node24` |
| The **rejected** lower majors really are still node20 | Same method at `upload-artifact@v5`, `download-artifact@v6`, `action-gh-release@v2` | ✅ All three `node20` — so v6/v7/v3 genuinely are the first node24 majors; the jump is not gratuitous |
| …but `setup-node@v5` and `pnpm/action-setup@v5` are **not** | Same method | ✅ **Both already `node24`.** The v6 pins are one major above the minimum — which is exactly what the amended `ci.yml` comment now says. R001 correctly closed. |
| `download-artifact@v7` still extracts into named subdirs (T012) | Read v7 `action.yml` | ✅ `merge-multiple` default `'false'`, described as "extracted into individual named directories within the specified path". `artifacts/site-checker-<arch>/…` globs resolve unchanged |
| `download-artifact@v8` is genuinely dangerous to accept | Read v8 `action.yml` | ✅ `digest-mismatch` default `'error'`, "will fail the action". Confirms B1 is a correctness concern, not preference |
| `action-gh-release@v3` preserves the "converges on one release" property | Read v3 `action.yml` | ✅ `overwrite_files` still defaults `'true'`; `files`, `fail_on_unmatched_files`, `generate_release_notes` all still accepted |
| `pnpm/action-setup@v6` needs no `version` input | `package.json:5` | ✅ `"packageManager": "pnpm@10.30.3"` present; v6 honors the pin |
| Which majors Dependabot would actually propose | `gh api .../git/ref/tags` for v6/v7 of each | ✅ `checkout@v7`, `setup-node@v7`, `upload-artifact@v7`, `download-artifact@v8` all exist. `pnpm/action-setup@v7` and `action-gh-release@v4` do **not** — correctly excluded from the ignore list |
| No `@v4` pin left behind | `grep -rn "uses:" .github/workflows/` | ✅ 20 `uses:` lines, matching the audit; zero `@v4` |
| `verify-install-channels.yml` has no actions | Same grep | ✅ 0 `uses:` lines. Correctly untouched |
| Findings recorded in the roadmap? | `grep -i` over `docs/ROADMAP.md` | ❌ **Zero hits** for dependabot / download-artifact / action-setup / node24. Basis for B2 |
| `docs/how-to/release.md` is tracked despite `docs/` being gitignored | `git ls-files docs/` | ✅ The only tracked path under `docs/`. T019's edit reaches the PR; a ROADMAP entry would not |
| All four YAML files parse | `yaml.safe_load` | ✅ Including `dependabot.yml` |
| `actionlint` | Run on all three workflows | ✅ Exit 0, no diagnostics |
| Constitution quality gates | Ran all four locally | ✅ `cargo test --locked` 62 passed; `cargo clippy -- -D warnings` clean; `pnpm test` 47 passed |

---

## Spec Coverage Matrix

There is no `spec.md`. `tasks.md` is the authority; its three user stories stand in for functional
requirements.

| Story / Task | Requirement | Status | Evidence |
|---|---|---|---|
| **US1** | `CI` produces zero Node 20 annotations on `rust` and `frontend` | ⚠️ Implemented, unverified | T003–T006 correct. **T007 open** — `ci.yml` triggers only on `push` to `main` and `pull_request`, so no run exists until the PR does |
| T003 | `checkout@v4` → `@v5`, both jobs | ✅ | `ci.yml:44`, `ci.yml:87` |
| T004 | `pnpm/action-setup@v4` → `@v6` | ✅ | `ci.yml:89` |
| T005 | `setup-node@v4` → `@v6`, inputs preserved | ✅ | `ci.yml:91–94`; `node-version: 22`, `cache: pnpm` unchanged |
| T006 | YAML valid, no stray `@v4` | ✅ | `yaml.safe_load` OK; `actionlint` clean; grep confirms zero `@v4` |
| T007 | Read annotations off a real `CI` run | ⏸️ Blocked on PR | Documented as blocked, not skipped |
| **US2** | `Release` clean across all five jobs | ⚠️ Implemented, unverified | T008–T014 correct. **T015 open** — needs a real tag push |
| T008 | `checkout@v5` ×3 | ✅ | `release.yml:93, 139, 264` |
| T009 | `pnpm/action-setup@v6` ×2 | ✅ | `release.yml:103, 150` |
| T010 | `setup-node@v6` ×2, inputs preserved | ✅ | `release.yml:105–108, 152–155` |
| T011 | `upload-artifact@v6`, inputs preserved | ✅ | `release.yml:218–222`; `if-no-files-found: error` intact |
| T012 | `download-artifact@v7`, unzip behavior confirmed | ✅ | `release.yml:231–233`; `merge-multiple` default verified — globs unchanged |
| T013 | `action-gh-release@v3`, three inputs preserved | ✅ | `release.yml:238–248` |
| T014 | YAML valid, `rust-cache@v2` untouched | ✅ | `release.yml:99, 145` unchanged, verified node24 |
| T015 | End-to-end release verification | ⏸️ Blocked on a release decision | Correctly deferred |
| **US3** | Future deprecations arrive as PRs | ❌ **Implemented but defective** | T016 created the file, but see **B1** — as configured its loudest weekly output is the set of decisions US2 just made. **T017 open** |
| T016 | `.github/dependabot.yml`, github-actions, `/`, weekly | ⚠️ | File valid and parses, but no `ignore`, no `groups`, no explicit limit |
| T017 | Confirm Dependabot accepted the config | ⏸️ Blocked on merge | |
| **Polish** | | | |
| T018 | Record the pinning convention in `ci.yml` | ✅ | Amended 14:38; the exception clause is accurate — verified against `setup-node@v5`/`pnpm@v5` |
| T019 | Runner floor in `docs/how-to/release.md`, after confirming tracked | ⚠️ | Done and file confirmed tracked, but see **W2** — the enumerated pin list will go stale |
| T020 | Re-read annotations on `main` post-merge | ⏸️ Blocked on merge | |

**Spec coverage**: 16 of 20 tasks complete (80%). All four open tasks are external-verification
tasks that cannot run from a local checkout, each with a stated unblocking condition. **Zero scope
creep** — every changed line maps to a task, and nothing in "Deliberately Out of Scope" was touched.

---

## Test Coverage Assessment

`tasks.md` declares no automated test tasks, and that remains the right call: the verification
surface is the GitHub Actions annotation list, which has no local equivalent. What matters is
whether the substitute — inspection against primary sources — was done, and it was.

| Area | Tests Exist? | Status | Gaps |
|---|---|---|---|
| `cargo test --locked` | ✅ | 62 passed | Unaffected — zero source files changed |
| `pnpm test` | ✅ | 47 passed | Unaffected |
| `cargo clippy -- -D warnings` | ✅ | Clean | Unaffected |
| `actionlint` (all workflows) | ❌ not in CI | Exit 0 by hand | **S2** — the one gap that this feature's own change surface would benefit from |
| YAML parse (4 files) | ❌ not in CI | All parse by hand | Covered incidentally by `actionlint` for workflows; `dependabot.yml` is checked by nothing |
| Annotation-free CI run | ❌ | ⏸️ T007 | The actual acceptance criterion. Not observable until the PR exists |
| Release artifact round-trip | ❌ | ⏸️ T015 | `upload@v6 → download@v7 → gh-release@v3` unexercised end to end |

**Nothing on this branch has run on a live runner.** The gates above confirm the branch is *inert*
with respect to application code — not that it fixed anything.

---

## Metrics Summary

| Metric | Value |
|---|---|
| Files reviewed | 4 changed (`ci.yml`, `release.yml`, `docs/how-to/release.md`, `dependabot.yml` new) + 3 read for scope (`verify-install-channels.yml`, `ROADMAP.md`, prior review) |
| Lines changed | +49 / −14 tracked, plus 21 new lines in `dependabot.yml` |
| Action pins changed | 14 `uses:` lines, 6 distinct actions |
| Action pins deliberately unchanged | 6 lines, 2 actions |
| 🔴 Blockers | **2** |
| 🟡 Warnings | 2 |
| 🟢 Suggestions | 3 |
| Spec coverage | 16 / 20 tasks (80%); 4 open, all externally blocked and documented |
| Action tags re-read from source | 14 |
| Prior-review findings closed | 1 of 5 (R001) |
| Constitution gates green | 4 / 4 code gates; **1 process gate failing** (findings-open) |

---

## Recommended Actions

### Must Fix (Blockers)

1. **B1 — `.github/dependabot.yml`**: add an `ignore:` block for `actions/checkout`,
   `actions/setup-node`, `actions/upload-artifact`, and `actions/download-artifact`, each with
   `update-types: ["version-update:semver-major"]` and a one-line reason drawn from `tasks.md`
   §"Deliberately Out of Scope". Four, not the three the prior review named — `setup-node@v7`
   exists and is declined, and `checkout@v7` means the proposal is v7, not v6.
2. **B2 — `docs/ROADMAP.md`**: record R003 (`pnpm/action-setup` → `pnpm/setup` successor migration)
   and, if not fixed inline, R004/S3. Fix R005's stale `ci.yml` line numbers in `tasks.md` directly.

### Should Fix (Warnings)

3. **W1** — correct `dependabot.yml`'s cargo/npm exclusion rationale; `--locked` and
   `--frozen-lockfile` do not detect upstream drift.
4. **W2** — de-enumerate the pin list in `docs/how-to/release.md` and the "v6" references in
   `ci.yml`'s comment, so Dependabot cannot falsify prose it does not update.

### Nice to Fix (Suggestions)

5. **S3** — `groups:` or an explicit `open-pull-requests-limit` in `dependabot.yml` (same edit as B1).
6. **S1** — re-flow the ragged comment wrap at `ci.yml:14–16`.
7. **S2** — add `actionlint` to CI. Pre-existing gap; the natural follow-up to a workflows-only feature.

### Conditions that survive regardless of the fixes above

- The PR body must state that **T007, T015, T017, and T020 are open**, that nothing on this branch
  has been observed on a live runner, and that US1's success criterion is confirmed by the first
  `CI` run of the PR itself — not by this review.
- `release.yml` is verified **by inspection only**. The artifact round-trip through
  `upload-artifact@v6` → `download-artifact@v7` → `action-gh-release@v3` has not been exercised.
  T015 should stay visibly open until the first tag push after merge.

---

## Provenance note

This feature bypassed `/speckit-specify` and `/speckit-plan`, so `spec.md` and `plan.md` do not
exist and `check-prerequisites.sh --require-tasks` hard-fails on the missing plan. `tasks.md` is the
sole design artifact and was reviewed as the spec; its "Provenance" and "Verification status"
sections carried enough detail to review against. For a change with a code surface, that
substitution would not be workable.

**Severity mapping used**: the constitution's Quality Gates speak of Critical / Important / Minor;
this template uses 🔴 Blocker / 🟡 Warning / 🟢 Suggestion. They are read as the same three tiers.
That mapping is what makes an open 🟡 from the prior review a gate failure, and therefore — per the
review command's rule that *any* constitution violation is automatically a 🔴 — a blocker here.

---

**Next step**: fix B1 and B2 (both small, both in files already on this branch), then re-run
`/speckit-staff-review-run`.

---

*Generated by `/speckit.review` — Staff-level code review for spec-driven development.*
