# Code Review Report: Retire the Node.js 20 Deprecation Warning in CI (re-review)

**Reviewer**: AI Agent (Staff Engineer Perspective)
**Date**: 2026-08-11 14:46 PDT
**Feature**: [tasks.md](../tasks.md) — no `spec.md` or `plan.md` exists
**Branch**: `20260811-141602-ci-node24-action-bumps` @ `f5032b6`
**Prior reviews**: [review-20260811-143026.md](review-20260811-143026.md) (⚠️ conditions) →
[review-20260811-144021.md](review-20260811-144021.md) (❌ changes required)
**Verdict**: ⚠️ **APPROVED WITH CONDITIONS**

---

## Executive Summary

**Both blockers from the 14:40 review are fixed, and the headline is better than that: US1's
acceptance criterion has now been confirmed on a live runner for the first time.** PR #27 exists,
and CI run [31539361533](https://github.com/clintcparker/site-checker/actions/runs/31539361533) at
`bed16c1` completed with **both jobs `success` and zero annotations on each** — I queried
`check-runs/<id>/annotations` for the `rust` and `frontend` jobs directly and both returned `[]`.
That is exactly what T007 asks for, and it is the whole point of the feature. Every claim in the
two prior reviews was verified by inspection only; this one is not. **T007 is closeable with
evidence.**

**B1 is fixed.** `.github/dependabot.yml` now carries an `ignore:` block covering four actions —
`checkout`, `setup-node`, `upload-artifact`, `download-artifact` — each at
`update-types: ["version-update:semver-major"]` rather than at a version, so a later v8/v9 does not
reopen a settled question. Four, not the three the first review named: `setup-node@v7` exists and
is declined by `tasks.md`, and `checkout@v7` exists so the proposal would have been v7, not the v6
that review cited. I re-parsed the file and checked every key against Dependabot's accepted
`updates` schema — no unknown keys, `ignore` entries use only `dependency-name`/`update-types`,
and `groups` + an explicit `open-pull-requests-limit: 5` close S3 in the same edit.

**B2 is fixed.** R003 (the `pnpm/setup` successor), S2 (no `actionlint` in CI), W2 (prose naming
exact majors), and the cargo/npm security-update consequence are now recorded in `docs/ROADMAP.md`
— the constitution's designated home for deferred work. R005's stale line numbers are corrected in
`tasks.md` directly (`f5032b6`); the pins moved from 28/71/73/75 to 47/90/92/94 when T018's comment
block was inserted, and both sets are now given. **W1 is also fixed**: `dependabot.yml`'s comment
no longer claims `--locked`/`--frozen-lockfile` catch cargo/npm drift — it now says plainly that
they do not, and states the actual reason for the exclusion. The constitution's Quality Gate
("no Critical or Important findings open; Minor findings fixed or recorded in the roadmap") is
satisfied. All four code gates are green and unmoved: 62 Rust, 47 frontend, clippy clean,
`actionlint` exit 0, 20 `uses:` lines with zero `@v4`.

**The conditions are about the record, and one of them is new and worth reading.** Three unattended
speckit runs shared this single checkout while this review executed: a QA pass rewrote `ci.yml`
twice under me (14:38 and 14:42 — the second time between my `Read` and my `Edit`, which the tool
refused), and `/speckit-ship-run` committed the tree, pushed, and opened PR #27 **while the review
was still running** (C2). The consequence is concrete: **PR #27's body lists B1 and B2 as open
blockers, and both are now fixed** (C1). A reviewer opening that PR today reads a "Known issues"
section describing a verdict that no longer holds, against a head commit one older than the branch.

`release.yml` remains verified **by inspection only**. T015 needs a real tag push, T017 needs the
merge, T020 needs the post-merge run. That is unchanged and correctly documented.

---

## Review Findings

| ID | Severity | File | Line(s) | Category | Finding | Recommendation |
|----|----------|------|---------|----------|---------|----------------|
| C1 | 🟡 Warning | PR #27 body | — | Documentation Accuracy | The PR description was composed from [review-20260811-144021.md](review-20260811-144021.md) and reports **B1 and B2 as open blockers** — it even says "the file blocker **B2** below turns on". Both were fixed after the body was written: B1 in `accd2ee`'s successor content and B2 across `docs/ROADMAP.md` (untracked) and `f5032b6`. The body also pins every artifact link to `bed16c1`, which is now one commit behind `f5032b6`. Concrete failure: a reviewer opens #27, reads that the change has two unresolved blockers and a ❌ CHANGES REQUIRED verdict, and either bounces it or spends the time re-deriving that the blockers are gone. | Update #27's "Known issues" to point at **this** report as the current verdict, state that B1/B2 are closed, and note that T007 is now confirmed by CI run 31539361533. Re-pin the artifact links to `f5032b6` so `tasks.md`'s corrected line numbers resolve. |
| C2 | 🟡 Warning | *process* (primary checkout) | — | Architecture / Process | Three unattended runs operated on the same working tree concurrently: `/speckit-implement` (13:31), this `/speckit-staff-review-run` (14:36), and `/speckit-ship-run` (14:41), plus a QA pass. `ci.yml` was rewritten at 14:38 and again at ~14:42 — the latter landing between this review's `Read` and its `Edit`, which the tool correctly refused. Ship then committed, pushed, and opened the PR mid-review. So review #1 was rendered against a **moving target**, and the ship step's PR body froze a verdict that its own pipeline was still in the middle of producing. This compounds the already-recorded fact that the branch is in the primary checkout with no worktree isolation. Nothing was corrupted — the tree is clean and every gate is green — but the ordering guarantee the review→ship pipeline depends on did not hold. | Serialize the review and ship steps, or have ship re-read `reviews/` immediately before composing the body rather than at pipeline start. At minimum, when several unattended speckit runs share one checkout, ship should refuse to compose a PR body while a review process is still live. |
| C3 | 🟢 Suggestion | `specs/…/tasks.md` | 101 | Spec Compliance | **T007 is now satisfied and still shows unchecked.** CI run 31539361533 at `bed16c1` concluded `success` on both `rust` and `frontend`, and `check-runs/93937996056/annotations` and `check-runs/93937995856/annotations` both return `[]` — no Node 20 deprecation message on either. Deliberately not edited here: PR #27's body already enumerates T007 as incomplete, and changing `tasks.md` now would put the two records out of step with no mechanism to update the PR. | Check T007 off citing run 31539366957/31539361533, and correct the PR body in the same pass as C1. The pins are byte-identical between `bed16c1` and `f5032b6` (the latter is docs-only), so the result carries. |
| C4 | 🟢 Suggestion | `docs/ROADMAP.md` | — | Architecture | B2 was closed by recording findings in `docs/ROADMAP.md`, which is the constitution's named home for deferred work — and which `.gitignore:28` (`docs/*`) excludes. So "recorded in the roadmap rather than silently dropped" is satisfied in letter while remaining **invisible to every reviewer of this PR**. The gate cannot really distinguish that from dropping them. Already tracked as issue #21 and called out in #27's own preamble; recorded here because this feature is the first case where the gate itself depends on the invisible file. | Nothing in this PR. When #21 is addressed, prefer moving deferred findings to GitHub issues so the Quality Gate has a reviewable artifact behind it. |
| C5 | 🟢 Suggestion | `.github/dependabot.yml` | 48–63 | Maintainability | Two of the six bumped actions — `pnpm/action-setup` and `softprops/action-gh-release` — are deliberately **not** in the ignore list (neither has a major above its current pin today: no `pnpm/action-setup@v7`, no `action-gh-release@v4`, both verified). That is the right call, but it means the ignore list silently encodes "these two are free to move", and `ci.yml`'s comment names `pnpm/action-setup`'s major explicitly. The prose-drift risk is now narrowed to exactly those two rather than eliminated. | No action. Already recorded in `docs/ROADMAP.md`; noted so the narrowing is understood as deliberate rather than an oversight. |

**Categories**: Documentation Accuracy, Architecture, Process, Spec Compliance, Maintainability

---

## Blocker remediation — verified closed

| Finding | Fix | Verified how | Result |
|---|---|---|---|
| **B1** — no `ignore:`; Dependabot would re-propose declined majors, incl. `download-artifact@v8` (`digest-mismatch: error`) | `ignore:` block, 4 actions, major-level | Re-parsed `dependabot.yml`; asserted every `updates` key against the accepted schema; listed each ignore entry | ✅ `checkout`, `setup-node`, `upload-artifact`, `download-artifact`, all `version-update:semver-major`. No unknown keys |
| **B1 (cont.)** — prior review's list was incomplete | Widened 3 → 4 | `gh api .../git/ref/tags` for `checkout@v7`, `setup-node@v7`, `upload-artifact@v7`, `download-artifact@v8` | ✅ All exist, so all four could have been proposed. `pnpm/action-setup@v7` and `action-gh-release@v4` do **not** exist — correctly omitted |
| **B2** — Minor findings neither fixed nor recorded | R003/S2/W2 + cargo-npm consequence → `docs/ROADMAP.md`; R005 → `tasks.md` | Read both files after the edit | ✅ New "Deferred from the node24 action-bump review" section; `tasks.md` gives both old and new line numbers |
| **W1** — false claim that `--locked`/`--frozen-lockfile` catch cargo/npm drift | Comment rewritten | Read `HEAD:.github/dependabot.yml` | ✅ Now states they do *not* detect upstream drift, gives the real reason, and flags security-only updates as a separate decision |
| **S3** — ungrouped, invisible default PR limit | `groups` + explicit limit | Parsed config | ✅ `groups: {github-actions: {patterns: ["*"]}}`, `open-pull-requests-limit: 5` |
| **S1** — ragged comment wrap in `ci.yml` | Fixed by the concurrent QA pass | Read `ci.yml:10–27` | ✅ Re-flowed; the two exceptions are stated accurately (`setup-node@v5` and `pnpm/action-setup@v5` are both already node24 — re-verified) |

---

## Live verification — new in this review

Everything below was observed on GitHub, not inferred. This is the first review of this feature
able to say that.

| Claim | Verified how | Result |
|---|---|---|
| **US1: `CI` emits zero Node 20 annotations (T007)** | `gh api repos/…/check-runs/<job>/annotations` for both jobs of run 31539361533 | ✅ **`rust` → 0 annotations. `frontend` → 0 annotations.** Both jobs `success`. Event `pull_request`, head `bed16c1` |
| The run is the PR's, not a stale one | `gh api .../actions/runs/31539361533 --jq .head_sha,.event` | ✅ `bed16c1`, `pull_request` — the branch head at ship time |
| A second run is in flight for `f5032b6` | `gh run list --branch …` | ⏳ 31539366957 `in_progress`. Pins are identical (`f5032b6` is docs-only), so the annotation result carries |
| PR exists | `gh pr list --head …` | ✅ #27, OPEN |
| Branch is pushed and current | `git ls-remote --heads origin`, `git push` | ✅ `f5032b6` on origin |
| `cargo test --locked` | Run locally | ✅ 62 passed |
| `pnpm test` | Run locally | ✅ 47 passed (6 files) |
| `cargo clippy -- -D warnings` | Run locally | ✅ Clean |
| `actionlint` on all workflows | Run locally | ✅ Exit 0, no diagnostics |
| Pin inventory unchanged by the fixes | `grep -rn "uses:" .github/workflows/` | ✅ 20 `uses:` lines, zero `@v4` |
| `dependabot.yml` schema-valid | `yaml.safe_load` + key assertion against the accepted `updates` key set | ✅ No unknown keys; all `ignore` entries well-formed |

---

## Spec Coverage Matrix

| Story / Task | Requirement | Status | Evidence |
|---|---|---|---|
| **US1** | `CI` produces zero Node 20 annotations on `rust` and `frontend` | ✅ **VERIFIED ON A LIVE RUNNER** | T003–T006 correct; **T007 satisfied** — run 31539361533, both jobs `success`, both annotation lists `[]`. Unchecked in `tasks.md` only (C3) |
| T003–T005 | `checkout@v5`, `pnpm/action-setup@v6`, `setup-node@v6`; inputs preserved | ✅ | `ci.yml:47, 90, 92, 94`; `node-version: 22` / `cache: pnpm` intact |
| T006 | YAML valid, no stray `@v4` | ✅ | Parses; `actionlint` clean; zero `@v4` |
| T007 | Read annotations off a real `CI` run | ✅ **Closeable** | See above — the criterion is met |
| **US2** | `Release` clean across all five jobs | ⚠️ Implemented, unverified | T008–T014 correct. **T015 open** — needs a real tag push |
| T008–T013 | 11 pins bumped, all inputs preserved | ✅ | `release.yml:93, 99, 103, 105, 139, 145, 150, 152, 218, 231, 238, 264` |
| T012 | `download-artifact@v7` still extracts to named subdirs | ✅ | v7 `action.yml`: `merge-multiple` default `'false'`. Globs resolve unchanged |
| T014 | YAML valid, `rust-cache@v2` untouched | ✅ | Verified node24, unchanged |
| T015 | End-to-end release verification | ⏸️ Blocked on a release decision | Correctly deferred |
| **US3** | Future deprecations arrive as PRs | ✅ **Now correct** | T016 complete and, after B1, no longer re-proposes declined majors. **T017 open** |
| T016 | `.github/dependabot.yml` | ✅ | Schema-valid; `ignore` ×4, `groups`, explicit limit |
| T017 | Confirm Dependabot accepted the config | ⏸️ Blocked on merge | Parses only after it lands on the default branch |
| **Polish** | | | |
| T018 | Pinning convention in `ci.yml` | ✅ | Comment accurate; exceptions verified against `setup-node@v5`/`pnpm@v5` |
| T019 | Runner floor in `docs/how-to/release.md` | ✅ | File confirmed tracked; drift risk recorded in ROADMAP |
| T020 | Re-read annotations on `main` post-merge | ⏸️ Blocked on merge | |

**Spec coverage**: **17 of 20 tasks complete (85%)** — up from 16, because T007 is now met.
The three still open (T015, T017, T020) each require an event on GitHub that has not happened:
a tag push, the merge, and the post-merge run. **Zero scope creep.**

---

## Test Coverage Assessment

| Area | Status | Note |
|---|---|---|
| `cargo test --locked` | ✅ 62 passed | Unaffected — zero source files changed |
| `pnpm test` | ✅ 47 passed | Unaffected |
| `cargo clippy -- -D warnings` | ✅ Clean | Unaffected |
| `actionlint` | ✅ Exit 0 | Still not in CI — recorded in ROADMAP (S2) |
| `dependabot.yml` schema | ✅ Validated by hand | Checked by nothing automated; `actionlint` does not cover it |
| **Annotation-free CI run** | ✅ **Observed** | Run 31539361533 — the acceptance criterion, no longer a substitute for one |
| Release artifact round-trip | ⏸️ T015 | `upload@v6 → download@v7 → gh-release@v3` still unexercised end to end |

The substitution that both prior reviews relied on — inspection against primary sources in place of
an untestable surface — held: the live run agrees with what inspection predicted.

---

## Metrics Summary

| Metric | Value |
|---|---|
| Files reviewed | 4 changed + 4 read for scope (`verify-install-channels.yml`, `ROADMAP.md`, 2 prior reviews) |
| 🔴 Blockers | **0** (2 found and fixed since the last review) |
| 🟡 Warnings | 2 (both about the record, neither about the pins) |
| 🟢 Suggestions | 3 |
| Spec coverage | 17 / 20 tasks (85%); 3 open, all externally blocked |
| Prior-review findings now closed | **8 of 8 actionable** — R001, R002/B1, R005, W1, S1, S3, B2, and T007's blocker |
| Constitution gates | **5 / 5** — 4 code gates green, and the findings-open process gate now satisfied |
| Live-runner verification | ✅ First for this feature |

---

## Recommended Actions

### Must Fix (Blockers)

None. Both blockers from the previous review are closed and independently re-verified.

### Should Fix (Warnings)

1. **C1 — update PR #27's body.** It currently presents B1 and B2 as open and the verdict as
   ❌ CHANGES REQUIRED. Point "Known issues" at this report, state that both blockers are closed,
   record that T007 is confirmed by CI run 31539361533, and re-pin artifact links to `f5032b6`.
2. **C2 — do not run review and ship concurrently against one checkout.** Ship composed the PR body
   from an interim review verdict while the review was still executing. Serializing the steps, or
   having ship re-read `reviews/` just before composing, removes the whole class of problem.

### Nice to Fix (Suggestions)

3. **C3** — check T007 off in `tasks.md` with the run ID, in the same pass as C1.
4. **C4** — when #21 is addressed, move deferred findings to issues so the Quality Gate has a
   visible artifact.
5. **C5** — no action; recorded so the two un-ignored actions are understood as deliberate.

### Conditions on the verdict

- **`release.yml` is still verified by inspection only.** The pins are correct at their tags and
  every input still exists, but `upload-artifact@v6` → `download-artifact@v7` →
  `action-gh-release@v3` has not been exercised end to end. **T015 should stay visibly open until
  the first tag push after merge** — that is the real test, and this review is not it.
- **T017 and T020 close only after merge.** Dependabot parses `.github/dependabot.yml` only on the
  default branch, and T020 is post-merge by definition.
- US1, by contrast, is **no longer** subject to this condition. It was observed.

---

## Provenance note

This feature bypassed `/speckit-specify` and `/speckit-plan`; `check-prerequisites.sh
--require-tasks` hard-fails on the missing `plan.md`, so `tasks.md` was reviewed as the spec.
Severity mapping is unchanged from the previous report: the constitution's Critical / Important /
Minor are read as 🔴 Blocker / 🟡 Warning / 🟢 Suggestion.

**This is the second and final review of this feature**, per the run's instructions. No third pass
will be made; the remaining findings above are left in place for the ship step to report.

---

*Generated by `/speckit.review` — Staff-level code review for spec-driven development.*
