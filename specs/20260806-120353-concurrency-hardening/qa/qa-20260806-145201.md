# QA Report: Concurrency & Robustness Hardening

**QA Mode**: CLI QA
**Date**: 2026-08-06 14:52:01 PDT
**Feature**: [spec.md](../spec.md)
**Environment**: macOS 26.6 (darwin 25.6.0, arm64) · rustc 1.97.1 · cargo 1.97.1 · Node v22.16.0 · pnpm 10.30.3 · vitest 4.1.10 · Tauri v2 desktop app, no application URL
**Verdict**: ⚠️ **PARTIAL PASS**

---

## QA Summary

Thirty-one scenarios were derived from the spec's thirteen acceptance scenarios, eighteen
functional requirements, and eight success criteria, and run in CLI QA mode against the merged
implementation in the primary checkout (`src-tauri/src/{lock,store,commands,engine,lib}.rs`).
Browser QA was not applicable and browser automation is not configured in this project: Site
Checker is a Tauri desktop app whose command layer is reachable only through a native window, and
`package.json` carries no Playwright/Puppeteer/Cypress/Selenium dependency. Twenty-eight scenarios
passed, none failed, one is partial, and two were skipped.

Every behaviour the feature exists to fix is proven by a test that actually runs. Fault recovery is
exercised against a genuinely poisoned mutex; the refused-add distinction is asserted on the
`AddError` variant and on the reloaded file's bytes; the overlapping-edit fix is proven against a
negative control that drives the *identical* barrier choreography through the old two-lock shape and
asserts the bug is present. All three merge gates are green from a clean run of this checkout —
Rust 55/0/0, frontend 30/0 unmodified, clippy clean at both `-D warnings` and `--all-targets`.
`grep -rn 'lock()\.unwrap()' src-tauri/src/` returns nothing, and all nine remaining lock sites were
enumerated: six route through `SharedStore::lock`, three through `lock::recover(..).0` each carrying
the reason the flag is discarded.

The one partial and both skips are the same pre-declared gap, and it is the open condition R002 from
[review-20260806-144450.md](../reviews/review-20260806-144450.md): the shell layer (`commands.rs`)
has no automated coverage by design (Constitution IV, research R7), and the manual click-through
that was supposed to stand in for it — add, edit, and delete against a running window — cannot be
driven from a non-interactive CLI session. To narrow that gap with machine evidence rather than
leave it bare, this run added a wire-contract cross-check that parses the seven `#[tauri::command]`
signatures out of `commands.rs` and matches them argument-for-argument against the `invoke()` calls
in `src/api.ts`. It passes for all seven, including the two commands (`update_site`, `delete_site`)
that changed signature in this feature and were the specific risk R002 named. What remains
unverified is narrower than R002 described, but it is not zero: no user action has been performed
against a real window since the change.

---

## Test Results

| ID | User Story | Scenario | Mode | Result | Evidence |
|----|-----------|----------|------|--------|----------|
| TC-001 | US1 (AS1–AS3) | After a fault while the site list was held, the next lock returns a usable guard and reports the recovery | CLI | ✅ | [us1-lock.txt](responses/us1-lock.txt) — `recover_returns_a_usable_guard_after_a_fault_and_reports_it` |
| TC-002 | US1 (AS1–AS3) | Every site-list access routes through the recovering lock; none can be written un-recovered | CLI | ✅ | [lock-discipline.txt](responses/lock-discipline.txt) — 6/6 sites via `SharedStore::lock` (`lib.rs:37`, `commands.rs:25/69/109/131`, `engine.rs:125`); `inner` private with no accessor; `no_module_in_this_crate_takes_a_lock_without_recovering` green |
| TC-003 | US1 (AS4) | Check-registry access survives a prior fault, and does so silently | CLI | ✅ | `engine.rs:62`, `engine.rs:78` — `lock::recover(&self.inner.tasks).0`, each with the FR-005 reason; no `emit` on either path |
| TC-004 | US1 | Startup-warning access survives a prior fault, silently | CLI | ✅ | `commands.rs:33` — `lock::recover(&state.warning).0.take()` with the FR-003 reason |
| TC-005 | US1 (AS5) | A recovered site-list lock raises exactly one banner on the existing `store-warning` channel | CLI | ⚠️ | `lock.rs:76–85` read by inspection: `recovered` gates one `self.warn(...)`; message names the saved list and the most recent change. One-shot behaviour proven at runtime by `a_second_recovery_reports_clean_so_no_degraded_mode_accumulates`. The `emit` itself has no runtime test — declined in advance (research R7) |
| TC-006 | US1 (edge) | A fault partway through a change: the half-applied state survives recovery | CLI | ✅ | [us1-lock.txt](responses/us1-lock.txt) — `recovery_preserves_what_the_faulting_operation_had_already_applied` |
| TC-007 | US1 (edge) | Recovery more than once in a session accumulates no permanent degraded mode | CLI | ✅ | [us1-lock.txt](responses/us1-lock.txt) — `a_second_recovery_reports_clean_so_no_degraded_mode_accumulates` |
| TC-008 | US1 | With no fault present, behaviour is indistinguishable from before | CLI | ✅ | `an_unpoisoned_lock_is_indistinguishable_from_todays_behaviour`; `git diff --stat 2ec8788..HEAD -- src/` empty; 30 frontend tests pass unmodified |
| TC-009 | US1 (edge) | A fault during startup, before the window shows: the startup list load recovers too | CLI | ✅ | `lib.rs:37` — `store.lock().list()` goes through `SharedStore::lock` |
| TC-010 | US2 (AS1) | A duplicate id is refused as `DuplicateId`, not reported as a write failure | CLI | ✅ | [cargo-test.txt](responses/cargo-test.txt) — `add_rejects_a_duplicate_id` (asserts the variant, not `.is_err()`) |
| TC-011 | US2 (AS2) | A refusal leaves the in-memory list and the saved file exactly as they were | CLI | ✅ | [cargo-test.txt](responses/cargo-test.txt) — `a_refused_add_changes_neither_the_list_nor_the_file`: byte-identical file, no staging artifact, original entry intact |
| TC-012 | US2 (AS1) | A refusal returns above `engine.start` — no timer starts and no row appears | CLI | ✅ | `commands.rs:72–78` `return Err(...)` precedes `state.engine.start(...)` at `commands.rs:83`; `src/form.ts:96–99` never reaches `hooks.onSaved` when `addSite` rejects |
| TC-013 | US2 (AS1) | The refusal message says the site was not added and never says "saved" | CLI | ✅ | `commands.rs:74–77` — *"That site was not added — the list already has an entry with the same identity. Nothing was changed."* `grep -i saved src-tauri/src/commands.rs` → 2 hits, both comments describing the *write-failure* path (`:59`, `:63`) |
| TC-014 | US2 (AS3) | A genuine write failure keeps today's behaviour: row appears, checks begin, banner fires | CLI | ✅ | `a_failed_save_leaves_the_previous_file_intact` asserts `AddError::Write`; `commands.rs:79` warns and falls through to `engine.start` + `Ok(site)` |
| TC-015 | US2 (AS4) | A valid, non-duplicate add succeeds with no warning | CLI | ✅ | `add_still_accepts_a_distinct_id`, `add_then_reload_round_trips`; `commands.rs:80` `Ok(()) => {}` raises no banner |
| TC-016 | US3 (AS1) | Two overlapping edits: the later decides from the earlier's result, not from a replaced picture | CLI | ✅ | [us3-contention.txt](responses/us3-contention.txt) — `two_overlapping_edits_decide_from_the_current_entry` |
| TC-017 | US3 (AS1) | The contention test is non-vacuous: the same choreography through the old shape loses the edit | CLI | ✅ | [us3-contention.txt](responses/us3-contention.txt) — `the_old_two_lock_shape_would_lose_the_earlier_edit`; both share one `run_overlapping_edits` driver and one `Barrier` sequence |
| TC-018 | US3 (AS2) | An unchanged address preserves the learned request method | CLI | ✅ | [cargo-test.txt](responses/cargo-test.txt) — `an_unchanged_url_carries_the_learned_method_forward` |
| TC-019 | US3 (AS3) | A changed address discards the learned method so it is re-learned | CLI | ✅ | [cargo-test.txt](responses/cargo-test.txt) — `a_changed_url_drops_the_learned_method_so_it_is_relearned` |
| TC-020 | US3 (AS4) | Editing a site that is no longer in the list: user is told, nothing is written | CLI | ✅ | `replacing_an_unknown_id_writes_nothing_at_all` (no save, no staging artifact); `commands.rs:118–119` returns *"That site no longer exists"* |
| TC-021 | US3 | List order is preserved across an edit | CLI | ✅ | [cargo-test.txt](responses/cargo-test.txt) — `replace_preserves_list_order` |
| TC-022 | US3 | A failed save leaves the edit standing in memory so the caller keeps the row and warns | CLI | ✅ | [cargo-test.txt](responses/cargo-test.txt) — `a_failed_save_still_leaves_the_edit_standing_in_memory` |
| TC-023 | All | Backend suite green with nothing skipped or ignored | CLI | ✅ | [cargo-test.txt](responses/cargo-test.txt) — 55 passed / 0 failed / 0 ignored |
| TC-024 | All | Frontend suite exactly 30 and unmodified | CLI | ✅ | [pnpm-test.txt](responses/pnpm-test.txt) — 30 passed (4 files); `git diff --stat 2ec8788..HEAD -- src/` empty |
| TC-025 | All | Linter clean at its strictest setting | CLI | ✅ | [clippy.txt](responses/clippy.txt) — `cargo clippy --all-targets -- -D warnings` exit 0; plain `-D warnings` also clean |
| TC-026 | All | No user-facing capability added: no file under `src/` in the implementation diff | CLI | ✅ | `git diff --name-only 2ec8788..8feb989` → 5 files under `src-tauri/src/` plus `tasks.md`; `-- src index.html src-tauri/tauri.conf.json` empty |
| TC-027 | All | The symlink-at-the-path behaviour from `003-durability` is untouched | CLI | ✅ | Per-function md5 of `stage`, `save`, `staging_path`, `load` identical at `2ec8788` and `HEAD` |
| TC-028 | All | The saved list's shape, field names, and location are unchanged | CLI | ✅ | Live `sites.json` (5 entries) parses with keys `id`/`url`/`label`/`interval_secs`/`method_override` — snake_case, matching `Site`; file SHA-256 byte-identical before and after this QA run |
| TC-029 | All | Wire contract: the seven commands match `src/api.ts` argument-for-argument after the signature changes | CLI | ✅ | [wire-contract.txt](responses/wire-contract.txt) — 7/7 OK, including `update_site` and `delete_site` |
| TC-030 | US1/US2/US3 | GUI click-through: add, edit, and delete a site against a running window (FR-007, quickstart §5) | Browser | 🔵 | Not runnable in CLI QA mode — Tauri native window, no GUI automation available. Open condition R002 |
| TC-031 | US1 | App launches, the list loads, and checks start under the `SharedStore` rewrite | Browser | 🔵 | Not re-run here. Performed once at implement time (`tasks.md` Notes: launched, loaded, no panic, `sites.json` byte-identical afterwards); not reproducible from this session |

**Legend**: ✅ Pass | ❌ Fail | ⚠️ Partial | 🔵 Skipped

---

## Acceptance Criteria Coverage

| Criterion | Test ID(s) | Status | Notes |
|-----------|-----------|--------|-------|
| US1-AS1: after a prior fault, an add still completes, saves, and starts checking | TC-001, TC-002, TC-015, TC-030 | ✅ Met | Recovery mechanism and the routing of `add_site`'s store access are both proven; the click-through itself is TC-030 |
| US1-AS2: after the same fault, an edit or delete is applied and saved | TC-001, TC-002, TC-016, TC-030 | ✅ Met | `commands.rs:109` and `commands.rs:131` both take the list through `SharedStore::lock` |
| US1-AS3: after the same fault, listing returns the list rather than hanging the app | TC-001, TC-002 | ✅ Met | `commands.rs:25` via `SharedStore::lock`; `recover` never panics and never errors |
| US1-AS4: after a fault in the check registry, checks still start, restart, and stop | TC-003 | ✅ Met | Both `tasks` locks recover; `reschedule` delegates to `start` |
| US1-AS5: a recovered site list warns the user through the existing banner | TC-005, TC-007 | ⚠️ Partially Met | Wiring, gating, and message read by inspection; one-shot proven at runtime. The `emit` has no runtime test — declared in advance, not discovered here |
| US2-AS1: a refused add adds no row, starts no checks, and says the site was not added | TC-010, TC-012, TC-013 | ✅ Met | Variant asserted at runtime; ordering and wording verified in source; frontend never calls `onSaved` on a rejection |
| US2-AS2: after a refusal, the saved list is unchanged on relaunch | TC-011 | ✅ Met | Asserted on the reloaded file's bytes, which is what proves the refusal preceded any write |
| US2-AS3: a genuine write failure keeps today's behaviour | TC-014 | ✅ Met | |
| US2-AS4: a valid, non-duplicate add shows the row, starts checks, and warns about nothing | TC-015 | ✅ Met | |
| US3-AS1: overlapping edits — the later applies on top of the earlier's result | TC-016, TC-017 | ✅ Met | The negative control is what makes this claim non-vacuous, permanently |
| US3-AS2: an unchanged address preserves the learned request method | TC-018 | ✅ Met | |
| US3-AS3: a changed address discards it for re-learning | TC-019 | ✅ Met | |
| US3-AS4: editing a site that no longer exists reports that and writes nothing | TC-020 | ✅ Met | Strictly better than the pre-feature behaviour, which returned `Ok` and resurrected a timer |
| SC-001: after one fault, 100% of ordinary actions complete; zero require a relaunch | TC-001–TC-004, TC-009 | ✅ Met | All nine lock sites recover; none can panic on a prior poisoning |
| SC-002: every shared-state access recovers; none can disable the app | TC-002, TC-003, TC-004 | ✅ Met | Nine sites, not the spec's ten — US3's collapse of `update_site`'s two locks into one. Known reconciliation, recorded in `plan.md` (research R9) |
| SC-003: every row shown corresponds to a stored entry; a refusal produces zero rows | TC-011, TC-012 | ✅ Met | |
| SC-004: overlapping edits resolve correctly; the method is discarded only on a real address change | TC-016, TC-018, TC-019 | ✅ Met | |
| SC-005: every message states what actually happened | TC-013, TC-020 | ✅ Met | |
| SC-006: each behaviour covered by a new test verified red first | TC-010, TC-016, TC-017, TC-001 | ✅ Met | Red states recorded in `tasks.md`; the negative control keeps US3's claim checkable after the fact |
| SC-007: full merge bar, nothing disabled or skipped | TC-023, TC-024, TC-025 | ✅ Met | 55/0/0, 30/0, clippy clean at `--all-targets` |
| SC-008: with no fault, behaviour is indistinguishable; every existing test passes unmodified | TC-008, TC-024, TC-026 | ✅ Met | Frontend diff against the pre-implementation commit is empty |
| FR-015 / FR-016 / FR-018: saved-list shape, no new capability, symlink behaviour untouched | TC-026, TC-027, TC-028 | ✅ Met | Save-path functions byte-identical by per-function hash |

**Coverage**: 13/13 spec acceptance scenarios exercised (100%); 12 fully met, 1 partially met (92% fully met). All 8 success criteria and the 3 scope requirements validated.

---

## Test Suite Results

| Test Suite | Total | Passed | Failed | Skipped | Coverage |
|-----------|-------|--------|--------|---------|----------|
| `cargo test` (site-checker-lib) | 55 | 55 | 0 | 0 (0 ignored) | Not measured — no coverage tool configured |
| `cargo test` (main binary + doc-tests) | 0 | 0 | 0 | 0 | n/a |
| `pnpm test` (vitest, 4 files) | 30 | 30 | 0 | 0 | Not measured — no coverage tool configured |
| `cargo clippy --all-targets -- -D warnings` | — | clean | 0 | 0 | n/a |
| `pnpm build` (`tsc && vite build`) | — | clean | 0 | 0 | Type-checks the frontend against `api.ts` |

No pre-existing failures. The backend suite went 42 → 55 in this feature (US1 +5, US2 +1 new plus 2
tightened, US3 +7); the frontend suite is unchanged at 30 and its files are byte-identical to the
pre-implementation commit.

---

## Failure Details

No scenario failed. The one partial and two skips are recorded below in the same form, because they
are the report's only open items.

### TC-005: A recovered site-list lock raises exactly one banner

**Status**: ⚠️ PARTIAL
**Steps to Reproduce**:
1. Poison the site-list mutex from a thread that panics under the guard.
2. Take the next `SharedStore::lock()`.
3. Observe whether one `store-warning` event is emitted with the FR-004 message.

**Expected**: exactly one banner, on the existing `store-warning` channel, saying the saved list may
not reflect the most recent change.
**Actual**: steps 1 and 2 are proven at runtime (`recover_returns_a_usable_guard_after_a_fault_and_reports_it`,
`a_second_recovery_reports_clean_so_no_degraded_mode_accumulates`). Step 3 was verified by reading
`lock.rs:76–85`: the `recovered` flag gates a single `self.warn(...)` whose message matches FR-004,
and `warn` is the crate's only `emit` of `store-warning`. There is no runtime assertion on the emit
because it needs an `AppHandle`; the `tauri` `test` feature and a mock-app harness were declined at
research time (R7) rather than added for three lines of `emit`.
**Evidence**: `src-tauri/src/lock.rs:76–108`; [us1-lock.txt](responses/us1-lock.txt)
**Severity**: Low — declared in advance in `plan.md` and `tasks.md`, not discovered here. The event
name and `{ message }` payload are unchanged, so `src/main.ts:109`'s `onStoreWarning(showBanner)`
needs nothing new.

### TC-030: GUI click-through — add, edit, delete against a running window

**Status**: 🔵 SKIPPED
**Steps to Reproduce**:
1. `pnpm tauri dev`.
2. Add a site, edit it, delete it.
3. Confirm each behaves exactly as before the feature (FR-007).

**Expected**: all three unchanged from pre-feature behaviour.
**Actual**: not executed. This run was CLI QA mode in a non-interactive session; Site Checker's
command layer is reachable only through a native Tauri window and the project has no browser or GUI
automation configured. This is open condition R002 from the staff review, and tasks T025 and T040
remain `[ ]` for the same reason.
**Evidence**: [wire-contract.txt](responses/wire-contract.txt) narrows but does not close the gap —
all seven command signatures match `src/api.ts` argument-for-argument, including the two commands
(`update_site`, `delete_site`) whose signatures changed in this feature, which was the specific risk
R002 named.
**Severity**: Medium — it is the FR-007 "nothing else changed" check, not a check of any new
behaviour, and every store-layer path underneath it is covered by a passing test. It should be run
by hand before merge or recorded in the ship notes as an accepted residual.

### TC-031: App launches, the list loads, checks start

**Status**: 🔵 SKIPPED
**Steps to Reproduce**:
1. `pnpm tauri dev`.
2. Confirm the window opens, the site list renders, and status updates begin.

**Expected**: the app launches and the `SharedStore` rewrite's startup path works end to end.
**Actual**: not re-run in this session, for the same reason as TC-030. It was performed once at
implement time and recorded in `tasks.md`: the app compiled, launched, loaded the list, ran with no
panic, and left `sites.json` byte-identical afterwards with no staging artifact beside it.
**Evidence**: `tasks.md` → "Not done, and not claimable"
**Severity**: Low — a prior successful run exists for the same code; only the re-run is missing.

---

## Environment Info

| Property | Value |
|----------|-------|
| Operating System | macOS 26.6 (Darwin 25.6.0, arm64) |
| Browser | n/a — Tauri native window; no browser automation configured in this project |
| Runtime | rustc 1.97.1 / cargo 1.97.1 · Node v22.16.0 · pnpm 10.30.3 |
| Application URL | n/a — desktop app |
| Test Runner | `cargo test` (backend) · `vitest` 4.1.10 (frontend) · `cargo clippy` |
| Checkout | `/Users/clint/src/clintcparker/site-checker` on `20260806-143500-concurrency-hardening-baseline-shots` @ `a99ccb8` (implementation merged to `main` at `3c8f9a3`) |
| Implementation range | `2ec8788..8feb989` |
| Data safety | Live `sites.json` copied to `/tmp/qa-sites-safety-20260806.json` before the run; SHA-256 verified identical afterwards — this QA run touched no user data |

---

## Metrics Summary

| Metric | Value |
|--------|-------|
| Total scenarios | 31 |
| ✅ Passed | 28 |
| ❌ Failed | 0 |
| ⚠️ Partial | 1 |
| 🔵 Skipped | 2 |
| Pass rate | 90.3% (100% of executed scenarios; 0 failures) |
| Acceptance criteria coverage | 100% exercised (13/13); 92% fully met (12/13) |
| Backend tests | 55 passed / 0 failed / 0 ignored |
| Frontend tests | 30 passed / 0 failed (unmodified) |
| Clippy | clean at `--all-targets -- -D warnings` |
| Unrecovered lock sites | 0 of 9 |

---

*Generated by `/speckit.qa` — Systematic QA testing for spec-driven development.*
