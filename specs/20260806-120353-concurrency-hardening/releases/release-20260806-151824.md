# Release Record — Concurrency Hardening Evidence

**Timestamp**: 2026-08-06 15:18:24 PDT
**Feature**: [Concurrency & Robustness Hardening](../spec.md)
**Run mode**: `/speckit.ship` — **unattended**. Every confirmation the command normally prompts for
(readiness summary, rebase/merge, push, CHANGELOG prepend, PR creation) was treated as answered YES.

---

## Pull Request

| | |
|---|---|
| **PR** | [#5 — docs: concurrency hardening evidence — reconstructed baseline screenshots, staff review, and QA report](https://github.com/clintcparker/site-checker/pull/5) |
| **Branch** | `20260806-143500-concurrency-hardening-baseline-shots` → `main` |
| **Description** | [pr-description-20260806-151824.md](pr-description-20260806-151824.md) |
| **Repository** | `clintcparker/site-checker` (**PRIVATE** — screenshots embedded as `github.com/.../blob/<sha>/...?raw=true`, since `raw.githubusercontent.com` does not render for reviewers on private repos) |

## Commit range

```
1de1eb015524ef8d26583b269d45e1d9ed482516..c22d66151a38811f1a9ceffce453a53b6f9e6899
```

3 commits · 21 files changed · +756 / −36 · all under `specs/20260806-120353-concurrency-hardening/`

| SHA | Subject |
|---|---|
| `a99ccb8` | docs: before screenshots for 20260806-120353-concurrency-hardening |
| `392c91a` | docs: after screenshots for 20260806-120353-concurrency-hardening |
| `c22d661` | docs: staff review and QA reports for concurrency hardening |

`c22d661` was created by this ship run, per the unattended instruction to commit rather than prompt:
the `reviews/` and `qa/` directories were untracked at pre-flight.

**Screenshot commits verified as ancestors of the pushed head** (`git merge-base --is-ancestor`), and
all twelve PNGs confirmed present in `origin/20260806-143500-concurrency-hardening-baseline-shots`
before any image URL was written into the PR body.

## What this ships

Verification evidence only — **zero source changes**. The feature's implementation (`f086cfa`,
`7ea3e66`, `8feb989`) already merged to `main` via [#4](https://github.com/clintcparker/site-checker/pull/4).
This branch adds:

- **12 screenshots** — a reconstructed `before/` set captured from `2ec8788` in a throwaway detached
  worktree (not `main`'s tip, which *is* the post-change state), plus a re-run `after/` set replacing
  the six committed in `74cb8db`.
- **[reviews/review-20260806-144450.md](../reviews/review-20260806-144450.md)** — staff review.
- **[qa/qa-20260806-145201.md](../qa/qa-20260806-145201.md)** plus seven raw response captures.
- A rewritten **[screenshots/manifest.json](../screenshots/manifest.json)** recording capture method,
  environment, per-pair pixel diffs, and the run's own caveats.

## Readiness check results

| Check | Result |
|---|---|
| Tasks | ⚡️ **41/43 complete** — T025 and T040 both `[~] Partially done` (GUI click-through not run) |
| Review | ⚡️ **APPROVED WITH CONDITIONS** — 0 blockers, 2 warnings, 2 suggestions |
| QA | ⚡️ **PARTIAL PASS** — 31 scenarios: 28 pass, 0 fail, 1 partial, 2 skipped |
| Working tree | ✅ Clean after `c22d661` |
| Branch sync | ✅ 0 behind / 3 ahead of `origin/main` — **no rebase needed**, merge-base was `origin/main`'s tip |
| Push | ✅ `git push -u origin` — new branch, no force |
| CI | ⚠️ **None configured** — no `.github/workflows/`, `gh run list` returns no runs |

**Overall: SHIPPED with notes.**

## Merge gates

Re-run by hand from this checkout, twice and independently (once by the review, once by QA), both
reporting identical numbers:

| Gate | Result |
|---|---|
| `cargo test` | 55 passed / 0 failed / 0 ignored (was 42 pre-feature) |
| `pnpm test` (vitest, 4 files) | 30 passed / 0 failed, files byte-identical to pre-implementation |
| `cargo clippy --all-targets -- -D warnings` | clean |
| `pnpm build` (`tsc && vite build`) | clean |

SC-007 (full merge bar, nothing disabled or skipped) holds.

## Surviving findings, carried into the PR under "Known issues"

| ID | Severity | Disposition |
|---|---|---|
| R001 | 🟡 Warning | **Open.** Lock guard `include_str!` module list is hand-maintained and its needle is a single literal spelling. Recommended: runtime `read_dir` + widened needle. |
| R002 | 🟡 Warning | **Recorded as an accepted residual**, satisfying review condition 1. Narrowed by the QA wire-contract check (7/7 signatures match `api.ts`, including `update_site`/`delete_site`) but not closed: no user action has been performed against a real window since the change. |
| R003 | 🟢 Suggestion | **Open.** Document the deliberate emit-under-guard exception in `SharedStore::lock`'s doc comment. |
| R004 | 🟢 Suggestion | **Open.** Pre-existing: `Store::load` accepts duplicate ids. Recommended: record in `docs/ROADMAP.md` §3. |
| TC-005 | ⚠️ Partial | The `store-warning` emit has no runtime assertion (needs an `AppHandle`; mock harness declined at research R7). Verified by inspection. |
| TC-030 | 🔵 Skipped | GUI click-through — same gap as R002. Severity Medium. |
| TC-031 | 🔵 Skipped | Launch/load/check smoke — performed at implement time and three more times during the screenshot pass, but not re-run in the QA session. Severity Low. |

**Deviation from review condition 2**: R001 and R004 were to be *fixed or appended to
`docs/ROADMAP.md`*. Neither was done. They are carried in the PR description instead, which records
them but does not satisfy the condition as written. A follow-up should append both to the roadmap.

## Changelog

**Not modified, deliberately.** `CHANGELOG.md` already carries the "Concurrency & robustness
hardening — 2026-08-06" entry, merged to `main` in `9cd7f33`. This branch contains no source change,
so prepending a second entry for the same feature would duplicate it. The unattended instruction
pre-answered the prepend prompt YES, but that prompt presupposes a new entry is warranted; it is not.
Recorded here rather than skipped silently.

## Linked artifacts

- [spec.md](../spec.md) · [plan.md](../plan.md) · [tasks.md](../tasks.md) · [research.md](../research.md) · [data-model.md](../data-model.md) · [quickstart.md](../quickstart.md)
- [contracts/](../contracts) — command surface, lock recovery, store mutation API
- [checklists/requirements.md](../checklists/requirements.md)
- [reviews/review-20260806-144450.md](../reviews/review-20260806-144450.md)
- [qa/qa-20260806-145201.md](../qa/qa-20260806-145201.md)
- [screenshots/manifest.json](../screenshots/manifest.json)
- Prior ship of this feature: [release-20260806-132343.md](release-20260806-132343.md) → [PR #4](https://github.com/clintcparker/site-checker/pull/4)

---

*Generated by `/speckit.ship` from spec-driven development artifacts.*
