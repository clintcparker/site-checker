# Release record — Concurrency & Robustness Hardening

**Shipped**: 2026-08-06 13:23 PDT · **Run mode**: unattended (`/speckit-ship-run`, all confirmations pre-answered yes)

## Pull request

- **URL**: https://github.com/clintcparker/site-checker/pull/4
- **Title**: `feat: concurrency & robustness hardening — close the three latent correctness holes in roadmap §1`
- **Branch**: `20260806-120325-section-1-docs` → `main`
- **Repository**: `clintcparker/site-checker` (private)
- **Description**: [`pr-description-20260806-132343.md`](./pr-description-20260806-132343.md)

## Commit range

`01303ccf61e9d50ca3d2476ebff183bcd2cb55fb..9cd7f33062cd79e21536f344183e92270c743d56`

9 commits · 30 files changed · 3122 insertions, 80 deletions

| SHA | Message |
|---|---|
| `9cd7f33` | docs: changelog entry for concurrency & robustness hardening |
| `74cb8db` | docs: after screenshots for 20260806-120353-concurrency-hardening |
| `8cbd6f2` | chore(companion): record implementing, not implemented |
| `bdd891f` | docs: close out concurrency hardening — scope, merge bar, roadmap |
| `8feb989` | feat(store): make an edit read, decide, and write as one step |
| `7ea3e66` | feat(store): tell a refused add apart from a failed write |
| `f086cfa` | feat(lock): recover from a poisoned lock instead of cascading panics |
| `2ec8788` | docs: spec, plan, and tasks for concurrency & robustness hardening |
| `818fab9` | docs: before screenshots for 20260806-120353-concurrency-hardening |

Note: `818fab9` recorded the before-pass *manifest only* — it committed no images, because screen capture was blocked in that session. See Screenshots below.

## Readiness check results

| Check | Verdict | Detail |
|---|---|---|
| Tasks | ⚡️ **41/43** | T025 and T040 unchecked. Both are manual click-through verification; both partially done with the by-eye code reads performed. Itemized in the PR description. Proceeded per unattended-run instruction. |
| Review | ➖ **not run** | No `reviews/` directory for this feature. |
| QA | ➖ **not run** | No `qa/` directory for this feature. |
| Working tree | ✅ **clean** | Nothing to commit before the pre-flight check; no stash needed. |
| Branch sync | ✅ **0 behind, 8 ahead** | No rebase required. `origin/main` was already the merge-base. |
| CI | ⚠️ **none configured** | No `.github/workflows/`; `gh run list --branch 20260806-120325-section-1-docs` returned no runs. Local gate run instead. |

**Overall**: READY TO SHIP (with notes)

## Gate run (local, at head)

| Gate | Result |
|---|---|
| `cargo test` | 55 passed, 0 failed (baseline 42 — 13 new tests) |
| `cargo clippy --all-targets -- -D warnings` | clean |
| `pnpm test` (vitest) | 30 passed, 0 failed (unchanged; no frontend file modified) |

## Artifacts

- Spec: `specs/20260806-120353-concurrency-hardening/spec.md`
- Plan: `specs/20260806-120353-concurrency-hardening/plan.md`
- Tasks: `specs/20260806-120353-concurrency-hardening/tasks.md`
- Research: `specs/20260806-120353-concurrency-hardening/research.md`
- Data model: `specs/20260806-120353-concurrency-hardening/data-model.md`
- Quickstart: `specs/20260806-120353-concurrency-hardening/quickstart.md`
- Contracts: `specs/20260806-120353-concurrency-hardening/contracts/` (command-surface, lock-recovery, store-mutation-api)
- Checklist: `specs/20260806-120353-concurrency-hardening/checklists/requirements.md`
- Review report: none
- QA report: none

## Screenshots

Six after images, no baseline. `screenshots/manifest.json` records the reason: the before pass ran in a VS Code tunnel session where macOS screen capture was blocked (`CGGetActiveDisplayList` returned 0 active displays, every `screencapture` output was 100% black). That was an environment limitation, not a defect in the base branch. The block had lifted by the after run, but a baseline cannot be reconstructed retroactively.

All six after images were verified non-black pixel-by-pixel before being kept. Captured views: `site-list`, `add-error`, `warning-banner` — each at 720×480 and 480×320. Embedded in the PR description as pinned-SHA blob URLs (`.../blob/9cd7f33.../...?raw=true`) rather than `raw.githubusercontent.com`, because the repository is private and raw URLs do not render there; the SHA is pinned rather than the branch name so the images survive branch deletion after merge.

## Changelog

Entry "Concurrency & robustness hardening — 2026-08-06" prepended to `CHANGELOG.md` in commit `9cd7f33`. It documents the three fixes (poison recovery via `SharedStore`, the `AddError` refusal/write-failure split, and the read-decide-write collapse into `Store::replace`) plus technical notes on the new `lock.rs` module, the 10→9 lock-site count, and the unchanged IPC contract.

## Notes for the merger

- `docs/ROADMAP.md` §1 was closed out **in the primary checkout, not this worktree** — `docs/` is gitignored, so that edit does not appear in this PR's diff. This was deliberate (research R10): the previous feature lost its roadmap edit by making it inside a worktree.
- The feature directory name (`20260806-120353-concurrency-hardening`) does not match the branch name (`20260806-120325-section-1-docs`). The branch slug was derived from the original description's leading words ("section 1 of docs/ROADMAP.md") before the feature was named.
- `main`'s `.specify/feature.json` is stale (points at `specs/003-durability`). The correct feature resolves only from inside the worktree.
