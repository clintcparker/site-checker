# Release Record: Clickable URLs Open in the Default Browser

**Shipped**: 2026-08-13 12:33:53 · **Run**: unattended (`/speckit-ship-run`)

## Pull Request

- **URL**: https://github.com/clintcparker/site-checker/pull/33
- **Title**: `feat: clickable URLs — a site's URL is now a control that opens it in the default browser`
- **Base**: `main` · **Head**: `20260813-105424-urls-clickable-and`
- **Description**: [pr-description-20260813-123353.md](./pr-description-20260813-123353.md)

## Commit Range

`f18b947a0c382f04327c3b611078794d8ea77aa0..2253ab851580e9f28d4598a032ad16f6e9ab6e69`

11 commits · 48 files changed (+2983 −20). Branch was 0 commits behind `origin/main`, so **no rebase or merge was performed**. Nothing was force-pushed.

## Feature Identity — how it was pinned

Resolved **from the run context only**, per the run instructions.

| Field | Value |
|---|---|
| `branch` | `20260813-105424-urls-clickable-and` |
| `feature_dir` | `specs/20260813-105424-urls-clickable-and` |
| `worktree_path` | `/Users/clint/src/clintcparker/site-checker--20260813-105424-urls-clickable-and` |

**The run context was not at any of the three documented lookup locations.** `$SPECIFY_INIT_DIR` was unset; neither the current directory nor the primary checkout contained `.specify/run-context.json`. It was found inside the worktree, because the file is *tracked* and was introduced by this feature's own specify commit (`fe38065`) — so it exists only on the feature branch, and the primary checkout was standing on `main`.

The file is flagged `"reconstructed": true` (the worktrees extension ships only `create-worktree.sh` and has no run-context writer).

Corroboration performed before any write:

- Only `run-context.json` anywhere under the repo — all 12 worktrees scanned plus a repo-wide `find`.
- Self-consistent: the named worktree exists, is on the named branch, and holds the named feature dir.
- Its branch had 10 unmerged commits, was unpushed, and had no PR.
- No other worktree branch had more than 1 unmerged commit.
- The screenshots manifest independently recorded the same resolution (note 1).

**Rejected inference**: `.specify/feature.json` in the primary checkout named `specs/20260811-141602-ci-node24-action-bumps` — a different, already-shipped feature. That is exactly the drift the instruction guards against; it was not used.

## Worktree Isolation

| Field | Reported |
|---|---|
| `worktree_isolation` | not reported — no `failed`, no `recovered` |
| `run_context` | not reported — no `collision` |
| session | ran from the **primary checkout** (on `main @ f18b947`), addressing the worktree by absolute path rather than via a `SPECIFY_INIT_DIR` override |

## Readiness Check Results

| Check | Result |
|---|---|
| Tasks | ✅ 27 / 27 complete — none outstanding |
| Review | ⚠️ APPROVED WITH CONDITIONS — 0 blockers, 2 warnings, 4 suggestions; **both warnings since closed by `bf30c32`** |
| QA | ✅ ALL PASSED (second pass); 27/28 acceptance criteria fully validated, 1 partial (SC-003) |
| Working tree | ✅ Clean — nothing to commit before pre-flight |
| Base ref | ✅ local `main` == `origin/main` == `f18b947`; branch 0 behind |
| Quality gates | ✅ `cargo test` 101 · `pnpm test` 80 · `clippy -D warnings` clean · `tsc --noEmit` clean |

**Overall**: READY TO SHIP (with notes).

Note: the primary checkout carries an untracked `.playwright-mcp/` directory. It is unrelated to this feature and sits on `main`, not in this branch's tree, so it was deliberately **not** committed.

## CI

No run existed when the PR was opened — `ci.yml` triggers on `push` to `main` and on `pull_request`, not on a feature-branch push. A run started with the PR:

- Run `31736623566` — **in_progress** at ship time, not waited on per the run instructions.

## Changelog

Prepended to [CHANGELOG.md](../../../CHANGELOG.md) as *"A site's URL is now a control that opens it — 2026-08-13"*, in the repo's existing prose style. Committed as `2253ab8`.

## Artifacts

| Artifact | Path |
|---|---|
| Spec | [spec.md](../spec.md) |
| Plan | [plan.md](../plan.md) |
| Research | [research.md](../research.md) |
| Data model | [data-model.md](../data-model.md) |
| Tasks | [tasks.md](../tasks.md) |
| Review | [reviews/review-20260813-115021.md](../reviews/review-20260813-115021.md) |
| QA | [qa/qa-20260813-121200.md](../qa/qa-20260813-121200.md) |
| Screenshots | [screenshots/manifest.json](../screenshots/manifest.json) |

**Gitignored in this repo** — `specs/*/screenshots/`, `specs/*/qa/responses/`, `specs/*/qa/screenshots/`, and `specs/*/releases/`. All were committed with `git add -f` so their links resolve. All 11 screenshot URLs pinned in the PR description were verified to return HTTP 200 at head `2253ab8`.

Repository visibility is **PUBLIC** (`gh repo view`), so screenshots are embedded as `raw.githubusercontent.com` URLs pinned to the head SHA rather than the branch name.

## Surviving Findings

Carried into the PR under *Known issues*: R003 (failed activation locked out for 1000 ms), R004 (`#[tauri::command(async)]` blocking a runtime worker — no action recommended), R005 (no `aria-label` on the control), R006 (no `document.activeElement` regression test; behaviour verified by QA, guard missing).

Environment gaps: SC-003's *visible* browser activation is unverified (Accessibility and Screen Recording both refused this run); WKWebView was never the engine under test — all browser QA and all `after/` screenshots ran in Chromium.

Eight open decisions taken without a user present (3 from spec.md, 5 from plan.md) are listed in the PR for confirmation.

---
*Generated by `/speckit.ship`.*
