# Release Record: Launch-at-login survives upgrades

**Shipped**: 2026-08-12 21:24 PDT (unattended run — no user present)
**Feature**: `20260812-202608-autostart-launchagent-path`
**Issue**: [#25](https://github.com/clintcparker/site-checker/issues/25)
**PR**: [#31 — fix: launch at login survives a brew upgrade — register the version-independent path](https://github.com/clintcparker/site-checker/pull/31)

## Pinned run context

Resolved from `.specify/run-context.json` in the primary checkout — **not** inferred from the
current branch, `.specify/feature.json`, or the newest directory under `specs/`.

| Field | Value |
|---|---|
| `branch` | `20260812-202608-autostart-launchagent-path` |
| `feature_dir` | `/Users/clint/src/clintcparker/site-checker--20260812-202608-autostart-launchagent-path/specs/20260812-202608-autostart-launchagent-path` |
| `worktree_path` | `/Users/clint/src/clintcparker/site-checker--20260812-202608-autostart-launchagent-path` |
| `primary_checkout` | `/Users/clint/src/clintcparker/site-checker` |
| `base_ref` / `base_sha` | `origin/main` / `1a711b7626c98828b54bdd358671bc61178b2790` |
| `session` | `primary` |
| `worktree_isolation` | `created` |

**Provenance flags carried into the PR**: `session=primary` (the branch is in its own worktree,
but the agent session ran from the primary checkout via `SPECIFY_INIT_DIR` — worktree entry needs
interactive approval an unattended run cannot give). `worktree_isolation=created` and no
`run_context=collision` — neither needed mention. `base_ref` equalled local `main` at
`1a711b7`, so the branch is not missing commits a reviewer holds locally. No feature inputs are
gitignored: every artifact under `feature_dir`, including `qa/responses/` and
`screenshots/SKIPPED.md`, is tracked and present in the pushed head (`git check-ignore` clean).

## Commit range

`1a711b7626c98828b54bdd358671bc61178b2790..08e501f716b29a4ed5a8e168f93546aea9de51ad`

11 commits · 31 files · +3783 / −62

```
08e501f docs(changelog): record launch-at-login upgrade survival
f581832 docs(screenshots): confirm after-mode skip for launch-at-login upgrade survival
a2f95cf docs(qa): add QA report for launch-at-login upgrade survival
fe9db51 docs(review): add staff review report for launch-at-login upgrade survival
a6f94b6 docs(specs): record upgrade-survival behaviour and implementation notes
bf3387c docs: name the LaunchAgent in both uninstall surfaces
dd19c3e fix(autostart): record a login-item path that survives brew upgrade
30955da docs(screenshots): skip screenshots for launch-at-login upgrade survival
dedf03b docs(tasks): add task list for launch-at-login survives upgrades
f0fa64f docs(plan): add implementation plan for launch-at-login upgrade survival
8fd1f08 docs(specify): add specification for launch-at-login upgrade survival
```

Note: the TC-013 / W002 remediation to `src-tauri/src/lib.rs` and `src-tauri/src/commands.rs`,
plus the `quickstart.md` §2d correction (W003), are folded into `a2f95cf` — they were applied
during the QA run, not as separate commits.

## Readiness check results

| Gate | Result |
|---|---|
| Tasks | ✅ 34 / 34 complete |
| Review | ⚡️ APPROVED WITH CONDITIONS — [review-20260812-210615.md](../reviews/review-20260812-210615.md) |
| QA | ⚡️ PARTIAL PASS — [qa-20260812-211740.md](../qa/qa-20260812-211740.md) |
| Working tree (worktree) | ✅ Clean before and after the CHANGELOG commit |
| Branch sync | ✅ 0 behind `origin/main` — no rebase or merge needed |
| Screenshots | ⏭️ `SKIPPED.md` — no UI surface touched |
| CI | ⚠️ No run existed at PR creation; not waited on per ship instruction |

**Overall**: READY TO SHIP (with notes). All confirmations treated as YES — unattended run.

## Local quality gates on `08e501f`

`cargo test` 95/95 · `cargo clippy -D warnings` clean · `pnpm test` 47/47 · out-of-tree QA harness 23/23

## Surviving findings carried into the PR

- **W001** 🟡 open — the upstream-template-drift guard `plan.md` and T018 promise does not exist; `plist_template` is a hand-written test-local copy that already differs from `auto-launch` 0.5.0's real output. `auto-launch = "0.5"` is a caret requirement, so a future `cargo update` can silently disable repair with every gate green.
- **S001** 🟢 open — the plist contract's "byte for byte" claim is inaccurate (trailing newline).
- **S002** 🟢 open — `lib.rs` and `AutoLaunch::enable()` reach `$HOME` through two different `dirs` crate versions in the same lockfile.
- **S003** 🟢 open — 151 lines of `.specify/extensions/screenshots/screenshots-config.yml` ride along, uncovered by any task and unrelated to #25.
- **SC-001 / SC-003 verification gaps** — a real `brew install` and `brew upgrade` need a released bottle containing this change. The first release after this merges is the first chance to confirm the fix against an actual upgrade.
- **TC-013 fix untested** — the remediation lives in the Tauri shell, which has no unit-test harness; verified by compilation, static greps and reading only.

**Resolved during the single remediation pass**: W002 / TC-013 (startup abort on a non-swallowed
error path) and W003 (`quickstart.md` §2d false-pass instruction).

## Changelog

Entry prepended to [CHANGELOG.md](../../../CHANGELOG.md) as
*"Launch at login survives a `brew upgrade` — 2026-08-12"* in commit `08e501f`.

## Next steps

1. Verify the CI run this PR triggered before merging — none existed at creation time.
2. Decide on W001: retract the drift claim and pin `auto-launch = "=0.5.0"`, or add a
   `cargo test`-resident check against the real template.
3. After the first release containing this change, run `quickstart.md` §3 and §4 — a real
   `brew install` and `brew upgrade` — to close SC-001 and SC-003.
