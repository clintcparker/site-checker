# Ship Record: Packaging & Distribution

**Shipped**: 2026-08-06 20:21 PDT
**Mode**: unattended run — every confirmation auto-answered YES
**Feature**: [spec.md](../spec.md)
**PR**: [#7 — feat: packaging & distribution — install with one brew command, release with one tag](https://github.com/clintcparker/site-checker/pull/7)
**PR description**: [pr-description-20260806-202120.md](pr-description-20260806-202120.md)

---

## What shipped, stated honestly

**Track A only.** The pipeline is built and statically proven; packaging is not live.
`brew install clintcparker/tap/site-checker` does not work today — no `v1.0.0` exists, the
repository is private, and none of the seven credentials is set. FR-001, FR-006, FR-015, FR-027
and the runtime half of FR-005 are deferred pending two maintainer decisions (T001: make the
repository public; T002: Apple Developer Program, $99/yr).

This section exists because it is condition #1 of the staff review, which asked the ship record
and the PR to say so plainly rather than letting "the YAML merged" read as "packaging shipped."

**Do not tag `v1.0.0` from this branch** (review condition #2). Pre-flight would correctly reject
it, but a version number burned on a credential probe cannot be reused.

## Readiness check results

| Gate | Result | Detail |
|---|---|---|
| Tasks | ⚠️ 32/47 (68%) | 15 open — all maintainer-gated, credential-gated, post-merge, or blocked on a broken local Homebrew. No unfinished implementation work. Enumerated in the PR description. |
| Staff review | ⚡️ APPROVED WITH CONDITIONS | 0 blockers · 6 warnings · 5 suggestions. [review-20260806-195824.md](../reviews/review-20260806-195824.md) |
| QA | ⚡️ PARTIAL PASS | 40 scenarios · 24 pass · **0 fail** · 6 partial · 10 blocked · 0 new defects. [qa-20260806-201154.md](../qa/qa-20260806-201154.md) |
| Working tree | ✅ Clean | After committing the review + QA artifacts as part of this run |
| Standing gates | ✅ Unmoved | 55 Rust · 30 frontend · clippy clean · lockfile clean · actionlint exit 0 |
| Screenshots | ⏭️ Skipped | `screenshots/SKIPPED.md`; no UI surface. Judged at `before`, re-verified at `after`, independently re-checked by QA. |

**Neither the review nor the QA report was remediated before this ship.** All 11 review findings
and all 6 QA partials survive, and every one is reproduced under "Known issues" in the PR
description. That was the instructed behaviour for this run, not an oversight.

## Git

| | |
|---|---|
| Branch | `20260806-190127-packaging-and-distribution` → `main` |
| Commit range | `5faa2d69bde12e0d46bbad9a19830476e20cb6be..b95ebc8dc9e7bbe6b6bb39db41cad151db5f3426` |
| Commits | 8 |
| Diff | 39 files changed, +5049 / −8 |
| Remote | `origin` — `git@github.com:clintcparker/site-checker.git` (**PRIVATE**) |

Commits in the range:

```
b95ebc8 docs: state plainly in the changelog that packaging is not yet live   ← added by ship
bef100f docs: staff review and QA reports for packaging & distribution        ← added by ship
93401fa docs: after screenshots skip verified for packaging & distribution
dbc059e chore: record implement progress (32/47) for the companion GUI
09bcd74 docs: spec artifacts and task status for packaging & distribution
2b6ca13 feat: publish Site Checker through a Homebrew cask, driven by one tag
be50fa4 build: collapse three version sources to one tag-written sentinel
f614617 docs: screenshots skipped — packaging & distribution changes no UI surface
```

### Synchronization

Rebased onto `origin/main` (branch was 1 commit behind `5faa2d6`). **One conflict**, in
`.specify/feature.json` — a single-line spec-kit pointer where both sides named this same feature,
differing only in absolute vs. repo-relative path form. Resolved by taking `origin/main`'s content;
the branch's own edit to that file was redundant. No source file was involved.

**No force-push was performed.** This was the branch's first push to the remote.

### Environment notes carried forward

- **Worktree isolation**: the branch lives in its own worktree at
  `/Users/clint/src/clintcparker/site-checker--20260806-190127-packaging-and-distribution`, so the
  branch was properly isolated. This ship run's *session* executed from the primary checkout
  (`/Users/clint/src/clintcparker/site-checker`, on `main`), resolving `FEATURE_DIR` through the
  absolute path in `.specify/feature.json`. Every git operation was explicitly directed at the
  worktree path rather than relying on the working directory. No worktree-step report was carried
  into this invocation; this is observed state.
- **Base ref**: local `main` and `origin/main` were identical at `5faa2d6`. The branch contains
  everything a reviewer has locally.
- **Gitignored feature inputs**: `.gitignore:28` is `docs/*`, so `docs/ROADMAP.md` (the §2 source
  of this entire spec, and the target of the FR-025 edit) and
  `docs/superpowers/plans/2026-07-23-site-checker.md` (FR-026) are **absent from the branch and
  invisible to reviewers**. Both edits were made in the primary checkout as the plan requires, and
  both review and QA confirmed them present and honest there (TC-015) — but they are
  attested-by-review, not reviewable in the PR.

## CI

`ci.yml` triggers on `push: branches: [main]` and `pull_request`, so pushing the branch fired
nothing. Opening PR #7 produced the workflow's **first execution anywhere**:

- Run [`31144193749`](https://github.com/clintcparker/site-checker/actions/runs/31144193749) —
  queued at the time this record was written.
- Two checks, `rust` and `frontend` — not four. GitHub surfaces one check per job; the four
  constitutional gates are named steps inside those two. Exactly as QA TC-039 predicted, and a
  reason to adjust T035's wording rather than the workflow.

The ship did not block on CI. **Both checks must be confirmed green before merging** — this run is
the only evidence that `ci.yml` itself works.

## Changelog

`CHANGELOG.md` already carried a full entry for this feature, written during implementation and
confirmed conventional by QA TC-016, so no entry was prepended. One paragraph was **added** in
`b95ebc8`: the entry opened "Site Checker gets an install path", which reads as though the install
line works. It now states plainly that it does not, names the two gating decisions, and lists the
deferred requirements.

## Surviving findings

Reproduced in full under "Known issues" in the PR description. Summary:

**Warnings** — R001 (verification is blind to checksum drift — highest value), R003 (`workflow_run`
has no success filter, so a `macos-latest` runner fires after failed releases), R002 (the tap check
races a CDN cache; research R12's premise is measurably wrong and should be corrected in place),
R004 (signing credentials materialised before `pnpm install` runs lifecycle scripts), R005
(`contents: write` is file-scoped and persisted into `.git/config`), R006 (`brew audit --cask
--new` never run; nothing validates the rendered cask is valid Ruby before it reaches the public
tap).

**Suggestions** — R007 (`fail_on_unmatched_files` unset), R010 (no `concurrency:` group), R011
(annotated tag unenforced), R008 (repository name hardcoded in one of two places), R009 (SC-010's
wording is out of step with `trash:` semantics — amend the spec, not the code).

**Environment** — Homebrew on the dev machine is non-functional (`brew config` aborts in the
vendored `json-2.21.1` gem after an interrupted vendored-gem install during T017). The repair
command is in `tasks.md`; it is an `rm -rf` against the user's Homebrew outside this repository and
was deliberately not run unattended. When repairing it: **do not pipe `brew audit`'s first run** —
that is what broke it.

## Artifact links

| Artifact | Path |
|---|---|
| Specification | [spec.md](../spec.md) |
| Plan | [plan.md](../plan.md) |
| Tasks | [tasks.md](../tasks.md) |
| Research | [research.md](../research.md) |
| Data model | [data-model.md](../data-model.md) |
| Quickstart | [quickstart.md](../quickstart.md) |
| Contracts | [contracts/](../contracts/) |
| Checklist | [checklists/requirements.md](../checklists/requirements.md) |
| Staff review | [reviews/review-20260806-195824.md](../reviews/review-20260806-195824.md) |
| QA report | [qa/qa-20260806-201154.md](../qa/qa-20260806-201154.md) |
| QA transcripts | [qa/responses/](../qa/responses/) |
| Screenshots | [screenshots/SKIPPED.md](../screenshots/SKIPPED.md) |
| PR description | [pr-description-20260806-202120.md](pr-description-20260806-202120.md) |

## Next steps

1. Confirm PR #7's `rust` and `frontend` checks go green.
2. Answer T001 (public repository) and T002 (Apple Developer Program). Everything else waits on these.
3. Address the R001 / R003 / R002 warnings before the first real release — R001 in particular,
   since it is blind in the direction that breaks users rather than the maintainer.
4. Merge, then T043: `git tag -a v1.0.0` — **only after step 2 is answered.**
5. Redefine "done" deliberately once the answers land (research R10, review condition #3).

---
*Generated by `/speckit.ship` — release engineering for spec-driven development.*
