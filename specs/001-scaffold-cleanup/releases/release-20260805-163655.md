# Release Record — Scaffold Cleanup

**Date**: 2026-08-05
**Feature**: `001-scaffold-cleanup`
**Run mode**: unattended (`/speckit-ship-run`, all confirmations pre-answered yes)

## Pull Request

- **URL**: https://github.com/clintcparker/site-checker/pull/1
- **Number**: #1
- **Title**: `feat: Scaffold Cleanup — remove create-tauri-app residue and sharpen two imprecise strings`
- **Base → Head**: `main` ← `20260805-150909-cleanup-scaffold-leftovers`
- **Description**: [`pr-description-20260805-163655.md`](./pr-description-20260805-163655.md)

## Commit Range

- **Base SHA** (`origin/main`): `2204c4bc6b486a4b1e6dbab1eea75b40fa4d5850`
- **Head SHA** (pushed): `dc781600505ac26d71daa564ecf6a23554ef2b67`
- **Range**: `2204c4b..dc78160`
- **Commits**: 12
- **Files changed**: 233 total — of which **29 are the feature**. The remaining ~204 come
  from `bca6937 "added speckit"`, an unpushed local-`main` commit the feature branch forked
  from, which installs the spec-kit tooling. Diff against `bca6937` to review the feature alone.

### Commits in this release

```
dc78160 docs: add a changelog with the scaffold-cleanup entry
e5ed274 docs: add the branch-slug spec artifact set for the cleanup feature
d68259e docs: after screenshots for 001-scaffold-cleanup (manifest only — capture blocked)
55641c6 chore(companion): record implement progress in .spec-context.json
c82667f docs: add the scaffold-cleanup spec, plan, and completed task list
3f3e37c docs: follow the crate rename through the screenshots capture docs
be67f9c docs: make two imprecise strings say what they mean
5c99f25 chore: identify the package and crate as Site Checker
be5464e chore: delete orphaned scaffold SVGs
522367e chore: remove the unregistered opener plugin
693ff82 docs: before screenshots (manifest only — capture blocked)
bca6937 added speckit
```

## Readiness Check Results

| Check | Result |
|---|---|
| Tasks | ⚡️ **28/29 complete** — T029 (final gate + open PR) was closed by this run |
| Review | ➖ No `reviews/` directory — `/speckit.review` was never run |
| QA | ➖ No `qa/` directory — `/speckit.qa` was never run |
| Working tree | ✅ Clean (after committing the outstanding spec artifacts as `e5ed274`) |
| Branch sync | ✅ 0 commits behind `origin/main` — **no rebase or merge was needed** |
| CI | ➖ **None configured** — no `.github/workflows/`, no runs on this branch |

**Overall**: SHIPPED WITH NOTES.

## Quality Gates (run locally against `dc78160`)

| Gate | Result |
|---|---|
| `cargo test` (in `src-tauri/`) | ✅ 29 passed, 0 failed |
| `pnpm test` (Vitest + happy-dom) | ✅ 12 passed across 2 files |
| `cargo clippy --all-targets -- -D warnings` | ✅ clean, zero warnings |

### Success criteria re-verified at ship time

- **SC-002** — `grep -rn "opener" …` → no matches ✅
- **SC-003** — `grep -rnE '"tauri-app"\|tauri_app_lib\|A Tauri App\|authors = \["you"\]' …` → no matches ✅
  (a loose `tauri-app` grep still hits `@tauri-apps/api` and `@tauri-apps/cli` — those are the
  Tauri org scope, not the scaffold name)
- **SC-004** — `src/assets/` no longer exists; it was empty after the three SVG deletions ✅
- **SC-005** — `tauri.conf.json` still pins `Site Checker` / `com.clintparker.site-checker` ✅

## Artifacts

- Spec: [`../spec.md`](../spec.md)
- Plan: [`../plan.md`](../plan.md)
- Tasks: [`../tasks.md`](../tasks.md)
- Screenshots manifest: [`../screenshots/manifest.json`](../screenshots/manifest.json)
- Supplementary artifact set (earlier pass, same roadmap section):
  [`../../20260805-150909-cleanup-scaffold-leftovers/`](../../20260805-150909-cleanup-scaffold-leftovers/)
- Changelog: [`../../../CHANGELOG.md`](../../../CHANGELOG.md) — created by this run; the repo had none
- Review report: none
- QA report: none

## Screenshots

**No images were captured, on either side of the pair.** The manifest exists; the
`screenshots/` directory holds nothing else. Cause: macOS TCC denies the agent's host
process (a VS Code tunnel/remote server, which has no GUI attribution) both the Screen
Recording grant `screencapture` needs and the Automation → System Events grant window
positioning needs. Reconfirmed against the live app window, not assumed.

The pair is empty but internally consistent — same views, same viewports, same seed data,
same blocker on both sides — and the manifest's own implementation verification returns
`verdict: pass`. Three views were planned and remain worth capturing once a human grants
the permissions: `site-list`, `corrupt-file-banner`, `unreadable-file-banner`, each at
`720×480` (default) and `480×320` (narrow).

## Follow-Ups

1. **A human should visually confirm the app before merge.** T028's FR-007 check was
   completed from indirect evidence only, for the TCC reason above.
2. Consolidate the two spec directories for this one feature.
3. Consider adding CI — this repo has no automated gate at all.
4. `origin/main` is behind local `main`; pushing `main` separately would shrink this PR's
   diff to just the 29 feature files.
5. The primary checkout at `/Users/clint/src/clintcparker/site-checker` still has untracked
   `specs/001-scaffold-cleanup/`, `.specify/feature.json`, and `.no-wiki-capture` on `main`.
   The first two are now committed on the feature branch, so a `git merge` after this PR
   lands will refuse to overwrite them — delete them from the primary checkout first.
